name: Schema Reference Guard

on:
  pull_request:
    branches:
      - main

jobs:
  schema-ref-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure schema_ref.sql unchanged
        run: |
          git diff --exit-code project/schema_ref.sql
