# ===== SCALP PROJECT DUMP =====
# 2025-11-21 14:02:46 UTC
# Root: /opt/scalp

========== TREE ==========
/opt/scalp/dp.sh                                                                         3217 2025-11-11 13:58:24.801071437 +0100
/opt/scalp/project/bin/OB_collect.sh                                                     1378 2025-11-14 13:11:08.592861780 +0100
/opt/scalp/project/bin/a_analyse.sh                                                       110 2025-10-28 20:36:25.965060683 +0100
/opt/scalp/project/bin/agg_3m_from_1m.sh                                                 1182 2025-11-12 13:37:13.902457360 +0100
/opt/scalp/project/bin/b_maint.sh                                                         653 2025-11-20 11:37:27.631856374 +0100
/opt/scalp/project/bin/b_maint_safe.sh                                                   1694 2025-11-20 11:40:09.264238604 +0100
/opt/scalp/project/bin/b_purge_except_feat.sh                                             773 2025-11-19 15:15:01.725814442 +0100
/opt/scalp/project/bin/cleanup_b_db.sh                                                    733 2025-11-19 15:25:54.434416520 +0100
/opt/scalp/project/bin/cleanup_scalp_services.sh                                         1565 2025-11-18 19:17:13.874597682 +0100
/opt/scalp/project/bin/closer_clean.sh                                                    230 2025-10-31 12:42:57.348097711 +0100
/opt/scalp/project/bin/dash.sh                                                           1551 2025-10-31 13:04:23.699099491 +0100
/opt/scalp/project/bin/dash_b_feat.sh                                                    2313 2025-11-15 13:12:00.992184978 +0100
/opt/scalp/project/bin/dash_oa_age.sh                                                     320 2025-11-19 09:35:55.041472774 +0100
/opt/scalp/project/bin/dash_ob_feat.sh                                                   2111 2025-11-15 14:33:14.428132131 +0100
/opt/scalp/project/bin/dash_ob_feat_coin.sh                                              1277 2025-11-15 15:37:58.956108726 +0100
/opt/scalp/project/bin/dash_ofcr.sh                                                       387 2025-11-17 20:51:03.724520947 +0100
/opt/scalp/project/bin/dash_pipeline_health.sh                                            621 2025-11-19 09:36:03.317484515 +0100
/opt/scalp/project/bin/dash_ws_age.sh                                                     331 2025-11-19 09:35:47.481462029 +0100
/opt/scalp/project/bin/db_purge_clean.sh                                                 2311 2025-11-19 10:46:57.663257648 +0100
/opt/scalp/project/bin/db_purge_scalp.sh                                                 2408 2025-11-19 10:52:50.327340646 +0100
/opt/scalp/project/bin/db_reset_scalp.sh                                                 1811 2025-11-19 10:54:24.547370235 +0100
/opt/scalp/project/bin/follower_clean.sh                                                  236 2025-10-31 12:26:25.059199562 +0100
/opt/scalp/project/bin/gest_checker.sh                                                    186 2025-11-17 16:51:18.726866398 +0100
/opt/scalp/project/bin/maint_ohlcv.sh                                                     845 2025-11-12 13:49:15.015239620 +0100
/opt/scalp/project/bin/oa_health.sh                                                       418 2025-11-05 20:06:08.395688113 +0100
/opt/scalp/project/bin/oa_ohlcv.sh                                                         89 2025-10-28 20:36:25.965060683 +0100
/opt/scalp/project/bin/oa_ohlcv_A.sh                                                       89 2025-10-28 20:36:25.965060683 +0100
/opt/scalp/project/bin/ob_aggregate_3m.sh                                                 790 2025-11-12 17:29:03.507453212 +0100
/opt/scalp/project/bin/ob_check.sh                                                        259 2025-10-30 08:14:25.694016732 +0100
/opt/scalp/project/bin/ob_feat.sh                                                          86 2025-10-28 20:36:25.965060683 +0100
/opt/scalp/project/bin/ob_freshness.sh                                                    967 2025-11-15 09:57:02.846778683 +0100
/opt/scalp/project/bin/ob_ohlcv_B.sh                                                       89 2025-10-28 20:36:25.969060690 +0100
/opt/scalp/project/bin/ob_ws_hard_stop.sh                                                 737 2025-11-13 12:56:23.523425385 +0100
/opt/scalp/project/bin/ob_ws_singleton.sh                                                 376 2025-11-13 13:10:02.248376041 +0100
/opt/scalp/project/bin/opener_clean.sh                                                    235 2025-10-31 12:22:06.630898758 +0100
/opt/scalp/project/bin/pipeline_diagnostics.sh                                           1109 2025-11-04 11:03:10.048776721 +0100
/opt/scalp/project/bin/recorder_clean.sh                                                  237 2025-10-31 12:51:33.860668329 +0100
/opt/scalp/project/bin/relaunch_ohlcv_feat.sh                                            2036 2025-11-13 13:32:53.721090821 +0100
/opt/scalp/project/bin/reset_b_db.sh                                                      989 2025-11-19 15:30:53.678666906 +0100
/opt/scalp/project/bin/run_a_ctx.sh                                                       111 2025-11-14 19:08:22.448736032 +0100
/opt/scalp/project/bin/run_a_feat.sh                                                      124 2025-11-19 08:34:51.153047417 +0100
/opt/scalp/project/bin/run_b_feat.sh                                                      522 2025-11-18 18:37:34.428433320 +0100
/opt/scalp/project/bin/run_oa.sh                                                          291 2025-11-18 19:47:56.871777864 +0100
/opt/scalp/project/bin/run_ofcr_test.sh                                                  1327 2025-11-20 18:19:41.571184222 +0100
/opt/scalp/project/bin/safe_agg_3m.sh                                                     379 2025-11-12 13:55:07.967580974 +0100
/opt/scalp/project/bin/setup_ofcr.sh                                                     1383 2025-11-17 20:55:13.792742430 +0100
/opt/scalp/project/bin/sync_and_collect_1m.sh                                             482 2025-11-12 13:41:10.846719057 +0100
/opt/scalp/project/bin/sync_ctx_a_to_b.sql                                                424 2025-11-03 11:10:43.794183609 +0100
/opt/scalp/project/bin/t_db_clean.sh                                                      649 2025-11-19 10:57:47.035539863 +0100
/opt/scalp/project/bin/t_db_reset_full.sh                                                 674 2025-11-19 10:58:48.371660789 +0100
/opt/scalp/project/bin/t_ticks.sh                                                         117 2025-10-30 14:42:24.525733094 +0100
/opt/scalp/project/bin/triggers_db_clean.sh                                               652 2025-11-19 10:56:25.919411485 +0100
/opt/scalp/project/bin/u_universe.sh                                                       89 2025-10-28 20:36:25.969060690 +0100
/opt/scalp/project/bin/x_dash.sh                                                         1597 2025-11-11 15:00:56.760521172 +0100
/opt/scalp/project/config/scalp.conf                                                       18 2025-10-25 17:08:55.549075009 +0200
/opt/scalp/project/data/schema_ref.sql                                                  15128 2025-11-21 15:00:27.583807540 +0100
/opt/scalp/project/requirements.txt                                                       113 2025-10-30 08:19:30.318294376 +0100
/opt/scalp/project/scripts/A_analyse.py                                                  1601 2025-10-28 22:19:44.465383115 +0100
/opt/scalp/project/scripts/A_config.py                                                    336 2025-10-28 22:19:44.465383115 +0100
/opt/scalp/project/scripts/A_ctx.py                                                      4912 2025-11-15 09:22:29.397400061 +0100
/opt/scalp/project/scripts/A_ctx_dash.py                                                 3531 2025-11-15 09:53:29.870639255 +0100
/opt/scalp/project/scripts/A_feat_builder.py                                             2740 2025-11-14 20:35:55.969947110 +0100
/opt/scalp/project/scripts/B_feat_builder_full.py                                        3720 2025-11-18 18:37:20.488424637 +0100
/opt/scalp/project/scripts/B_feat_builder_incremental.py                                 3478 2025-11-18 18:37:01.924413218 +0100
/opt/scalp/project/scripts/H_perf.py                                                     2240 2025-10-28 22:19:44.465383115 +0100
/opt/scalp/project/scripts/H_score.py                                                     368 2025-10-28 22:19:44.465383115 +0100
/opt/scalp/project/scripts/OA_HYBRID.py                                                  4408 2025-11-19 09:33:58.301304557 +0100
/opt/scalp/project/scripts/OA_ohlcv_A.py                                                 3433 2025-11-19 10:24:31.328616604 +0100
/opt/scalp/project/scripts/OB_collect.py                                                 7344 2025-11-17 14:40:23.704407969 +0100
/opt/scalp/project/scripts/OB_collect_active.py                                          3567 2025-11-12 09:24:02.487342442 +0100
/opt/scalp/project/scripts/S_cleanup_signals.sh                                           267 2025-10-28 09:52:03.409264353 +0100
/opt/scalp/project/scripts/S_cleanup_ticks.sh                                             147 2025-10-28 10:25:52.647287857 +0100
/opt/scalp/project/scripts/T_ticks.py                                                    3212 2025-11-14 18:08:45.736693102 +0100
/opt/scalp/project/scripts/T_ticks_debug.py                                              1378 2025-10-30 12:41:26.768642739 +0100
/opt/scalp/project/scripts/T_ticks_test_raw.py                                           1250 2025-10-30 12:43:49.868791255 +0100
/opt/scalp/project/scripts/U_universe.py                                                  572 2025-10-28 22:19:44.469383119 +0100
/opt/scalp/project/scripts/WS_candles_to_wsdb.py                                         4696 2025-11-19 09:31:56.837123422 +0100
/opt/scalp/project/scripts/WS_multi_candles.py                                           3379 2025-11-19 09:20:25.475861499 +0100
/opt/scalp/project/scripts/b_dash.py                                                     2112 2025-10-29 08:11:52.064666231 +0100
/opt/scalp/project/scripts/budget.py                                                      666 2025-11-11 14:50:25.312129791 +0100
/opt/scalp/project/scripts/budget_updater.py                                             3836 2025-11-08 10:36:43.054525766 +0100
/opt/scalp/project/scripts/closer.py                                                     3728 2025-11-20 18:04:53.854353806 +0100
/opt/scalp/project/scripts/closer_finalize.py                                            3029 2025-11-09 08:04:51.274529804 +0100
/opt/scalp/project/scripts/follower.py                                                   5575 2025-11-20 18:01:18.670142114 +0100
/opt/scalp/project/scripts/gen_schema_ref.sh                                              920 2025-11-18 18:06:26.735109460 +0100
/opt/scalp/project/scripts/gest.py                                                       7321 2025-11-21 05:46:42.852733797 +0100
/opt/scalp/project/scripts/health_dash.py                                                1328 2025-11-08 16:38:22.191168545 +0100
/opt/scalp/project/scripts/opener.py                                                     2130 2025-11-20 16:53:59.773495303 +0100
/opt/scalp/project/scripts/recorder.py                                                   3344 2025-11-20 18:13:13.230872664 +0100
/opt/scalp/project/scripts/signals.py                                                    3706 2025-11-19 18:21:32.519196017 +0100
/opt/scalp/project/scripts/ticks.py                                                      3263 2025-10-29 08:32:53.050121154 +0100
/opt/scalp/project/scripts/triggers.py                                                   7592 2025-11-19 14:11:12.886416560 +0100
/opt/scalp/project/scripts/ws_test_candle15m.py                                          1525 2025-11-19 09:16:33.327262864 +0100
/opt/scalp/project/sql/fix_pipeline_schema.sql                                           2373 2025-11-18 17:15:13.279277557 +0100
/opt/scalp/project/sql/gest_schema.sql                                                    518 2025-11-20 14:08:10.646268917 +0100
/opt/scalp/project/sql/gest_update_view_opener.sql                                        299 2025-11-19 19:22:55.806212131 +0100
/opt/scalp/project/sql/gest_view_opener_for_gest.sql                                      299 2025-11-19 19:43:03.723396760 +0100
/opt/scalp/project/sql/init_signals.sql                                                   832 2025-11-19 15:13:43.377740316 +0100
/opt/scalp/project/sql/migration_gest_trades_all.sql                                      740 2025-11-18 16:09:19.444502837 +0100
/opt/scalp/project/sql/migration_signals_full_reset.sql                                  1290 2025-11-18 16:07:46.916434452 +0100
/opt/scalp/project/sql/opener_schema.sql                                                  228 2025-11-20 14:08:22.594278457 +0100
/opt/scalp/project/sql/reset_gest.sql                                                     637 2025-11-18 16:26:02.305384529 +0100
/opt/scalp/project/sql/reset_signals.sql                                                 1029 2025-11-18 16:25:51.597375814 +0100
/opt/scalp/project/sql/reset_triggers.sql                                                 716 2025-11-18 17:07:57.634981380 +0100
/opt/scalp/project/sql/signals_schema.sql                                                 653 2025-11-19 17:11:05.383563995 +0100
/opt/scalp/project/sql/view_closer_for_gest.sql                                           157 2025-11-20 14:09:27.318330679 +0100
/opt/scalp/project/sql/view_follower_for_gest.sql                                         175 2025-11-20 14:09:17.114322387 +0100
/opt/scalp/project/sql/view_gest_for_opener.sql                                           182 2025-11-20 14:09:00.062308579 +0100
/opt/scalp/project/sql/view_opener_for_gest.sql                                           212 2025-11-20 14:09:09.150315932 +0100
/opt/scalp/project/sql/view_recorder_for_gest.sql                                         139 2025-11-20 14:09:35.058336983 +0100
/opt/scalp/project/sql/view_signals_for_gest.sql                                          356 2025-11-20 16:09:47.722699716 +0100
/opt/scalp/project/sql/ws_schema.sql                                                     1045 2025-11-19 09:31:18.789065152 +0100

========== FILE CONTENT ==========

----- FILE: /opt/scalp/dp.sh -----
#!/usr/bin/env bash
set -euo pipefail

# ---------- CONFIG ----------
ROOT="/opt/scalp"
DUMP_DIR="${ROOT}/dump"
mkdir -p "$DUMP_DIR"
TS="$(date +'%Y%m%d_%H%M%S')"
OUT="${DUMP_DIR}/scalp_full_${TS}.txt"
MAX_FILE_SIZE_KB="${MAX_FILE_SIZE_KB:-512}"

# ---------- GIT ENV ----------
[ -f /etc/scalp.env ] && . /etc/scalp.env
: "${GIT_USERNAME:?}"
: "${GIT_TOKEN:?}"
GIT_BRANCH="${GIT_BRANCH:-main}"
GIT_EMAIL_USE="${GIT_EMAIL:-${GIT_USERNAME}@users.noreply.github.com}"
REMOTE_URL="https://${GIT_HOST:-github.com}/${GIT_OWNER:-$GIT_USERNAME}/${GIT_REPO:-Scalp}.git"

# ---------- HEADER ----------
{
  echo "# ===== SCALP PROJECT DUMP ====="
  echo "# $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
  echo "# Root: $ROOT"
  echo
  echo "========== TREE =========="
} > "$OUT"

find "$ROOT" -type f \
  -not -path '*/.git/*' -not -path '*/venv/*' \
  -not -path '*/dump/*' -not -path '*/logs/*' \
  -size -"${MAX_FILE_SIZE_KB}"k \
  \( -name '*.py' -o -name '*.sh' -o -name '*.conf' -o -name '*.sql' -o -name '*.txt' \) |
  sort | while IFS= read -r f; do
    sz=$(stat -c%s "$f" 2>/dev/null || echo 0)
    mt=$(stat -c%y "$f" 2>/dev/null || echo unknown)
    printf "%-80s %12s %s\n" "$f" "$sz" "$mt" >> "$OUT"
done

{
  echo
  echo "========== FILE CONTENT =========="
} >> "$OUT"

find "$ROOT" -type f \
  -not -path '*/.git/*' -not -path '*/venv/*' \
  -not -path '*/dump/*' -not -path '*/logs/*' \
  -size -"${MAX_FILE_SIZE_KB}"k \
  \( -name '*.py' -o -name '*.sh' -o -name '*.conf' -o -name '*.sql' -o -name '*.txt' \) |
  sort | while IFS= read -r f; do
    echo -e "\n----- FILE: $f -----" >> "$OUT"
    cat "$f" 2>/dev/null >> "$OUT" || true
done

# ---------- DATABASE STRUCTURE (finale complète) ----------
{
  echo
  echo "========== DATABASE STRUCTURE =========="
} >> "$OUT"

for DB_PATH in /opt/scalp/project/data/*.db; do
  [ -s "$DB_PATH" ] || continue
  echo -e "\n----- DATABASE: $DB_PATH -----" >> "$OUT"

  {
    echo "-- TABLES --"
    sqlite3 -readonly "$DB_PATH" ".tables" 2>/dev/null || echo "(locked or unreadable)"
    echo

    for T in $(sqlite3 -readonly "$DB_PATH" ".tables" 2>/dev/null); do
      echo "[TABLE: $T]"
      sqlite3 -readonly "$DB_PATH" "PRAGMA table_info($T);" 2>/dev/null || true
      echo
    done

    echo "-- VIEWS --"
    sqlite3 -readonly "$DB_PATH" "SELECT name, sql FROM sqlite_master WHERE type='view' ORDER BY name;" 2>/dev/null || echo "(no views)"
    echo

    echo "-- INDEXES --"
    sqlite3 -readonly "$DB_PATH" "SELECT name, sql FROM sqlite_master WHERE type='index' ORDER BY name;" 2>/dev/null || echo "(no indexes)"
    echo

    echo "-- TRIGGERS --"
    sqlite3 -readonly "$DB_PATH" "SELECT name, sql FROM sqlite_master WHERE type='trigger' ORDER BY name;" 2>/dev/null || echo "(no triggers)"
    echo
  } >> "$OUT"
done

# ---------- GIT PUSH ----------
cd "$ROOT"
git rev-parse --is-inside-work-tree >/dev/null 2>&1 || git init -q .
git config user.name "$GIT_USERNAME"
git config user.email "$GIT_EMAIL_USE"
git remote get-url origin >/dev/null 2>&1 || git remote add origin "$REMOTE_URL"

git add -f "$OUT"
git commit -m "dump ${TS}" || true
AUTH_URL="https://${GIT_USERNAME}:${GIT_TOKEN}@${REMOTE_URL#https://}"
git push -f "$AUTH_URL" HEAD:"$GIT_BRANCH"

----- FILE: /opt/scalp/project/bin/OB_collect.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB_OB="/opt/scalp/project/data/ob.db"
DB_U="/opt/scalp/project/data/u.db"
LOG="/opt/scalp/project/logs/ob_collect.log"

echo "[$(date)] OB_COLLECT START" >> "$LOG"

# load universe
mapfile -t UNIVERSE < <(sqlite3 -readonly "$DB_U" "SELECT instId FROM universe;")

# transform AAVE/USDT → AAVEUSDT_UMCBL
convert_inst() {
    echo "${1//\//}"_UMCBL
}

while true; do
    for inst in "${UNIVERSE[@]}"; do
        sym=$(convert_inst "$inst")

        URL="https://api.bitget.com/api/mix/v1/market/candles?symbol=${sym}&granularity=60"
        JSON=$(curl -s "$URL")

        # Check valid response (must be array)
        if echo "$JSON" | jq -e '.[0]' >/dev/null 2>&1; then
            ts=$(echo "$JSON" | jq -r '.[0][0]')
            o=$(echo "$JSON"  | jq -r '.[0][1]')
            h=$(echo "$JSON"  | jq -r '.[0][2]')
            l=$(echo "$JSON"  | jq -r '.[0][3]')
            c=$(echo "$JSON"  | jq -r '.[0][4]')
            v=$(echo "$JSON"  | jq -r '.[0][5]')

            sqlite3 "$DB_OB" <<SQL
INSERT INTO ohlcv_1m(instId, ts, o,h,l,c,v)
VALUES("$sym", $ts, $o, $h, $l, $c, $v)
ON CONFLICT(instId, ts) DO UPDATE SET
    o=excluded.o,
    h=excluded.h,
    l=excluded.l,
    c=excluded.c,
    v=excluded.v;
SQL

        else
            echo "[$(date)] ERR $sym Invalid JSON: $JSON" >> "$LOG"
        fi
    done

    sleep 1
done

----- FILE: /opt/scalp/project/bin/a_analyse.sh -----
#!/usr/bin/env bash
set -euo pipefail
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/A_ctx.py

----- FILE: /opt/scalp/project/bin/agg_3m_from_1m.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/ob.db"

# Sécurité: vérifier que la base n'est pas verrouillée
if lsof "$DB" | grep -q ob.db; then
  echo "❌ ob.db en cours d'utilisation. Ferme les processus avant d'agréger."
  lsof "$DB"
  exit 1
fi

sqlite3 "$DB" <<'SQL'
PRAGMA busy_timeout=5000;

-- (Optionnel mais recommandé si non créés) Index pour accélérer l'agrégation
-- CREATE INDEX IF NOT EXISTS idx_ohlcv_1m_inst_ts ON ohlcv_1m(instId, ts);

-- Nettoyage 3m
DELETE FROM ohlcv_3m;

-- Agrégation 1m -> 3m sans fonctions analytiques (compat SQLite 3.37)
WITH g AS (
  SELECT
    instId,
    (ts/180000)*180000 AS ts3,
    MIN(ts) AS ts_min,
    MAX(ts) AS ts_max,
    MAX(h) AS h,
    MIN(l) AS l,
    SUM(v) AS v
  FROM ohlcv_1m
  GROUP BY instId, ts3
)
INSERT INTO ohlcv_3m(instId, ts, o, h, l, c, v)
SELECT
  g.instId,
  g.ts3 AS ts,
  (SELECT o FROM ohlcv_1m WHERE instId=g.instId AND ts=g.ts_min) AS o,
  g.h,
  g.l,
  (SELECT c FROM ohlcv_1m WHERE instId=g.instId AND ts=g.ts_max) AS c,
  g.v
FROM g;

-- Vérification
SELECT '3m' AS tf, COUNT(*) AS rows, datetime(MAX(ts)/1000,'unixepoch','localtime') AS last_ts
FROM ohlcv_3m;
SQL

----- FILE: /opt/scalp/project/bin/b_maint.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/b.db"
LOG="/opt/scalp/project/logs/b_maint.log"

echo "[$(date '+%F %T')] START b.db maintenance" >>"$LOG"

sqlite3 "$DB" <<'SQL'
PRAGMA journal_mode=WAL;
PRAGMA busy_timeout=5000;

-- Garder uniquement les 150 dernières lignes par timeframe
DELETE FROM feat_1m WHERE ts NOT IN (
    SELECT ts FROM feat_1m ORDER BY ts DESC LIMIT 150
);
DELETE FROM feat_3m WHERE ts NOT IN (
    SELECT ts FROM feat_3m ORDER BY ts DESC LIMIT 150
);
DELETE FROM feat_5m WHERE ts NOT IN (
    SELECT ts FROM feat_5m ORDER BY ts DESC LIMIT 150
);

VACUUM;
SQL

echo "[$(date '+%F %T')] CLEAN OK" >>"$LOG"

----- FILE: /opt/scalp/project/bin/b_maint_safe.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/b.db"
LOG="/opt/scalp/project/logs/b_maint_safe.log"

echo "===== B.DB SAFE MAINT START $(date '+%F %T') =====" | tee -a "$LOG"

###########################################################
# 1. STOP SERVICES
###########################################################
echo "[1] Stop B feat services" | tee -a "$LOG"
systemctl stop scalp-b-feat-builder.service 2>/dev/null || true
systemctl stop scalp-b-feat-builder.timer 2>/dev/null || true

echo "[2] Kill leftover python processes" | tee -a "$LOG"
pkill -f B_feat_builder_incremental.py 2>/dev/null || true
pkill -f B_feat_builder_full.py        2>/dev/null || true
pkill -f run_b_feat.sh                 2>/dev/null || true
sleep 1

###########################################################
# 2. PURGE B.DB (SAFE)
###########################################################

echo "[3] Purge b.db (keep last 150 per TF)" | tee -a "$LOG"

sqlite3 "$DB" <<'SQL'
PRAGMA journal_mode=WAL;
PRAGMA busy_timeout=5000;

DELETE FROM feat_1m WHERE ts NOT IN (
    SELECT ts FROM feat_1m ORDER BY ts DESC LIMIT 150
);
DELETE FROM feat_3m WHERE ts NOT IN (
    SELECT ts FROM feat_3m ORDER BY ts DESC LIMIT 150
);
DELETE FROM feat_5m WHERE ts NOT IN (
    SELECT ts FROM feat_5m ORDER BY ts DESC LIMIT 150
);

VACUUM;
SQL

echo "[4] Purge completed" | tee -a "$LOG"

###########################################################
# 3. RESTART SERVICES
###########################################################
echo "[5] Restart B feat builder" | tee -a "$LOG"
systemctl start scalp-b-feat-builder.timer
sleep 1

echo "===== B.DB SAFE MAINT DONE $(date '+%F %T') =====" | tee -a "$LOG"

----- FILE: /opt/scalp/project/bin/b_purge_except_feat.sh -----
#!/bin/bash
set -e

DB="/opt/scalp/project/data/b.db"

echo "=== CLEAN b.db → KEEP ONLY feat_* AND v_feat_* ==="

sqlite3 "$DB" <<'SQL'
-- Whitelist des objets à conserver
WITH keep AS (
    SELECT 'feat_1m'  AS name UNION ALL
    SELECT 'feat_3m'  UNION ALL
    SELECT 'feat_5m'  UNION ALL
    SELECT 'v_feat_1m' UNION ALL
    SELECT 'v_feat_3m' UNION ALL
    SELECT 'v_feat_5m'
),

-- Récupérer toutes les tables et vues existantes
objs AS (
    SELECT name, type FROM sqlite_master
    WHERE name NOT LIKE 'sqlite_%'
)

-- Générer les DROP
SELECT 'DROP ' ||
       CASE type WHEN 'table' THEN 'TABLE' ELSE 'VIEW' END ||
       ' IF EXISTS ' || name || ';'
FROM objs
WHERE name NOT IN (SELECT name FROM keep)
AND type IN ('table','view');
SQL

echo "=== DONE ==="

----- FILE: /opt/scalp/project/bin/cleanup_b_db.sh -----
#!/usr/bin/env bash
set -e

DB="/opt/scalp/project/data/b.db"

echo "=== CLEAN CLEAN B.DB ==="

# 1. Checkpoint WAL
sqlite3 "$DB" "PRAGMA wal_checkpoint(FULL);"

echo "=== DROP TABLES ==="

# Drop exact tables
sqlite3 "$DB" "
DROP TABLE IF EXISTS bus_out;
DROP TABLE IF EXISTS bus_out_backup;
DROP TABLE IF EXISTS bus_out_new;
DROP TABLE IF EXISTS ctx_cache;
DROP TABLE IF EXISTS signals_B;
DROP TABLE IF EXISTS sqlite_sequence;
DROP TABLE IF EXISTS triggers;
"

echo "=== DROP VIEWS ==="

sqlite3 "$DB" "
DROP VIEW IF EXISTS v_for_gest;
DROP VIEW IF EXISTS v_opener_for_gest;
DROP VIEW IF EXISTS v_signals_for_open;
DROP VIEW IF EXISTS v_signals_overview;
"

echo "=== VACUUM ==="
sqlite3 "$DB" "VACUUM;"

echo "=== CLEANUP OK ==="

----- FILE: /opt/scalp/project/bin/cleanup_scalp_services.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "===== CLEANUP SCALP SERVICES ====="

SYSTEMD_DIR="/etc/systemd/system"

# Services SCALP valides qui NE DOIVENT PAS ÊTRE TOUCHÉS
KEEP=(
  scalp-ticks.service
  scalp-ob.service
  scalp-signal.service
  scalp-b-signals.service
  scalp-b-triggers.service
  scalp-opener.service
  scalp-follower.service
  scalp-closer.service
  scalp-recorder.service
  scalp-universe.service
  scalp-gest.service
  scalp-budget.service
)

KEEP_TIMERS=(
  scalp-ticks.timer
  scalp-ob.timer
  scalp-signal.timer
  scalp-b-feat.timer
  scalp-universe.timer
  scalp-maint-ohlcv.timer
  scalp-clean-signals.timer
  scalp-clean-tdb.timer
)

is_kept() {
  local item="$1"
  for k in "${KEEP[@]}" "${KEEP_TIMERS[@]}"; do
    if [[ "$k" == "$item" ]]; then
      return 0
    fi
  done
  return 1
}

# Trouver tous les services scalp*
echo
echo "--- SCALP SERVICES FOUND ---"
ALL=$(systemctl list-unit-files | grep -i scalp | awk '{print $1}')
echo "$ALL"

echo
echo "--- CLEANING UNUSED / FAILED ---"

for svc in $ALL; do
  if is_kept "$svc"; then
    echo "KEEP   $svc"
    continue
  fi

  # Vérifier s’il existe réellement sur disque
  if [[ ! -f "$SYSTEMD_DIR/$svc" ]]; then
    echo "REMOVE (phantom) $svc"
    systemctl disable "$svc" >/dev/null 2>&1 || true
    continue
  fi

  echo "DISABLE + DELETE $svc"
  systemctl stop "$svc" 2>/dev/null || true
  systemctl disable "$svc" 2>/dev/null || true
  rm -f "$SYSTEMD_DIR/$svc"
done

echo
echo "--- RELOADING SYSTEMD ---"
systemctl daemon-reload

echo "===== CLEANUP COMPLETE ====="

----- FILE: /opt/scalp/project/bin/closer_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/closer.db"
sqlite3 "$DB" "DELETE FROM trades_close WHERE ts_close < (strftime('%s','now')-7200)*1000;"
sqlite3 "$DB" "VACUUM;"
echo "[CLEANER] closer.db compacted"

----- FILE: /opt/scalp/project/bin/dash.sh -----
#!/usr/bin/env bash
set -euo pipefail

DATA="/opt/scalp/project/data"
LOG="/opt/scalp/project/logs/dash.log"
now_s=$(date +%s)
ts_now="$(date '+%Y-%m-%d %H:%M:%S')"
printf "\n===== SCALP PIPE HEALTH %s =====\n" "$(date '+%H:%M:%S')" | tee -a "$LOG"

check_db() {
    local name="$1" file="$2" table="$3" ts_col="$4"
    if [[ ! -f "$file" ]]; then
        printf "%-10s ❌ no DB found\n" "$name" | tee -a "$LOG"
        return
    fi
    local count ts_last age_s h m s age_hms status
    count=$(sqlite3 "$file" "SELECT COUNT(*) FROM $table;")
    ts_last=$(sqlite3 "$file" "SELECT MAX($ts_col)/1000 FROM $table;")
    [[ -z "$ts_last" || "$ts_last" == "0" ]] && ts_last=$now_s
    age_s=$(( now_s - ${ts_last%.*:-0} ))
    h=$((age_s/3600)); m=$(( (age_s%3600)/60 )); s=$((age_s%60))
    age_hms=$(printf "%02dh%02dm%02ds" "$h" "$m" "$s")

    if (( age_s < 10 )); then status="✅"
    elif (( age_s < 60 )); then status="⚠️"
    else status="❌"
    fi

    printf "%-10s %s  %4s recs | last=%s | age=%s\n" \
        "$name" "$status" "$count" "$(date '+%H:%M:%S' -d @$ts_last 2>/dev/null || date -r "$ts_last" '+%H:%M:%S')" "$age_hms" \
        | tee -a "$LOG"
}

check_db "opener"   "$DATA/opener.db"   "trades_open_init" "ts_create"
check_db "follower" "$DATA/follower.db" "trades_follow"    "ts_update"
check_db "closer"   "$DATA/closer.db"   "trades_close"     "ts_close"
check_db "recorder" "$DATA/recorder.db" "trades_record"    "ts_record"

printf "==============================================\n" | tee -a "$LOG"
echo "" >> "$LOG"

----- FILE: /opt/scalp/project/bin/dash_b_feat.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB_OB="/opt/scalp/project/data/ob.db"
DB_B="/opt/scalp/project/data/b.db"
NOW_MS=$(($(date +%s) * 1000))

hr() { printf "%s\n" "-------------------------------------------"; }

# ------------------------------
# 1. OB stats
# ------------------------------
ob_rows_1m=$(sqlite3 $DB_OB "SELECT COUNT(*) FROM ohlcv_1m;")
ob_rows_3m=$(sqlite3 $DB_OB "SELECT COUNT(*) FROM ohlcv_3m;")
ob_rows_5m=$(sqlite3 $DB_OB "SELECT COUNT(*) FROM ohlcv_5m;")

ob_last_1m=$(sqlite3 $DB_OB "SELECT MAX(ts) FROM ohlcv_1m;")
ob_last_3m=$(sqlite3 $DB_OB "SELECT MAX(ts) FROM ohlcv_3m;")
ob_last_5m=$(sqlite3 $DB_OB "SELECT MAX(ts) FROM ohlcv_5m;")

age() {
    local last=$1
    echo $(( (NOW_MS - last) / 1000 ))
}

# ------------------------------
# 2. FEAT stats
# ------------------------------
feat_rows_1m=$(sqlite3 $DB_B "SELECT COUNT(*) FROM feat_1m;")
feat_rows_3m=$(sqlite3 $DB_B "SELECT COUNT(*) FROM feat_3m;")
feat_rows_5m=$(sqlite3 $DB_B "SELECT COUNT(*) FROM feat_5m;")

feat_last_1m=$(sqlite3 $DB_B "SELECT MAX(ts) FROM feat_1m;")
feat_last_3m=$(sqlite3 $DB_B "SELECT MAX(ts) FROM feat_3m;")
feat_last_5m=$(sqlite3 $DB_B "SELECT MAX(ts) FROM feat_5m;")

# ------------------------------
# PRINT
# ------------------------------
echo "==========================================="
echo "      MINI DASH — B FEAT (1m/3m/5m)"
echo "==========================================="

echo
echo "---- [OB OHLCV] ----"
printf "1m | rows=%-6s age=%4ss\n" "$ob_rows_1m" "$(age $ob_last_1m)"
printf "3m | rows=%-6s age=%4ss\n" "$ob_rows_3m" "$(age $ob_last_3m)"
printf "5m | rows=%-6s age=%4ss\n" "$ob_rows_5m" "$(age $ob_last_5m)"

echo
echo "---- [FEAT] ----"
printf "1m | rows=%-6s age=%4ss\n" "$feat_rows_1m" "$(age $feat_last_1m)"
printf "3m | rows=%-6s age=%4ss\n" "$feat_rows_3m" "$(age $feat_last_3m)"
printf "5m | rows=%-6s age=%4ss\n" "$feat_rows_5m" "$(age $feat_last_5m)"

echo
echo "---- [STATUS] ----"
a1=$(age $feat_last_1m)
a3=$(age $feat_last_3m)
a5=$(age $feat_last_5m)

status="OK"
[ "$a1" -gt 300 ] && status="⚠️ 1m lag"
[ "$a3" -gt 900 ] && status="⚠️ 3m lag"
[ "$a5" -gt 900 ] && status="⚠️ 5m lag"

echo "$status"

echo
echo "==========================================="
echo "              FIN MINI DASH"
echo "==========================================="

----- FILE: /opt/scalp/project/bin/dash_oa_age.sh -----
#!/bin/bash
DB=/opt/scalp/project/data/oa.db

echo "===== OA AGE ====="
for tf in 5 15 30; do
  table="ohlcv_${tf}m"
  row=$(sqlite3 $DB "SELECT MAX(ts) FROM $table")
  if [ -z "$row" ]; then
    echo "${tf}m : no data"
    continue
  fi
  age=$(( $(date +%s%3N) - row ))
  echo "${tf}m : last_ts=$row age_ms=$age"
done

----- FILE: /opt/scalp/project/bin/dash_ob_feat.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB_OB="/opt/scalp/project/data/ob.db"
DB_B="/opt/scalp/project/data/b.db"

age() {
  ts_ms="$1"
  now_ms=$(($(date +%s) * 1000))
  echo $(( (now_ms - ts_ms) / 1000 ))
}

echo "==========================================="
echo "      MINI DASH — OB + FEAT (1m/3m/5m)"
echo "==========================================="

### OB OHLCV ###
echo "---- [OB OHLCV] ----"

for tf in 1m 3m 5m; do
    table="ohlcv_${tf}"
    rows=$(sqlite3 "$DB_OB" "SELECT COUNT(*) FROM $table;")
    last_ts=$(sqlite3 "$DB_OB" "SELECT MAX(ts) FROM $table;")
    if [[ -z "$last_ts" || "$last_ts" == "0" ]]; then age_s="n/a"
    else age_s=$(age "$last_ts")
    fi
    printf "%-3s | rows=%-6s age=%4ss\n" "$tf" "$rows" "$age_s"
done

### FEAT ###
echo "---- [FEAT] ----"

for tf in 1m 3m 5m; do
    table="feat_${tf}"
    rows=$(sqlite3 "$DB_B" "SELECT COUNT(*) FROM $table;")
    last_ts=$(sqlite3 "$DB_B" "SELECT MAX(ts) FROM $table;")
    if [[ -z "$last_ts" || "$last_ts" == "0" ]]; then age_s="n/a"
    else age_s=$(age "$last_ts")
    fi
    printf "%-3s | rows=%-6s age=%4ss\n" "$tf" "$rows" "$age_s"
done


### STATUS ###
echo "---- [STATUS] ----"

ok=true
msg=""

# seuils
S1=120   # 1m max age
S3=180   # 3m max age
S5=300   # 5m max age  (corrigé)

check_tf() {
  tf="$1"; ob="$2"; ft="$3"; max="$4"
  if (( ob > max )); then ok=false; msg="$msg ⚠️ OB ${tf} lag (${ob} s)\n"; fi
  if (( ft > max )); then ok=false; msg="$msg ⚠️ FEAT ${tf} lag (${ft} s)\n"; fi
}

# ages
ob1=$(age "$(sqlite3 "$DB_OB" "SELECT IFNULL(MAX(ts),0) FROM ohlcv_1m;")")
ft1=$(age "$(sqlite3 "$DB_B" "SELECT IFNULL(MAX(ts),0) FROM feat_1m;")")

ob3=$(age "$(sqlite3 "$DB_OB" "SELECT IFNULL(MAX(ts),0) FROM ohlcv_3m;")")
ft3=$(age "$(sqlite3 "$DB_B" "SELECT IFNULL(MAX(ts),0) FROM feat_3m;")")

ob5=$(age "$(sqlite3 "$DB_OB" "SELECT IFNULL(MAX(ts),0) FROM ohlcv_5m;")")
ft5=$(age "$(sqlite3 "$DB_B" "SELECT IFNULL(MAX(ts),0) FROM feat_5m;")")

check_tf 1m "$ob1" "$ft1" "$S1"
check_tf 3m "$ob3" "$ft3" "$S3"
check_tf 5m "$ob5" "$ft5" "$S5"

if $ok; then
    echo "🟢 OK"
else
    echo -e "$msg"
fi


----- FILE: /opt/scalp/project/bin/dash_ob_feat_coin.sh -----
#!/usr/bin/env bash
set -euo pipefail

COIN="${1:-BTC/USDT}"

DB_OB="/opt/scalp/project/data/ob.db"
DB_B="/opt/scalp/project/data/b.db"

echo "====================================================="
echo "        DASH OB + FEAT — $COIN (1m / 3m / 5m)"
echo "====================================================="

for TF in 1m 3m 5m; do
    echo
    echo "-------------------- OHLCV $TF ----------------------"
    sqlite3 -readonly "$DB_OB" <<SQL
.headers on
.mode column
SELECT ts,
       datetime(ts/1000,'unixepoch','localtime') AS local,
       o,h,l,c,v
FROM ohlcv_${TF}
WHERE instId='${COIN}'
ORDER BY ts DESC
LIMIT 10;
SQL

    echo
    echo "-------------------- FEAT $TF ------------------------"
    sqlite3 -readonly "$DB_B" <<SQL
.headers on
.mode column
SELECT ts,
       datetime(ts/1000,'unixepoch','localtime') AS local,
       c,
       ema12, ema26,
       macd, macdsignal, macdhist,
       rsi14_1m AS rsi,
       atr14  AS atr,
       bb_mid, bb_up, bb_low, bb_width,
       mom, roc, slope_ema12_1m AS slope,
       ctx
FROM feat_${TF}
WHERE instId='${COIN}'
ORDER BY ts DESC
LIMIT 10;
SQL

done

echo "====================================================="
echo "                       END"
echo "====================================================="

----- FILE: /opt/scalp/project/bin/dash_ofcr.sh -----
#!/usr/bin/env bash

echo "===== OFCR DASH ====="
echo
echo "--- STATES ---"
sqlite3 /opt/scalp/project/data/gest.db "
SELECT state, COUNT(*) FROM trades_all GROUP BY state;
"

echo
echo "--- LAST 10 ---"
sqlite3 /opt/scalp/project/data/gest.db "
SELECT trade_uid, instId, side, state, datetime(ts_update/1000,'unixepoch','localtime')
FROM trades_all
ORDER BY ts_update DESC LIMIT 10;
"

----- FILE: /opt/scalp/project/bin/dash_pipeline_health.sh -----
#!/bin/bash

echo "===== PIPELINE HEALTH (WS → OA → FEAT → CTX) ====="

echo
/opt/scalp/project/bin/dash_ws_age.sh

echo
/opt/scalp/project/bin/dash_oa_age.sh

echo
echo "FEAT latest:"
sqlite3 /opt/scalp/project/data/a.db \
  "SELECT tf, datetime(MAX(ts)/1000,'unixepoch','localtime') FROM feat_5m UNION ALL
   SELECT tf, datetime(MAX(ts)/1000,'unixepoch','localtime') FROM feat_15m UNION ALL
   SELECT tf, datetime(MAX(ts)/1000,'unixepoch','localtime') FROM feat_30m;"

echo
echo "CTX latest:"
sqlite3 /opt/scalp/project/data/a.db \
  "SELECT datetime(MAX(ts_update)/1000,'unixepoch','localtime') FROM ctx_cache;"

----- FILE: /opt/scalp/project/bin/dash_ws_age.sh -----
#!/bin/bash
DB=/opt/scalp/project/data/ws.db

echo "===== WS CANDLES AGE ====="
for tf in 5 15 30; do
  table="ws_ohlcv_${tf}m"
  row=$(sqlite3 $DB "SELECT MAX(ts) FROM $table")
  if [ -z "$row" ]; then
    echo "${tf}m : no data"
    continue
  fi
  age=$(( $(date +%s%3N) - row ))
  echo "${tf}m : last_ts=$row age_ms=$age"
done

----- FILE: /opt/scalp/project/bin/db_purge_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "===== SCALP DB PURGE CLEAN ($(date '+%F %T')) ====="

DB_SIG="/opt/scalp/project/data/signals.db"
DB_TRIG="/opt/scalp/project/data/triggers.db"
DB_T="/opt/scalp/project/data/t.db"

# Retentions
RET_SIGNAL_DAYS=3
RET_TRIGGERS_DAYS=3
RET_TICKS_MINUTES=10

# -----------------------------
# 1. Stop services concernés
# -----------------------------
echo "[1] Stop services"
systemctl stop scalp-b-triggers.service 2>/dev/null || true
systemctl stop scalp-signal.service 2>/dev/null || true
systemctl stop scalp-ticks.service 2>/dev/null || true
sleep 1

# -----------------------------
# 2. Taille avant purge
# -----------------------------
echo
echo "[2] Taille AVANT purge"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

# -----------------------------
# 3. Purge SIGNALS
# -----------------------------
echo
echo "[3] Purge signals.db (≥ ${RET_SIGNAL_DAYS} jours)"

sqlite3 "$DB_SIG" <<SQL
PRAGMA journal_mode=WAL;
DELETE FROM signals_B
WHERE ts_signal < (strftime('%s','now') - ${RET_SIGNAL_DAYS}*86400)*1000;

-- Retirer aussi les signaux orphelins (invalides)
DELETE FROM signals_B WHERE instId IS NULL OR side IS NULL;

VACUUM;
SQL

# -----------------------------
# 4. Purge TRIGGERS
# -----------------------------
echo
echo "[4] Purge triggers.db (≥ ${RET_TRIGGERS_DAYS} jours)"

sqlite3 "$DB_TRIG" <<SQL
PRAGMA journal_mode=WAL;
DELETE FROM triggers
WHERE ts < (strftime('%s','now') - ${RET_TRIGGERS_DAYS}*86400)*1000;

DELETE FROM triggers WHERE instId IS NULL;

VACUUM;
SQL

# -----------------------------
# 5. Purge TICKS
# -----------------------------
echo
echo "[5] Purge ticks > ${RET_TICKS_MINUTES} minutes"

sqlite3 "$DB_T" <<SQL
PRAGMA journal_mode=WAL;
DELETE FROM ticks
WHERE ts_ms < (strftime('%s','now') - ${RET_TICKS_MINUTES}*60)*1000;
VACUUM;
SQL

# -----------------------------
# 6. Taille après purge
# -----------------------------
echo
echo "[6] Taille APRÈS purge"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

# -----------------------------
# 7. Restart services
# -----------------------------
echo
echo "[7] Restart services"
systemctl start scalp-b-triggers.service 2>/dev/null || true
systemctl start scalp-signal.service 2>/dev/null || true
systemctl start scalp-ticks.service 2>/dev/null || true

echo
echo "===== PURGE COMPLETED ====="

----- FILE: /opt/scalp/project/bin/db_purge_scalp.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "===== SCALP DB PURGE (DOC 188) $(date '+%F %T') ====="

DB_SIG="/opt/scalp/project/data/signals.db"
DB_TRIG="/opt/scalp/project/data/triggers.db"
DB_T="/opt/scalp/project/data/t.db"

# Retention
RET_SIG_DAYS=3
RET_TRIG_DAYS=3
RET_TICKS_MIN=10

#########################################
# 1. STOP SERVICES
#########################################
echo "[1] Stopping services"
systemctl stop scalp-signals.service 2>/dev/null || true
systemctl stop scalp-triggers.service 2>/dev/null || true
systemctl stop scalp-ticks.service 2>/dev/null || true
sleep 1

#########################################
# 2. DB size BEFORE purge
#########################################
echo
echo "[2] Size BEFORE purge"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

#########################################
# 3. PURGE signals.db (TABLE: signals)
#########################################
echo
echo "[3] Purging signals.db (table: signals)"

sqlite3 "$DB_SIG" <<SQL
PRAGMA journal_mode=WAL;

DELETE FROM signals
WHERE ts_signal < (strftime('%s','now') - ${RET_SIG_DAYS}*86400) * 1000;

DELETE FROM signals
WHERE instId IS NULL OR trade_uid IS NULL;

VACUUM;
SQL

#########################################
# 4. PURGE triggers.db (TABLE: triggers)
#########################################
echo
echo "[4] Purging triggers.db (table: triggers)"

sqlite3 "$DB_TRIG" <<SQL
PRAGMA journal_mode=WAL;

DELETE FROM triggers
WHERE ts < (strftime('%s','now') - ${RET_TRIG_DAYS}*86400) * 1000;

DELETE FROM triggers
WHERE instId IS NULL;

VACUUM;
SQL

#########################################
# 5. PURGE ticks (TABLE: ticks)
#########################################
echo
echo "[5] Purging t.db (table: ticks)"

sqlite3 "$DB_T" <<SQL
PRAGMA journal_mode=WAL;

DELETE FROM ticks
WHERE ts_ms < (strftime('%s','now') - ${RET_TICKS_MIN}*60) * 1000;

VACUUM;
SQL

#########################################
# 6. Size AFTER purge
#########################################
echo
echo "[6] Size AFTER purge"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

#########################################
# 7. Restart services
#########################################
echo
echo "[7] Restarting services"
systemctl start scalp-signals.service 2>/dev/null || true
systemctl start scalp-triggers.service 2>/dev/null || true
systemctl start scalp-ticks.service 2>/dev/null || true

echo
echo "===== PURGE COMPLETED SUCCESSFULLY ====="

----- FILE: /opt/scalp/project/bin/db_reset_scalp.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "===== SCALP DB RESET (DOC 188) $(date '+%F %T') ====="

DB_SIG="/opt/scalp/project/data/signals.db"
DB_TRIG="/opt/scalp/project/data/triggers.db"
DB_T="/opt/scalp/project/data/t.db"

#########################################
# 1. STOP SERVICES
#########################################
echo "[1] Stopping services"
systemctl stop scalp-signals.service 2>/dev/null || true
systemctl stop scalp-triggers.service 2>/dev/null || true
systemctl stop scalp-ticks.service 2>/dev/null || true
sleep 1

#########################################
# 2. SIZE BEFORE
#########################################
echo
echo "[2] Size BEFORE reset"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

#########################################
# 3. RESET signals.db
#########################################
echo
echo "[3] RESET signals.db"
sqlite3 "$DB_SIG" <<SQL
DELETE FROM signals;
VACUUM;
SQL

#########################################
# 4. RESET triggers.db
#########################################
echo
echo "[4] RESET triggers.db"
sqlite3 "$DB_TRIG" <<SQL
DELETE FROM triggers;
VACUUM;
SQL

#########################################
# 5. RESET t.db
#########################################
echo
echo "[5] RESET t.db"
sqlite3 "$DB_T" <<SQL
DELETE FROM ticks;
VACUUM;
SQL

#########################################
# 6. SIZE AFTER
#########################################
echo
echo "[6] Size AFTER reset"
ls -lh "$DB_SIG" "$DB_TRIG" "$DB_T"

#########################################
# 7. Restart services
#########################################
echo
echo "[7] Restarting services"
systemctl start scalp-signals.service 2>/dev/null || true
systemctl start scalp-triggers.service 2>/dev/null || true
systemctl start scalp-ticks.service 2>/dev/null || true

echo
echo "===== RESET DONE ====="

----- FILE: /opt/scalp/project/bin/follower_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/follower.db"
sqlite3 "$DB" "DELETE FROM trades_follow WHERE ts_update < (strftime('%s','now')-7200)*1000;"
sqlite3 "$DB" "VACUUM;"
echo "[CLEANER] follower.db compacted"

----- FILE: /opt/scalp/project/bin/gest_checker.sh -----
#!/usr/bin/env bash
sqlite3 /opt/scalp/project/data/gest.db "
SELECT trade_uid,instId,side,state,datetime(ts_update/1000,'unixepoch') FROM trades_all ORDER BY ts_update DESC LIMIT 20;
"

----- FILE: /opt/scalp/project/bin/maint_ohlcv.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/ob.db"
LOG="/opt/scalp/project/logs/maint_ohlcv.log"
echo "[$(date '+%F %T')] START maintenance" >>"$LOG"

# --- Nettoyage (max rows) ---
sqlite3 "$DB" <<'SQL'
DELETE FROM ohlcv_1m WHERE ts NOT IN (
  SELECT ts FROM ohlcv_1m ORDER BY ts DESC LIMIT 450
);
DELETE FROM ohlcv_3m WHERE ts NOT IN (
  SELECT ts FROM ohlcv_3m ORDER BY ts DESC LIMIT 150
);
DELETE FROM ohlcv_5m WHERE ts NOT IN (
  SELECT ts FROM ohlcv_5m ORDER BY ts DESC LIMIT 150
);
VACUUM;
SQL

echo "[$(date '+%F %T')] CLEAN OK" >>"$LOG"

# --- Ré-agrégation 3m ---
bash /opt/scalp/project/bin/agg_3m_from_1m.sh >>"$LOG" 2>&1 || echo "agg_3m fail" >>"$LOG"

# --- Vérification fraîcheur ---
bash /opt/scalp/project/bin/check_ohlcv_freshness.sh >>"$LOG" 2>&1

echo "[$(date '+%F %T')] DONE maintenance" >>"$LOG"

----- FILE: /opt/scalp/project/bin/oa_health.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/oa.db"

age_s=$(sqlite3 "$DB" "SELECT (strftime('%s','now') - MAX(ts)/1000) FROM ohlcv_5m;")
printf '[OA_HEALTH] last 5m age: %.1fs\n' "$age_s"

if (( $(echo "$age_s > 120" | bc -l) )); then
  echo "[OA_HEALTH] ❌ OHLCV_5m too old ($age_s s) → restarting OA"
  systemctl restart scalp-oa.service
else
  echo "[OA_HEALTH] ✅ OA fresh ($age_s s)"
fi

----- FILE: /opt/scalp/project/bin/oa_ohlcv.sh -----
#!/bin/bash
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/OA_ohlcv_A.py

----- FILE: /opt/scalp/project/bin/oa_ohlcv_A.sh -----
#!/bin/bash
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/OA_ohlcv_A.py

----- FILE: /opt/scalp/project/bin/ob_aggregate_3m.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/ob.db"
LOCK="/var/lock/ob_agg3m.lock"
LOG="/opt/scalp/project/logs/ob_agg3m.log"

exec /usr/bin/flock -n "$LOCK" bash -c "
  echo \"\$(date '+%F %T') [AGG3M] Start aggregation\" >> \$LOG
  sqlite3 \$DB '
    DELETE FROM ohlcv_3m;
    INSERT INTO ohlcv_3m (instId, ts, o, h, l, c, v)
    SELECT instId,
           (ts/180000)*180000 AS ts_group,
           first_value(o) OVER w AS o,
           MAX(h) OVER w AS h,
           MIN(l) OVER w AS l,
           last_value(c) OVER w AS c,
           SUM(v) OVER w AS v
    FROM ohlcv_1m
    WINDOW w AS (PARTITION BY instId, (ts/180000) ORDER BY ts ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)
    GROUP BY instId, ts_group;
  '
  echo \"\$(date '+%F %T') [AGG3M] Done\" >> \$LOG
"

----- FILE: /opt/scalp/project/bin/ob_check.sh -----
#!/usr/bin/env bash
sqlite3 /opt/scalp/project/data/ob.db <<'SQL'
.headers on
.mode column
SELECT instId,
       COUNT(*) AS n,
       datetime(MAX(ts)/1000,'unixepoch','localtime') AS last_ts
FROM ohlcv_1m
GROUP BY instId
ORDER BY last_ts DESC
LIMIT 10;
SQL

----- FILE: /opt/scalp/project/bin/ob_feat.sh -----
#!/bin/bash
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/OB_feat.py

----- FILE: /opt/scalp/project/bin/ob_freshness.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB_OB="/opt/scalp/project/data/ob.db"

# TF -> (table:limit_age)
declare -A TF_TABLES=(
    ["1m"]="ohlcv_1m:60"
    ["3m"]="ohlcv_3m:180"
    ["5m"]="ohlcv_5m:300"
)

now_ms=$(($(date +%s) * 1000))

echo "=== LAST OB CANDLES USED ==="
printf "%-3s %-19s %-7s %s\n" "tf" "last_ts" "age_s" "status"
echo "--  -------------------  -------  -------"

for tf in 1m 3m 5m; do
    entry="${TF_TABLES[$tf]}"
    table="${entry%%:*}"
    limit="${entry##*:}"

    last_ts=$(sqlite3 "$DB_OB" "SELECT MAX(ts) FROM $table;")

    if [[ -z "$last_ts" || "$last_ts" == "NULL" ]]; then
        printf "%-3s %-19s %-7s %s\n" "$tf" "---" "---" "❌"
        continue
    fi

    age=$(( (now_ms - last_ts) / 1000 ))
    last_human=$(date -d "@$((last_ts / 1000))" +"%Y-%m-%d %H:%M:%S")

    status="⚠️"
    if (( age < limit )); then
        status="✅"
    fi

    printf "%-3s %-19s %-7d %s\n" "$tf" "$last_human" "$age" "$status"
done


----- FILE: /opt/scalp/project/bin/ob_ohlcv_B.sh -----
#!/bin/bash
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/OB_ohlcv_B.py

----- FILE: /opt/scalp/project/bin/ob_ws_hard_stop.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "⛔ Arrêt timer + service..."
systemctl stop scalp-ob-ws.timer 2>/dev/null || true
systemctl disable scalp-ob-ws.timer 2>/dev/null || true

systemctl stop scalp-ob-ws.service 2>/dev/null || true
systemctl disable scalp-ob-ws.service 2>/dev/null || true

echo "🧨 Kill ALL collectors + ALL builders..."
pkill -9 -f OB_ws_collect_v2.py || true
pkill -9 -f OB_ws_collect.py || true
pkill -9 -f ob_ws_singleton.sh || true
pkill -9 -f B_feat_builder_all.py || true
pkill -9 -f flock || true

echo "🔒 Remove lock file..."
rm -f /var/lock/ob_ws.lock 2>/dev/null || true

echo "🔍 Check if ob.db still opened:"
lsof /opt/scalp/project/data/ob.db || echo 'OK: none'

echo "🧹 Cleanup done."

----- FILE: /opt/scalp/project/bin/ob_ws_singleton.sh -----
#!/usr/bin/env bash
set -euo pipefail

LOCKFILE="/var/lock/ob_ws.lock"

# 🚫 Tue d’abord toute activité résiduelle dès le lancement
pkill -9 -f OB_ws_collect_v2.py 2>/dev/null || true
pkill -9 -f B_feat_builder_all.py 2>/dev/null || true

exec /usr/bin/flock -n "$LOCKFILE" \
    /opt/scalp/project/venv/bin/python3 \
    /opt/scalp/project/scripts/OB_ws_collect_v2.py

----- FILE: /opt/scalp/project/bin/opener_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/opener.db"
sqlite3 "$DB" "DELETE FROM trades_open_init WHERE ts_create < (strftime('%s','now')-7200)*1000;"
sqlite3 "$DB" "VACUUM;"
echo "[CLEANER] opener.db compacted"

----- FILE: /opt/scalp/project/bin/pipeline_diagnostics.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB_O="/opt/scalp/project/data/opener.db"
DB_F="/opt/scalp/project/data/follower.db"
DB_C="/opt/scalp/project/data/closer.db"
DB_B="/opt/scalp/project/data/b.db"

sqlite3 "$DB_C" <<'SQL'
ATTACH DATABASE '"$DB_O"' AS O;
ATTACH DATABASE '"$DB_F"' AS F;
ATTACH DATABASE '"$DB_B"' AS B;
.headers on
.mode column

WITH sig AS (
  SELECT instId, MAX(ts) AS ts_signal_s
  FROM B.signals_B
  GROUP BY instId
),
open AS (
  SELECT instId, MAX(ts_open) AS ts_open
  FROM O.trades_open_init
  GROUP BY instId
),
closed AS (
  SELECT instId, side, entry, exit, pnl, reason_exit, atr, ts_open, ts_close
  FROM main.positions_closed
)
SELECT
  c.instId,
  c.side,
  c.reason_exit,
  printf('%.4f', c.pnl) AS pnl,
  (c.ts_close - c.ts_open) AS trade_ms,
  CASE WHEN s.ts_signal_s IS NULL THEN NULL
       ELSE (c.ts_open - s.ts_signal_s*1000)
  END AS open_latency_ms
FROM closed c
LEFT JOIN sig s   ON s.instId = c.instId
LEFT JOIN open o  ON o.instId = c.instId AND o.ts_open = c.ts_open
ORDER BY c.ts_close DESC
LIMIT 50;

DETACH DATABASE O;
DETACH DATABASE F;
DETACH DATABASE B;
SQL

----- FILE: /opt/scalp/project/bin/recorder_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail
DB="/opt/scalp/project/data/recorder.db"
sqlite3 "$DB" "DELETE FROM trades_record WHERE ts_record < (strftime('%s','now')-86400)*1000;"
sqlite3 "$DB" "VACUUM;"
echo "[CLEANER] recorder.db compacted"

----- FILE: /opt/scalp/project/bin/relaunch_ohlcv_feat.sh -----
#!/usr/bin/env bash
set -euo pipefail

echo "===== RELANCE OHLCV + FEAT ($(date '+%F %T')) ====="

# --- stop services ---
echo "[1] Stop services"
systemctl stop scalp-ob.service || true
systemctl stop scalp-oa.service || true
systemctl stop scalp-b-feat-builder.service || true
systemctl stop scalp-b-feat-builder.timer || true

# --- kill python leftover ---
echo "[2] Kill runaway processes"
pkill -f OB_collect.py || true
pkill -f OA_ohlcv_A.py || true
pkill -f B_feat_builder || true

# --- cleanup OHLCV if old ---
DB_OB="/opt/scalp/project/data/ob.db"
echo "[3] Check OHLCV timestamps"

AGE1=$(sqlite3 "$DB_OB" "SELECT (strftime('%s','now') - MAX(ts/1000)) FROM ohlcv_1m;" 2>/dev/null || echo 999999)
AGE5=$(sqlite3 "$DB_OB" "SELECT (strftime('%s','now') - MAX(ts/1000)) FROM ohlcv_5m;" 2>/dev/null || echo 999999)

if (( AGE1 > 300 || AGE5 > 300 )); then
    echo "⚠️ OHLCV trop vieux → reset tables"
    sqlite3 "$DB_OB" "DELETE FROM ohlcv_1m;"
    sqlite3 "$DB_OB" "DELETE FROM ohlcv_3m;"
    sqlite3 "$DB_OB" "DELETE FROM ohlcv_5m;"
else
    echo "OK OHLCV pas trop vieux"
fi

# --- cleanup feat if old ---
DB_B="/opt/scalp/project/data/b.db"
echo "[4] Check FEAT timestamps"

F1=$(sqlite3 "$DB_B" "SELECT (strftime('%s','now') - MAX(ts/1000)) FROM feat_1m;" 2>/dev/null || echo 999999)

if (( F1 > 300 )); then
    echo "⚠️ Features trop vieux → reset feat tables"
    sqlite3 "$DB_B" "DELETE FROM feat_1m;"
    sqlite3 "$DB_B" "DELETE FROM feat_3m;"
    sqlite3 "$DB_B" "DELETE FROM feat_5m;"
fi

# --- restart pipeline ---
echo "[5] Restart OA"
systemctl start scalp-oa.service

echo "[6] Restart OB"
systemctl start scalp-ob.service

echo "[7] Restart feat builder (timer)"
systemctl start scalp-b-feat-builder.timer

echo "===== RELANCE OK ====="

echo "[8] Vérification dans 10 secondes…"
sleep 10

echo "--- OHLCV FRESHNESS ---"
bash /opt/scalp/project/bin/check_ohlcv_freshness.sh | sed 's/^/    /'

echo "--- FEAT FRESHNESS ---"
bash /opt/scalp/project/bin/check_feat_freshness.sh | sed 's/^/    /'


----- FILE: /opt/scalp/project/bin/reset_b_db.sh -----
#!/bin/bash
DB="/opt/scalp/project/data/b.db"

echo "=== RESET B.DB CLEAN ==="
echo "Target: $DB"
sqlite3 "$DB" "PRAGMA journal_mode=WAL;"

# Liste des objets réels
echo "--- Current objects:"
sqlite3 "$DB" "SELECT name, type FROM sqlite_master ORDER BY name;"

# Suppression des objets NON autorisés
TO_DROP=$(
  sqlite3 "$DB" "SELECT name, type FROM sqlite_master WHERE name NOT IN (
      'feat_1m','feat_3m','feat_5m',
      'idx_feat1','idx_feat3','idx_feat5',
      'v_feat_1m','v_feat_3m','v_feat_5m',
      'sqlite_sequence'
  );"
)

echo "--- Objects to DROP:"
echo "$TO_DROP"

while read -r obj; do
    [ -z "$obj" ] && continue
    NAME=$(echo "$obj" | cut -d'|' -f1)
    TYPE=$(echo "$obj" | cut -d'|' -f2)
    echo "DROP $TYPE $NAME"
    sqlite3 "$DB" "DROP $TYPE IF EXISTS $NAME;"
done <<< "$TO_DROP"

echo "--- VACUUM..."
sqlite3 "$DB" "VACUUM;"

echo "--- FINAL CONTENT:"
sqlite3 "$DB" "SELECT name, type FROM sqlite_master ORDER BY name;"

echo "=== RESET FINISHED ==="

----- FILE: /opt/scalp/project/bin/run_a_ctx.sh -----
#!/usr/bin/env bash
set -euo pipefail

/opt/scalp/project/venv/bin/python3 /opt/scalp/project/scripts/A_ctx.py

----- FILE: /opt/scalp/project/bin/run_a_feat.sh -----
#!/usr/bin/env bash
set -euo pipefail
cd /opt/scalp/project
source venv/bin/activate
exec python3 scripts/A_feat_builder.py

----- FILE: /opt/scalp/project/bin/run_b_feat.sh -----
#!/usr/bin/env bash
set -euo pipefail

LOG="/opt/scalp/project/logs/run_b_feat.log"
echo "===== RUN B_FEAT $(date '+%Y-%m-%d %H:%M:%S') =====" | tee -a "$LOG"

cd /opt/scalp/project/scripts

echo "→ FEAT 1m incremental" | tee -a "$LOG"
python3 B_feat_builder_incremental.py >> "$LOG" 2>&1

echo "→ FEAT 3m full" | tee -a "$LOG"
python3 B_feat_builder_full.py 3m >> "$LOG" 2>&1

echo "→ FEAT 5m full" | tee -a "$LOG"
python3 B_feat_builder_full.py 5m >> "$LOG" 2>&1

echo "===== END RUN B_FEAT =====" | tee -a "$LOG"

----- FILE: /opt/scalp/project/bin/run_oa.sh -----
#!/usr/bin/env bash
set -euo pipefail

LOG="/opt/scalp/project/logs/oa.log"
mkdir -p /opt/scalp/project/logs

echo "===== OA $(date '+%Y-%m-%d %H:%M:%S') =====" | tee -a "$LOG"

cd /opt/scalp/project/scripts

python3 A_ctx_engine.py >> "$LOG" 2>&1

echo "===== END OA =====" | tee -a "$LOG"

----- FILE: /opt/scalp/project/bin/run_ofcr_test.sh -----
#!/usr/bin/env bash

ROOT="/opt/scalp/project"
LOGDIR="$ROOT/logs"

mkdir -p "$LOGDIR"

echo "=== SCALP — LANCEMENT PIPELINE OFCR (TEST) ==="

# Kill anciens processus éventuels
echo "Killing old processes..."
pkill -9 -f gest.py     2>/dev/null
pkill -9 -f opener.py   2>/dev/null
pkill -9 -f follower.py 2>/dev/null
pkill -9 -f closer.py   2>/dev/null
pkill -9 -f recorder.py 2>/dev/null
pkill -9 -f signals.py  2>/dev/null

sleep 0.3

# LANCEMENT
echo "Lancement SIGNALS..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/signals.py \
    >> $LOGDIR/signals_test.log 2>&1 &

echo "Lancement GEST..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/gest.py \
    >> $LOGDIR/gest_test.log 2>&1 &

echo "Lancement OPENER..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/opener.py \
    >> $LOGDIR/opener_test.log 2>&1 &

echo "Lancement FOLLOWER..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/follower.py \
    >> $LOGDIR/follower_test.log 2>&1 &

echo "Lancement CLOSER..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/closer.py \
    >> $LOGDIR/closer_test.log 2>&1 &

echo "Lancement RECORDER..."
nohup $ROOT/venv/bin/python3 $ROOT/scripts/recorder.py \
    >> $LOGDIR/recorder_test.log 2>&1 &

sleep 0.5

echo
echo "=== PIPELINE OFCR LANCÉ ==="
echo "Pour vérifier les logs en direct :"
echo "  tail -f $LOGDIR/gest_test.log"
echo

----- FILE: /opt/scalp/project/bin/safe_agg_3m.sh -----
#!/usr/bin/env bash
DB="/opt/scalp/project/data/ob.db"
echo "=== SAFE 3M AGG $(date '+%F %T') ==="
# Attente si DB utilisée
for i in {1..30}; do
  if lsof "$DB" | grep -q python3; then
    echo "⏳ Attente libération ob.db ($i/30)"
    sleep 2
  else
    bash /opt/scalp/project/bin/agg_3m_from_1m.sh
    exit 0
  fi
done
echo "❌ Timeout : ob.db toujours utilisée."
exit 1

----- FILE: /opt/scalp/project/bin/setup_ofcr.sh -----
#!/usr/bin/env bash
set -euo pipefail

cd /opt/scalp/project

# 1. stop OFCR services cleanly
systemctl stop scalp-gest.service || true
systemctl stop scalp-opener.service || true
systemctl stop scalp-follower.service || true
systemctl stop scalp-closer.service || true
systemctl stop scalp-recorder.service || true

sleep 1

# 2. apply schemas
sqlite3 data/gest.db < sql/schema_gest.sql
sqlite3 data/opener.db < sql/schema_opener.sql
sqlite3 data/follower.db < sql/schema_follower.sql
sqlite3 data/closer.db < sql/schema_closer.sql
sqlite3 data/recorder.db < sql/schema_recorder.sql

# 3. apply views (need b.db + gest.db)
sqlite3 data/b.db    < sql/v_for_gest.sql
sqlite3 data/gest.db < sql/v_for_opener.sql
sqlite3 data/gest.db < sql/v_for_follower.sql
sqlite3 data/gest.db < sql/v_for_closer.sql
sqlite3 data/gest.db < sql/v_for_recorder.sql

# 4. enable WAL
sqlite3 data/gest.db     "PRAGMA journal_mode=WAL;"
sqlite3 data/opener.db   "PRAGMA journal_mode=WAL;"
sqlite3 data/follower.db "PRAGMA journal_mode=WAL;"
sqlite3 data/closer.db   "PRAGMA journal_mode=WAL;"
sqlite3 data/recorder.db "PRAGMA journal_mode=WAL;"

sleep 1

# 5. start workers except gest
systemctl start scalp-opener.service
systemctl start scalp-follower.service
systemctl start scalp-closer.service
systemctl start scalp-recorder.service

sleep 1

# 6. start gest last
systemctl start scalp-gest.service


----- FILE: /opt/scalp/project/bin/sync_and_collect_1m.sh -----
#!/usr/bin/env bash
set -euo pipefail
LOG="/opt/scalp/project/logs/ob_collect_sync.log"

# Attente du top minute exact
sec=$(date +%S)
if [ "$sec" != "00" ]; then
  wait=$((60 - sec))
  echo "[$(date '+%F %T')] attente ${wait}s pour top-minute..." | tee -a "$LOG"
  sleep $wait
fi

# Lancement de la collecte 1m
echo "[$(date '+%F %T')] lancement OB_collect.py (1m)" | tee -a "$LOG"
/opt/scalp/project/venv/bin/python3 /opt/scalp/project/scripts/OB_collect.py --tf 1m >>"$LOG" 2>&1

----- FILE: /opt/scalp/project/bin/sync_ctx_a_to_b.sql -----
ATTACH DATABASE '/opt/scalp/project/data/a.db' AS A;

-- Mise à jour de feat_1m.ctx avec le contexte réel de A
UPDATE feat_1m
SET ctx = (
    SELECT ctx_a.ctx
    FROM A.ctx_a_latest AS ctx_a
    WHERE REPLACE(ctx_a.instId,'/','') = REPLACE(feat_1m.instId,'/','')
)
WHERE EXISTS (
    SELECT 1
    FROM A.ctx_a_latest AS ctx_a
    WHERE REPLACE(ctx_a.instId,'/','') = REPLACE(feat_1m.instId,'/','')
);

DETACH DATABASE A;

----- FILE: /opt/scalp/project/bin/t_db_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/t.db"

echo "===== CLEAN t.db WAL ($(date '+%F %T')) ====="

# Stop tick service
systemctl stop scalp-ticks.service 2>/dev/null || true
sleep 1

echo "[1] WAL FILES BEFORE"
ls -lh ${DB}*

echo "[2] CHECKPOINT WAL"
sqlite3 "$DB" "PRAGMA wal_checkpoint(FULL);"

echo "[3] Disable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=DELETE;"

echo "[4] VACUUM (compact file)"
sqlite3 "$DB" "VACUUM;"

echo "[5] Re-enable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=WAL;"

echo "[6] WAL FILES AFTER"
ls -lh ${DB}*

echo "===== CLEAN DONE ====="

systemctl start scalp-ticks.service 2>/dev/null || true

----- FILE: /opt/scalp/project/bin/t_db_reset_full.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/t.db"

echo "===== FULL RESET t.db ($(date '+%F %T')) ====="

systemctl stop scalp-ticks.service 2>/dev/null || true
sleep 1

echo "[1] Size BEFORE"
ls -lh ${DB}*

echo "[2] DELETE ALL ticks"
sqlite3 "$DB" "DELETE FROM ticks;"

echo "[3] CHECKPOINT WAL"
sqlite3 "$DB" "PRAGMA wal_checkpoint(FULL);"

echo "[4] Disable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=DELETE;"

echo "[5] VACUUM"
sqlite3 "$DB" "VACUUM;"

echo "[6] Re-enable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=WAL;"

echo "[7] Size AFTER"
ls -lh ${DB}*

echo "===== FULL RESET DONE ====="

systemctl start scalp-ticks.service 2>/dev/null || true

----- FILE: /opt/scalp/project/bin/t_ticks.sh -----
#!/usr/bin/env bash
set -euo pipefail
cd /opt/scalp/project
source venv/bin/activate
exec python3 scripts/T_ticks.py

----- FILE: /opt/scalp/project/bin/triggers_db_clean.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/triggers.db"

echo "===== CLEAN triggers.db WAL ($(date '+%F %T')) ====="

# Stop service triggers
systemctl stop scalp-triggers.service 2>/dev/null || true
sleep 1

echo "[1] WAL FILES BEFORE"
ls -lh ${DB}*

echo "[2] CHECKPOINT WAL"
sqlite3 "$DB" "PRAGMA wal_checkpoint(FULL);"

echo "[3] Disable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=DELETE;"

echo "[4] VACUUM"
sqlite3 "$DB" "VACUUM;"

echo "[5] Re-enable WAL"
sqlite3 "$DB" "PRAGMA journal_mode=WAL;"

echo "[6] WAL FILES AFTER"
ls -lh ${DB}*

echo "===== DONE ====="

systemctl start scalp-triggers.service 2>/dev/null || true

----- FILE: /opt/scalp/project/bin/u_universe.sh -----
#!/bin/bash
cd /opt/scalp/project
source venv/bin/activate
python3 scripts/U_universe.py

----- FILE: /opt/scalp/project/bin/x_dash.sh -----
#!/usr/bin/env bash
set -euo pipefail

DB="/opt/scalp/project/data/gest.db"
SERVICES=(
  scalp-signal.service
  scalp-opener.service
  scalp-follower.service
  scalp-closer.service
  scalp-recorder.service
  scalp-budget.service
  scalp-gest-bus.service
)

clear
echo "===== SCALP SYSTEM HEALTH DASH ====="
echo "📅 $(date '+%Y-%m-%d %H:%M:%S')"
echo

# ---- 1. SYSTEMD STATUS ----
printf "%-22s %-8s %-10s\n" "SERVICE" "STATUS" "PID"
echo "---------------------------------------------"

for svc in "${SERVICES[@]}"; do
  if systemctl is-active --quiet "$svc"; then
    pid=$(systemctl show -p MainPID --value "$svc")
    printf "%-22s ✅ %-8s %-10s\n" "$svc" "active" "$pid"
  else
    printf "%-22s ❌ %-8s\n" "$svc" "inactive"
  fi
done

echo
echo "---- 2. BUS EVENTS ----"

sqlite3 -readonly "$DB" <<'SQL'
.headers on
.mode column
.width 10 8 8 22
SELECT src AS MODULE,
       COUNT(*) AS TOTAL,
       COUNT(*) FILTER (WHERE ts > (strftime('%s','now')-300)*1000) AS LAST5MIN,
       MAX(datetime(ts/1000,'unixepoch','localtime')) AS LAST_EVENT
FROM bus_events
GROUP BY src
ORDER BY src;
SQL

echo
echo "---- 3. LATEST 10 EVENTS ----"
sqlite3 -readonly "$DB" <<'SQL'
.headers off
.mode column
.width 10 14 14 22
SELECT src, instId, event, datetime(ts/1000,'unixepoch','localtime')
FROM bus_events
ORDER BY id DESC
LIMIT 10;
SQL

echo
echo "---- 4. SUMMARY ----"
sqlite3 -readonly "$DB" "SELECT COUNT(*) AS total_events, COUNT(DISTINCT src) AS modules, MAX(datetime(ts/1000,'unixepoch','localtime')) AS last_event FROM bus_events;"

echo
echo "✅ DASH refresh every 10s (Ctrl+C to quit)"

----- FILE: /opt/scalp/project/config/scalp.conf -----
UNIVERSE_LIMIT=20

----- FILE: /opt/scalp/project/data/schema_ref.sql -----
-- === SCHEMA GLOBAL SCALP ===
-- Généré le 2025-11-21 15:00:27

-- --- oa.db ---
CREATE TABLE bus_out(
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        src TEXT,
        instId TEXT,
        event TEXT,
        payload TEXT DEFAULT '{}',
        ts INTEGER DEFAULT (strftime('%s','now')*1000),
        ack INTEGER DEFAULT 0
      )
CREATE TABLE ohlcv_15m(
          instId TEXT,
          ts INTEGER,
          open REAL, high REAL, low REAL, close REAL, volume REAL,
          PRIMARY KEY(instId, ts)
        )
CREATE TABLE ohlcv_30m(
          instId TEXT,
          ts INTEGER,
          open REAL, high REAL, low REAL, close REAL, volume REAL,
          PRIMARY KEY(instId, ts)
        )
CREATE TABLE ohlcv_5m(
          instId TEXT,
          ts INTEGER,
          open REAL, high REAL, low REAL, close REAL, volume REAL,
          PRIMARY KEY(instId, ts)
        )
CREATE TABLE sqlite_sequence(name,seq)
CREATE VIEW v_ohlcv_15m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_15m
WHERE ts IN (
  SELECT ts FROM ohlcv_15m AS o2
  WHERE o2.instId = ohlcv_15m.instId
  ORDER BY ts DESC
  LIMIT 150
)
CREATE VIEW v_ohlcv_30m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_30m
WHERE ts IN (
  SELECT ts FROM ohlcv_30m AS o2
  WHERE o2.instId = ohlcv_30m.instId
  ORDER BY ts DESC
  LIMIT 150
)
CREATE VIEW v_ohlcv_5m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_5m
WHERE ts IN (
  SELECT ts FROM ohlcv_5m AS o2
  WHERE o2.instId = ohlcv_5m.instId
  ORDER BY ts DESC
  LIMIT 150
)

-- --- a.db ---
CREATE TABLE bus_out(
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        src TEXT,
        instId TEXT,
        event TEXT,
        payload TEXT DEFAULT '{}',
        ts INTEGER DEFAULT (strftime('%s','now')*1000),
        ack INTEGER DEFAULT 0
      )
CREATE TABLE ctx_A (
    instId   TEXT,
    ts       INTEGER,
    score_U  REAL,
    score_A  REAL,
    p_buy    REAL,
    p_sell   REAL,
    ctx      TEXT
)
CREATE TABLE ctx_a_latest (
    instId TEXT PRIMARY KEY,
    ts INTEGER,
    score_U REAL,
    score_A REAL,
    p_buy REAL,
    p_sell REAL,
    ctx TEXT
)
CREATE TABLE feat_15m(
  instId TEXT,
  ts INT,
  open REAL,
  high REAL,
  low REAL,
  close REAL,
  vol REAL
, ema12 REAL, ema26 REAL, macd  REAL, rsi14 REAL)
CREATE TABLE feat_30m(
  instId TEXT,
  ts INT,
  open REAL,
  high REAL,
  low REAL,
  close REAL,
  vol REAL
, ema12 REAL, ema26 REAL, macd  REAL, rsi14 REAL)
CREATE TABLE feat_5m(
  instId TEXT,
  ts INT,
  open REAL,
  high REAL,
  low REAL,
  close REAL,
  vol REAL
, ema12 REAL, ema26 REAL, macd  REAL, rsi14 REAL)
CREATE TABLE ohlcv_15m (
        instId TEXT,
        ts INTEGER,
        open REAL,
        high REAL,
        low REAL,
        close REAL,
        vol REAL
    )
CREATE TABLE ohlcv_30m (
        instId TEXT,
        ts INTEGER,
        open REAL,
        high REAL,
        low REAL,
        close REAL,
        vol REAL
    )
CREATE TABLE ohlcv_5m (
        instId TEXT,
        ts INTEGER,
        open REAL,
        high REAL,
        low REAL,
        close REAL,
        vol REAL
    )
CREATE TABLE sqlite_sequence(name,seq)
CREATE VIEW v_ctx_latest AS
SELECT
    instId,
    ctx,
    score_A
FROM ctx_A_latest
CREATE VIEW v_ohlcv_freshness AS
SELECT instId, '5m' AS tf,
       COUNT(*) AS n_candles,
       MAX(ts) AS ts_last,
       datetime(MAX(ts)/1000,'unixepoch','localtime') AS last_local,
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1) AS age_s
FROM ohlcv_5m
GROUP BY instId
UNION ALL
SELECT instId, '15m',
       COUNT(*), MAX(ts),
       datetime(MAX(ts)/1000,'unixepoch','localtime'),
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1)
FROM ohlcv_15m
GROUP BY instId
UNION ALL
SELECT instId, '30m',
       COUNT(*), MAX(ts),
       datetime(MAX(ts)/1000,'unixepoch','localtime'),
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1)
FROM ohlcv_30m
GROUP BY instId
ORDER BY tf, age_s ASC

-- --- ob.db ---
CREATE TABLE feat_1m (
            instId TEXT NOT NULL,
            ts INTEGER NOT NULL,
            open REAL, high REAL, low REAL, close REAL, vol REAL,
            PRIMARY KEY(instId, ts)
        )
CREATE TABLE feat_3m (
            instId TEXT NOT NULL,
            ts INTEGER NOT NULL,
            open REAL, high REAL, low REAL, close REAL, vol REAL,
            PRIMARY KEY(instId, ts)
        )
CREATE TABLE feat_5m (
            instId TEXT NOT NULL,
            ts INTEGER NOT NULL,
            open REAL, high REAL, low REAL, close REAL, vol REAL,
            PRIMARY KEY(instId, ts)
        )
CREATE TABLE ohlcv_1m (
    instId TEXT NOT NULL,
    ts     INTEGER NOT NULL,
    o REAL, h REAL, l REAL, c REAL, v REAL,
    PRIMARY KEY(instId, ts)
)
CREATE TABLE ohlcv_3m (
    instId TEXT NOT NULL,
    ts     INTEGER NOT NULL,
    o REAL, h REAL, l REAL, c REAL, v REAL,
    PRIMARY KEY(instId, ts)
)
CREATE TABLE ohlcv_5m (
    instId TEXT NOT NULL,
    ts     INTEGER NOT NULL,
    o REAL, h REAL, l REAL, c REAL, v REAL,
    PRIMARY KEY(instId, ts)
)

-- --- b.db ---
CREATE TABLE feat_1m (
    instId TEXT,
    ts INTEGER,
    o REAL, h REAL, l REAL, c REAL, v REAL,
    ema9 REAL, ema12 REAL, ema21 REAL, ema26 REAL, ema50 REAL,
    macd REAL, macdsignal REAL, macdhist REAL,
    rsi REAL, atr REAL,
    bb_mid REAL, bb_std REAL, bb_up REAL, bb_low REAL, bb_width REAL,
    mom REAL, roc REAL, slope REAL,
    ctx TEXT,
    PRIMARY KEY(instId, ts)
)
CREATE TABLE feat_3m(
  instId TEXT,
  ts INT,
  o REAL,
  h REAL,
  l REAL,
  c REAL,
  v REAL,
  ema9 REAL,
  ema12 REAL,
  ema21 REAL,
  ema26 REAL,
  ema50 REAL,
  macd REAL,
  macdsignal REAL,
  macdhist REAL,
  rsi REAL,
  atr REAL,
  bb_mid REAL,
  bb_std REAL,
  bb_up REAL,
  bb_low REAL,
  bb_width REAL,
  mom REAL,
  roc REAL,
  slope REAL,
  ctx TEXT
)
CREATE TABLE feat_5m(
  instId TEXT,
  ts INT,
  o REAL,
  h REAL,
  l REAL,
  c REAL,
  v REAL,
  ema9 REAL,
  ema12 REAL,
  ema21 REAL,
  ema26 REAL,
  ema50 REAL,
  macd REAL,
  macdsignal REAL,
  macdhist REAL,
  rsi REAL,
  atr REAL,
  bb_mid REAL,
  bb_std REAL,
  bb_up REAL,
  bb_low REAL,
  bb_width REAL,
  mom REAL,
  roc REAL,
  slope REAL,
  ctx TEXT
)
CREATE VIEW v_feat_1m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_1m
CREATE VIEW v_feat_3m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_3m
CREATE VIEW v_feat_5m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_5m

-- --- budget.db ---
CREATE TABLE budget_state(
            id INTEGER PRIMARY KEY CHECK (id=1),
            balance REAL DEFAULT 1000.0,
            margin REAL DEFAULT 0.0,
            pnl_real REAL DEFAULT 0.0,
            ts_update INTEGER
        )
CREATE TABLE bus_out (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    instId TEXT,
    event TEXT,
    ts INTEGER DEFAULT (strftime('%s','now')*1000),
    ack INTEGER DEFAULT 0
, payload TEXT, src TEXT)
CREATE TABLE ledger (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  src TEXT,
  instId TEXT,
  side TEXT,
  type TEXT,        -- margin / pnl_real / deposit / withdraw
  amount REAL,
  ts INTEGER DEFAULT (strftime('%s','now')*1000)
)
CREATE TABLE sqlite_sequence(name,seq)
CREATE VIEW v_balance AS
SELECT ROUND(SUM(CASE
  WHEN type IN ('deposit','pnl_real') THEN amount
  WHEN type='withdraw' THEN -amount
  ELSE 0 END),6) AS balance_usdt
FROM ledger
CREATE VIEW v_budget_overview AS
SELECT
  ROUND(balance,6) AS balance,
  ROUND(margin,6)  AS margin,
  ROUND(pnl_real,6) AS pnl_real,
  datetime(ts_update,'unixepoch','localtime') AS last_update
FROM budget_state
CREATE VIEW v_exposure AS
SELECT
    instId,
    ROUND(SUM(CASE WHEN type='margin' THEN amount ELSE 0 END),6) AS margin,
    ROUND(SUM(CASE WHEN type='pnl_real' THEN amount ELSE 0 END),6) AS pnl_real
FROM ledger
GROUP BY instId
ORDER BY ABS(margin) DESC

-- --- gest.db ---
CREATE TABLE trades (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,
    reason TEXT,
    ctx TEXT,
    score_A REAL,
    score_B REAL,
    price_signal REAL,
    atr_signal REAL,
    ts_signal INTEGER,
    ts_local TEXT,
    hms TEXT,
    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,
    sl REAL,
    tp REAL,
    ts_open INTEGER,
    status TEXT,
    ts_status_update INTEGER
)
CREATE VIEW v_for_closer AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    atr_signal,
    price_signal,
    score_B,
    ctx,
    ts_open,
    ts_follow,
    ts_signal
FROM trades
WHERE status='to_close'
ORDER BY ts_follow DESC
CREATE VIEW v_for_follower AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    ts_open,
    atr_signal,
    price_signal,
    score_B,
    ctx,
    status
FROM trades
WHERE status IN ('opened','followed')
ORDER BY ts_open DESC
CREATE VIEW v_for_opener AS
SELECT
    uid,
    instId,
    side,
    price_signal,
    atr_signal,
    score_B,
    ctx,
    ts_signal
FROM trades
WHERE status='new'
ORDER BY ts_signal DESC
CREATE VIEW v_for_recorder AS
SELECT
    uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price_signal,
    atr_signal,
    ts_signal,

    entry,
    qty,
    lev,
    margin,
    sl AS sl_init,
    tp AS tp_init,
    ts_open,
    ts_follow,

    price_close,
    pnl_usdt,
    pnl_pct,
    ts_close,
    reason_close
FROM trades
WHERE status='closed'
ORDER BY ts_close DESC

-- --- h.db ---
CREATE TABLE bus_out(
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        src TEXT,
        instId TEXT,
        event TEXT,
        payload TEXT DEFAULT '{}',
        ts INTEGER DEFAULT (strftime('%s','now')*1000),
        ack INTEGER DEFAULT 0
      )
CREATE TABLE score_H(
            instId TEXT PRIMARY KEY,
            avg_pnl REAL,
            winrate REAL,
            trades INTEGER,
            last_update INTEGER
        )
CREATE TABLE sqlite_sequence(name,seq)

-- --- t.db ---
CREATE TABLE ticks(
  instId TEXT PRIMARY KEY,
  lastPr REAL,
  ts INTEGER,
  ts_ms INTEGER
)
CREATE VIEW v_ticks_latest AS
SELECT
  t.instId,
  t.lastPr,
  t.ts,
  strftime('%H:%M:%S', t.ts/1000, 'unixepoch','localtime') AS hms,
  printf('%02d:%02d:%02d',
     ((strftime('%s','now')*1000 - t.ts)/1000)/3600,
     (((strftime('%s','now')*1000 - t.ts)/1000)%3600)/60,
     ((strftime('%s','now')*1000 - t.ts)/1000)%60
  ) AS age_hms
FROM ticks t
ORDER BY t.instId

-- --- u.db ---
CREATE TABLE bus_out(
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        src TEXT,
        instId TEXT,
        event TEXT,
        payload TEXT DEFAULT '{}',
        ts INTEGER DEFAULT (strftime('%s','now')*1000),
        ack INTEGER DEFAULT 0
      )
CREATE TABLE sqlite_sequence(name,seq)
CREATE TABLE universe(
  instId TEXT PRIMARY KEY
, score_U REAL DEFAULT 0, active INTEGER DEFAULT 1)

-- --- triggers.db ---
CREATE TABLE sqlite_sequence(name,seq)
CREATE TABLE trig_state (
    instId     TEXT PRIMARY KEY,
    reason     TEXT,
    side       TEXT,
    score      REAL,
    ts         INTEGER
)
CREATE TABLE triggers (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    instId      TEXT    NOT NULL,
    tf          TEXT    NOT NULL,           -- timeframe (ex: '1m')
    side        TEXT    NOT NULL,           -- 'buy' / 'sell'
    reason      TEXT    NOT NULL,           -- type de trigger
    score       REAL    NOT NULL DEFAULT 0, -- score brut du trigger
    ctx         TEXT,                       -- contexte (optionnel)
    confidence  REAL    NOT NULL DEFAULT 0, -- réserve si tu veux
    price       REAL,                       -- lastPr au moment du trigger
    atr         REAL,                       -- ATR utilisé (optionnel)
    ts          INTEGER NOT NULL,           -- timestamp ms
    consumed    INTEGER DEFAULT 0           -- 0 = non pris, 1 = consommé
)
CREATE VIEW v_triggers_latest AS
WITH last_ts AS (
    SELECT instId, MAX(ts) AS ts_last
    FROM triggers
    GROUP BY instId
)
SELECT
    t.instId,
    t.side,
    t.reason,
    t.score   AS score_B,
    t.ctx,
    t.price,
    t.atr,
    t.ts      AS ts_trigger
FROM triggers t
JOIN last_ts lt
  ON lt.instId = t.instId AND lt.ts_last = t.ts
ORDER BY t.ts DESC

-- --- signals.db ---
CREATE TABLE signals (
    trade_uid TEXT PRIMARY KEY,
    instId    TEXT NOT NULL,
    side      TEXT NOT NULL,
    reason    TEXT NOT NULL,
    ctx       TEXT,
    score_A   REAL,
    score_B   REAL,
    price     REAL NOT NULL,
    atr       REAL,
    ts_signal INTEGER NOT NULL,
    ts_local  TEXT,
    hms       TEXT
)
CREATE VIEW v_for_gest AS
SELECT
    trade_uid AS uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price AS price_signal,
    atr AS atr_signal,
    ts_signal,
    ts_local,
    hms
FROM signals
ORDER BY ts_signal DESC

-- --- opener.db ---
CREATE TABLE trades_opener (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,
    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,
    sl REAL,
    tp REAL,
    ts_open INTEGER,
    status TEXT
)
CREATE VIEW v_opener_for_gest AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    ts_open
FROM trades_opener
WHERE status='opened'
ORDER BY ts_open DESC

-- --- follower.db ---
CREATE TABLE trades_follower (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,

    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,

    sl REAL,
    tp REAL,

    atr REAL,
    price_open REAL,
    score_B REAL,
    ctx TEXT,

    ts_open INTEGER,
    ts_follow INTEGER,

    sl_updated INTEGER,
    tp_updated INTEGER,
    be_triggered INTEGER,
    pyramided INTEGER,

    status TEXT
)
CREATE VIEW v_follower_for_gest AS
SELECT
    uid, instId, side,
    entry, qty, lev,
    sl, tp,
    ts_follow,
    status
FROM trades_follower
WHERE status='followed'
ORDER BY ts_follow DESC

-- --- closer.db ---
CREATE TABLE trades_closer (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,

    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,

    sl REAL,
    tp REAL,

    price_close REAL,
    ts_close INTEGER,

    pnl_usdt REAL,
    pnl_pct REAL,

    reason_close TEXT
)
CREATE VIEW v_closer_for_gest AS
SELECT
    uid,
    instId,
    price_close,
    pnl_usdt,
    pnl_pct,
    ts_close,
    reason_close
FROM trades_closer
ORDER BY ts_close DESC

-- --- recorder.db ---
CREATE TABLE trades_record (
    uid TEXT PRIMARY KEY,

    -- SIGNAL
    instId TEXT,
    side TEXT,
    reason TEXT,
    ctx TEXT,
    score_A REAL,
    score_B REAL,
    price_signal REAL,
    atr_signal REAL,
    ts_signal INTEGER,

    -- OPENER
    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,
    sl_init REAL,
    tp_init REAL,
    ts_open INTEGER,

    -- FOLLOWER
    sl_final REAL,
    tp_final REAL,
    be_triggered INTEGER,
    pyramided INTEGER,
    ts_follow INTEGER,

    -- CLOSER
    price_close REAL,
    pnl_usdt REAL,
    pnl_pct REAL,
    ts_close INTEGER,
    reason_close TEXT,

    -- META
    ts_recorded INTEGER
)
CREATE VIEW v_recorder_for_gest AS
SELECT
    uid,
    ts_recorded
FROM trades_record
ORDER BY ts_recorded DESC

-- === FIN ===

----- FILE: /opt/scalp/project/requirements.txt -----
ccxt>=4.2.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
python-dateutil>=2.8.2
aiohttp>=3.9.0
matplotlib>=3.8.0

----- FILE: /opt/scalp/project/scripts/A_analyse.py -----
#!/usr/bin/env python3
import sqlite3, math, time

DB_A = "/opt/scalp/project/data/a.db"
DB_U = "/opt/scalp/project/data/u.db"
DB_OB = "/opt/scalp/project/data/ob.db"
TF = "5m"

def softmax(x):
    e = [math.exp(i) for i in x]
    s = sum(e)
    return [v/s for v in e]

def get_universe():
    con = sqlite3.connect(DB_U)
    rows = con.execute("SELECT instId FROM universe ORDER BY instId").fetchall()
    con.close()
    return [r[0] for r in rows]

def load_ob(inst):
    con = sqlite3.connect(DB_OB)
    row = con.execute(f"SELECT rsi FROM ob_{TF} WHERE instId=? ORDER BY ts DESC LIMIT 1", (inst,)).fetchone()
    con.close()
    return row[0] if row else None

def compute_ctx(rsi):
    if rsi < 30: return "bullish"
    if rsi > 70: return "bearish"
    if 45 <= rsi <= 55: return "range"
    return "none"

def compute_score_A(ctx):
    if ctx == "bullish": return 0.8
    if ctx == "bearish": return -0.8
    if ctx == "range": return 0.0
    return 0.0

def run():
    insts = get_universe()
    now = int(time.time()*1000)
    out = []

    for inst in insts:
        rsi = load_ob(inst)
        if rsi is None:
            continue
        ctx = compute_ctx(rsi)
        score_A = compute_score_A(ctx)
        p_buy, p_sell = softmax([score_A, -score_A])
        out.append((inst, now, 0.0, score_A, p_buy, p_sell, ctx))

    con = sqlite3.connect(DB_A)
    con.execute("DELETE FROM ctx_a_latest")
    con.executemany("INSERT INTO ctx_a_latest(instId,ts,score_U,score_A,p_buy,p_sell,ctx) VALUES (?,?,?,?,?,?,?)", out)
    con.commit()
    con.close()

if __name__ == "__main__":
    run()

----- FILE: /opt/scalp/project/scripts/A_config.py -----
#!/usr/bin/env python3

# DB paths
DB_A  = "/opt/scalp/project/data/a.db"
DB_OB = "/opt/scalp/project/data/ob.db"
DB_OA = "/opt/scalp/project/data/oa.db"
DB_U  = "/opt/scalp/project/data/u.db"

# TF used
TF_SHORT = "5m"
TF_MED   = "15m"
TF_LONG  = "30m"

# Weights for score (softmax later)
W_SHORT = 0.50
W_MED   = 0.30
W_LONG  = 0.20

----- FILE: /opt/scalp/project/scripts/A_ctx.py -----
#!/usr/bin/env python3
# SCALP - A CTX (contexte directionnel + intensité)
# score_U : ratio volume/volatilité (0→1)
# score_A : force du contexte directionnel (0→1)

import sqlite3
import numpy as np
import pandas as pd
import logging
import sys
from datetime import datetime

# ---------------------------------------
# LOGGING
# ---------------------------------------
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s A_CTX %(levelname)s %(message)s",
    handlers=[
        logging.StreamHandler(sys.stdout),
        logging.FileHandler("/opt/scalp/project/logs/a_ctx.log", mode='a')
    ]
)

# ---------------------------------------
# DB CONNECT
# ---------------------------------------
def connect_db(path):
    conn = sqlite3.connect(path, timeout=30, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    return conn

# ---------------------------------------
# LOAD FEAT TABLES
# ---------------------------------------
def load_feat(conn, table):
    q = f"""
        SELECT instId, ts, ema12, ema26, macd, rsi14
        FROM {table}
        ORDER BY instId, ts;
    """
    return pd.read_sql_query(q, conn)

def load_ohlcv_with_volume(conn, table):
    q = f"""
        SELECT instId, ts, close, volume
        FROM {table}
        ORDER BY instId, ts;
    """
    return pd.read_sql_query(q, conn)

# ---------------------------------------
# COMPUTE SCORE_U : volume / volatilité
# ---------------------------------------
def compute_score_U(df_feat, df_ohlcv):
    df = df_feat.merge(df_ohlcv, on=["instId","ts"], how="inner")

    # volatilité : |close - ema26|
    df["volatility"] = (df["close"] - df["ema26"]).abs()

    df["raw_U"] = df["volume"] / (df["volatility"] + 1e-9)

    # NORMALISATION DOUCE
    df["score_U"] = np.tanh(df["raw_U"])

    return df[["instId","ts","score_U"]]

# ---------------------------------------
# SCORE TF DIRECTIONNEL
# ---------------------------------------
def compute_score_tf(df):
    # signaux directionnels simples
    s_ema  = np.tanh((df["ema12"] - df["ema26"]) / (1e-8 + abs(df["ema26"])))
    s_macd = np.tanh(df["macd"])
    s_rsi  = np.tanh((df["rsi14"] - 50) / 50)

    S = 0.5 * s_ema + 0.3 * s_macd + 0.2 * s_rsi
    return float(S)

# ---------------------------------------
# SOFTMAX DIRECTIONNEL
# ---------------------------------------
def softmax_ctx(S):
    e_buy  = np.exp(S)
    e_sell = np.exp(-S)
    e_rng  = 1.0

    Z = e_buy + e_sell + e_rng
    p_buy  = float(e_buy  / Z)
    p_sell = float(e_sell / Z)

    if p_buy >= max(p_sell, 1 - p_buy - p_sell):
        ctx = "bullish"
    elif p_sell >= max(p_buy, 1 - p_buy - p_sell):
        ctx = "bearish"
    else:
        ctx = "range"

    return p_buy, p_sell, ctx

# ---------------------------------------
# MAIN CTX PIPELINE
# ---------------------------------------
def main():
    logging.info("Start A_CTX (light)")

    conn_a  = connect_db("/opt/scalp/project/data/a.db")
    conn_oa = connect_db("/opt/scalp/project/data/oa.db")

    # Load features
    f5  = load_feat(conn_a, "feat_5m")
    f15 = load_feat(conn_a, "feat_15m")
    f30 = load_feat(conn_a, "feat_30m")

    # Load ohlcv for score_U
    ohlcv5 = load_ohlcv_with_volume(conn_oa, "ohlcv_5m")

    # Compute score_U
    dfU = compute_score_U(f5, ohlcv5)  # basé sur TF rapide (5m)
    dfU = dfU.groupby("instId").tail(1).set_index("instId")

    # Latest rows per TF
    f5_latest  = f5.groupby("instId").tail(1).set_index("instId")
    f15_latest = f15.groupby("instId").tail(1).set_index("instId")
    f30_latest = f30.groupby("instId").tail(1).set_index("instId")

    # Clear ctx table
    conn_a.execute("DELETE FROM ctx_a_latest;")

    ts_now = int(datetime.now().timestamp() * 1000)

    insts = sorted(f5_latest.index.unique())
    for inst in insts:
        try:
            r5  = f5_latest.loc[inst]
            r15 = f15_latest.loc[inst]
            r30 = f30_latest.loc[inst]
            u   = dfU.loc[inst]["score_U"]

            # Direction TF
            S5  = compute_score_tf(r5)
            S15 = compute_score_tf(r15)
            S30 = compute_score_tf(r30)

            # Fusion multi-TF
            S = 0.5*S5 + 0.3*S15 + 0.2*S30

            # score_A = force directionnelle (0→1)
            score_A = abs(S)

            # softmax
            p_buy, p_sell, ctx = softmax_ctx(S)

            logging.info(f"{inst} S={S:.4f} scoreU={u:.3f} pbuy={p_buy:.3f} psell={p_sell:.3f} ctx={ctx}")

            conn_a.execute("""
                INSERT INTO ctx_a_latest (instId, ts, score_U, score_A, p_buy, p_sell, ctx)
                VALUES (?, ?, ?, ?, ?, ?, ?)
            """, (inst, ts_now, float(u), float(score_A), p_buy, p_sell, ctx))

        except Exception as e:
            logging.error(f"{inst} error: {e}")

    logging.info("Done.")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/A_ctx_dash.py -----
#!/usr/bin/env python3
# SCALP - Dash Contexte A (CTX + Scores + TF Freshness)

import sqlite3
import pandas as pd
from datetime import datetime
import os

DB_A  = "/opt/scalp/project/data/a.db"
DB_OA = "/opt/scalp/project/data/oa.db"

def connect(path):
    conn = sqlite3.connect(path, timeout=30, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL;")
    return conn

# ---------------------------------------------------------
# LOAD CTX A
# ---------------------------------------------------------
def load_ctx():
    conn = connect(DB_A)
    q = """
        SELECT instId, ts,
               score_U, score_A,
               p_buy, p_sell, ctx
        FROM ctx_a_latest
        ORDER BY instId;
    """
    df = pd.read_sql_query(q, conn)
    conn.close()
    return df

# ---------------------------------------------------------
# FRESHNESS OA
# ---------------------------------------------------------
FRESH_LIMITS = {
    "5m":  300,     # 5 min
    "15m": 900,     # 15 min
    "30m": 1800     # 30 min
}

def load_freshness(tf):
    conn = connect(DB_OA)
    table = f"ohlcv_{tf}"
    q = f"""
        SELECT MAX(ts) as last_ts
        FROM {table};
    """
    row = conn.execute(q).fetchone()
    conn.close()

    if row is None or row[0] is None:
        return None, None, False

    last_ts = row[0]
    age = int(datetime.now().timestamp()*1000 - last_ts) // 1000
    ok = age < FRESH_LIMITS[tf]
    return last_ts, age, ok

# ---------------------------------------------------------
# DISPLAY DASH
# ---------------------------------------------------------
def show():
    os.system("clear")
    print("===== CTX DASH (A) =====\n")

    df = load_ctx()
    
    if df.empty:
        print("CTX vide — A_CTX n'a pas encore tourné.")
        return

    # SUMMARY CONTEXT
    bullish = (df["ctx"] == "bullish").sum()
    bearish = (df["ctx"] == "bearish").sum()
    range_c = (df["ctx"] == "range").sum()
    total   = len(df)

    print("=== CTX SUMMARY ===")
    print(f"bullish | {bullish}")
    print(f"bearish | {bearish}")
    print(f"range   | {range_c}")
    print(f"-------------------")
    print(f"TOTAL   | {total}\n")

    # TOP 5 TRIÉ PAR score_A DESC
    df_sorted = df.sort_values("score_A", ascending=False).head(5)

    print("=== TOP CONTEXTUEL (TOP 5 trié par score_A) ===")
    print(f"{'instId':8} {'ctx':7} {'sU':>5} {'sA':>5} {'pB':>5} {'pS':>5} {'age':>10}")
    print("-"*60)

    now = datetime.now()

    for _, r in df_sorted.iterrows():
        ts = datetime.fromtimestamp(r["ts"] / 1000)
        age = now - ts
        ah = age.seconds // 3600
        am = (age.seconds // 60) % 60
        asec = age.seconds % 60

        print(f"{r['instId']:8} "
              f"{r['ctx']:7} "
              f"{r['score_U']:.2f} "
              f"{r['score_A']:.2f} "
              f"{r['p_buy']:.2f} "
              f"{r['p_sell']:.2f} "
              f"{ah:02d}:{am:02d}:{asec:02d}")


    # FRESHNESS OA
    print("=== LAST OA CANDLES USED ===")
    print(f"{'tf':3} {'last_ts':19} {'age_s':7} {'status'}")
    print("--  -------------------  -------  -------")

    for tf in ["5m", "15m", "30m"]:
        last_ts, age, ok = load_freshness(tf)
        if last_ts is None:
            print(f"{tf:3} {'---':19} {'---':7}  ❌")
            continue

        last = datetime.fromtimestamp(last_ts / 1000).strftime("%Y-%m-%d %H:%M:%S")
        status = "✅" if ok else "⚠️"

        print(f"{tf:3} {last:19} {age:7}  {status}")


if __name__ == "__main__":
    show()

----- FILE: /opt/scalp/project/scripts/A_feat_builder.py -----
#!/usr/bin/env python3
# SCALP - A Feature Builder (5m/15m/30m)

import sqlite3
import pandas as pd
import numpy as np
import logging
import sys

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s A_FEAT %(levelname)s %(message)s",
    handlers=[
        logging.StreamHandler(sys.stdout),
        logging.FileHandler("/opt/scalp/project/logs/a_feat_builder.log", mode='a')
    ]
)

def connect_db(path):
    conn = sqlite3.connect(path, timeout=30, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    return conn

# ---------------------------
# Indicators
# ---------------------------
def compute_indicators(df):
    df = df.sort_values("ts")

    df["ema12"] = df.groupby("instId")["close"].transform(lambda x: x.ewm(span=12).mean())
    df["ema26"] = df.groupby("instId")["close"].transform(lambda x: x.ewm(span=26).mean())
    df["macd"]  = df["ema12"] - df["ema26"]

    def rsi(close):
        delta = close.diff()
        gain = delta.clip(lower=0)
        loss = -delta.clip(upper=0)
        avg_gain = gain.rolling(14).mean()
        avg_loss = loss.rolling(14).mean()
        rs = avg_gain / (avg_loss + 1e-9)
        return 100 - (100 / (1 + rs))

    df["rsi14"] = df.groupby("instId")["close"].transform(rsi)
    return df

# ---------------------------
# Load OHLCV
# ---------------------------
def load_tf(conn, table):
    q = f"""
        SELECT instId, ts, open, high, low, close, volume
        FROM {table}
        ORDER BY instId, ts;
    """
    return pd.read_sql_query(q, conn)

# ---------------------------
# Write FEAT
# ---------------------------
def write_features(conn, df, table):
    conn.execute(f"DELETE FROM {table};")
    out = df[["instId", "ts", "ema12", "ema26", "macd", "rsi14"]]
    out.to_sql(table, conn, if_exists="append", index=False)

# ---------------------------
# MAIN
# ---------------------------
def main():
    logging.info("Start A_FEAT...")

    conn_oa = connect_db("/opt/scalp/project/data/oa.db")
    conn_a  = connect_db("/opt/scalp/project/data/a.db")

    TF = {
        "5m":  ("ohlcv_5m",  "feat_5m"),
        "15m": ("ohlcv_15m", "feat_15m"),
        "30m": ("ohlcv_30m", "feat_30m")
    }

    for tf, (ohlcv_table, feat_table) in TF.items():
        logging.info(f"Load {tf} OHLCV…")
        df = load_tf(conn_oa, ohlcv_table)

        if df.empty:
            logging.warning(f"{tf} empty, skip")
            continue

        logging.info(f"Compute indicators {tf}…")
        df = compute_indicators(df)

        logging.info(f"Write {feat_table}…")
        write_features(conn_a, df, feat_table)

    logging.info("A_FEAT completed OK")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/B_feat_builder_full.py -----
#!/usr/bin/env python3
import sqlite3
import pandas as pd
import numpy as np
import time
import logging
import sys
import os

DB_A = "/opt/scalp/project/data/a.db"
DB_OB = "/opt/scalp/project/data/ob.db"
DB_B = "/opt/scalp/project/data/b.db"
LOGFILE = "/opt/scalp/project/logs/B_feat_builder_full.log"

os.makedirs(os.path.dirname(LOGFILE), exist_ok=True)

logging.basicConfig(
    filename=LOGFILE,
    level=logging.INFO,
    format="%(asctime)s B_FEAT_FULL %(levelname)s %(message)s"
)

BUSY_TIMEOUT = 5000

def connect(db):
    conn = sqlite3.connect(db, timeout=BUSY_TIMEOUT)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    conn.execute("PRAGMA busy_timeout=%d;" % BUSY_TIMEOUT)
    return conn

def ema(series, span):
    return series.ewm(span=span, adjust=False).mean()

def rsi(series, period=14):
    delta = series.diff()
    gain = np.where(delta > 0, delta, 0)
    loss = np.where(delta < 0, -delta, 0)
    roll_up = pd.Series(gain).rolling(period).mean()
    roll_down = pd.Series(loss).rolling(period).mean()
    rs = roll_up / roll_down
    return 100 - (100 / (1 + rs))

def atr(df, period=14):
    high = df["h"]
    low = df["l"]
    close = df["c"]
    prev_close = close.shift(1)
    tr = pd.concat([
        (high - low),
        (high - prev_close).abs(),
        (low - prev_close).abs()
    ], axis=1).max(axis=1)
    return tr.rolling(period).mean()

def compute_features(df):
    df["ema9"]  = ema(df["c"], 9)
    df["ema12"] = ema(df["c"], 12)
    df["ema21"] = ema(df["c"], 21)
    df["ema26"] = ema(df["c"], 26)
    df["ema50"] = ema(df["c"], 50)

    df["macd"]   = df["ema12"] - df["ema26"]
    df["signal"] = ema(df["macd"], 9)
    df["hist"]   = df["macd"] - df["signal"]

    df["rsi"]  = rsi(df["c"], 14)
    df["atr"]  = atr(df, 14)

    df["bb_mid"] = df["c"].rolling(20).mean()
    df["bb_std"] = df["c"].rolling(20).std()
    df["bb_up"]  = df["bb_mid"] + 2*df["bb_std"]
    df["bb_low"] = df["bb_mid"] - 2*df["bb_std"]
    df["bb_width"] = (df["bb_up"] - df["bb_low"]) / df["bb_mid"]

    df["mom"] = df["c"].diff(5)
    df["roc"] = df["c"].pct_change(5)
    df["slope"] = df["c"].diff()

    return df

def load_ctx():
    connA = connect(DB_A)
    df = pd.read_sql_query("SELECT instId, ctx FROM ctx_cache;", connA)
    connA.close()
    return dict(zip(df.instId, df.ctx))

def main():
    if len(sys.argv) < 2:
        print("usage: B_feat_builder_full.py <3m|5m>")
        sys.exit(1)

    tf = sys.argv[1]
    table_ohlcv = f"ohlcv_{tf}"
    table_feat  = f"feat_{tf}"

    logging.info(f"START FULL TF={tf}")

    ctx_map = load_ctx()

    conn_ob = connect(DB_OB)
    df = pd.read_sql_query(f"""
        SELECT instId, ts, o, h, l, c, v
        FROM {table_ohlcv}
        ORDER BY instId, ts ASC
    """, conn_ob)
    conn_ob.close()

    if df.empty:
        logging.warning("empty ohlcv")
        return

    df = compute_features(df)
    df["ctx"] = df["instId"].map(ctx_map).fillna("unknown")

    conn_b = connect(DB_B)
    cur = conn_b.cursor()
    conn_b.execute("BEGIN;")

    cur.execute(f"DELETE FROM {table_feat};")

   for _, row in df.iterrows():
        cur.execute(f"""
            INSERT INTO {table_feat} (
                instId, ts, o, h, l, c, v,
                ema9, ema12, ema21, ema26, ema50,
                macd, signal, hist,
                rsi, atr,
                bb_mid, bb_std, bb_up, bb_low, bb_width,
                mom, roc, slope,
                ctx
            ) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
        """, tuple(row))

    conn_b.commit()
    conn_b.close()

    logging.info(f"done FULL TF={tf} rows={len(df)}")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/B_feat_builder_incremental.py -----
#!/usr/bin/env python3
import sqlite3
import pandas as pd
import numpy as np
import time
import logging
import sys
import os

DB_A = "/opt/scalp/project/data/a.db"
DB_OB = "/opt/scalp/project/data/ob.db"
DB_B = "/opt/scalp/project/data/b.db"
LOGFILE = "/opt/scalp/project/logs/B_feat_builder_incremental.log"

os.makedirs(os.path.dirname(LOGFILE), exist_ok=True)

logging.basicConfig(
    filename=LOGFILE,
    level=logging.INFO,
    format="%(asctime)s B_FEAT_1M %(levelname)s %(message)s"
)

BUSY_TIMEOUT = 5000

def connect(db):
    conn = sqlite3.connect(db, timeout=BUSY_TIMEOUT)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    conn.execute("PRAGMA busy_timeout=%d;" % BUSY_TIMEOUT)
    return conn

def ema(series, span):
    return series.ewm(span=span, adjust=False).mean()

def rsi(series, period=14):
    delta = series.diff()
    gain = np.where(delta > 0, delta, 0)
    loss = np.where(delta < 0, -delta, 0)
    roll_up = pd.Series(gain).rolling(period).mean()
    roll_down = pd.Series(loss).rolling(period).mean()
    rs = roll_up / roll_down
    return 100 - (100 / (1 + rs))

def atr(df, period=14):
    high = df["h"]
    low = df["l"]
    close = df["c"]
    prev_close = close.shift(1)
    tr = pd.concat([
        (high - low),
        (high - prev_close).abs(),
        (low - prev_close).abs()
    ], axis=1).max(axis=1)
    return tr.rolling(period).mean()

def compute_features(df):
    df["ema9"]  = ema(df["c"], 9)
    df["ema12"] = ema(df["c"], 12)
    df["ema21"] = ema(df["c"], 21)
    df["ema26"] = ema(df["c"], 26)
    df["ema50"] = ema(df["c"], 50)

    df["macd"]   = df["ema12"] - df["ema26"]
    df["signal"] = ema(df["macd"], 9)
    df["hist"]   = df["macd"] - df["signal"]

    df["rsi"] = rsi(df["c"], 14)

    df["atr"] = atr(df, 14)

    df["bb_mid"] = df["c"].rolling(20).mean()
    df["bb_std"] = df["c"].rolling(20).std()
    df["bb_up"]  = df["bb_mid"] + 2*df["bb_std"]
    df["bb_low"] = df["bb_mid"] - 2*df["bb_std"]
    df["bb_width"] = (df["bb_up"] - df["bb_low"]) / df["bb_mid"]

    df["mom"] = df["c"].diff(5)
    df["roc"] = df["c"].pct_change(5)
    df["slope"] = df["c"].diff()

    return df

def load_ctx():
    connA = connect(DB_A)
    df = pd.read_sql_query("SELECT instId, ctx FROM ctx_cache;", connA)
    connA.close()
    return dict(zip(df.instId, df.ctx))

def main():
    logging.info("START 1m incremental")

    ctx_map = load_ctx()

    conn_ob = connect(DB_OB)
    df = pd.read_sql_query("""
        SELECT instId, ts, o, h, l, c, v
        FROM ohlcv_1m
        ORDER BY instId, ts ASC
    """, conn_ob)
    conn_ob.close()

    if df.empty:
        logging.warning("no 1m data")
        return

    df = compute_features(df)
    df["ctx"] = df["instId"].map(ctx_map).fillna("unknown")

    conn_b = connect(DB_B)
    cur = conn_b.cursor()
    conn_b.execute("BEGIN;")
    for _, row in df.iterrows():
        cur.execute("""
            INSERT OR REPLACE INTO feat_1m (
                instId, ts, o, h, l, c, v,
                ema9, ema12, ema21, ema26, ema50,
                macd, signal, hist,
                rsi, atr,
                bb_mid, bb_std, bb_up, bb_low, bb_width,
                mom, roc, slope,
                ctx
            ) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
        """, tuple(row))
    conn_b.commit()
    conn_b.close()

    logging.info(f"done 1m rows={len(df)}")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/H_perf.py -----
#!/usr/bin/env python3
import sqlite3, time, statistics, logging, sys
from pathlib import Path

LOG = "/opt/scalp/project/logs/H_perf.log"
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s %(message)s",
    handlers=[logging.FileHandler(LOG), logging.StreamHandler(sys.stdout)]
)

DB_CLOSED = "/opt/scalp/project/data/x_closed.db"
DB_H = "/opt/scalp/project/data/h.db"

def connect_db(path):
    con = sqlite3.connect(path, timeout=30, isolation_level=None)
    con.execute("PRAGMA journal_mode=WAL;")
    con.execute("PRAGMA synchronous=NORMAL;")
    con.execute("PRAGMA busy_timeout=5000;")
    return con

def ensure_closed_schema():
    con = connect_db(DB_CLOSED)
    con.execute("""
        CREATE TABLE IF NOT EXISTS positions_closed(
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            instId TEXT, side TEXT, entry REAL, exit REAL,
            sl REAL, tp REAL, pnl REAL, reason TEXT,
            ts_open INTEGER, ts_close INTEGER
        )
    """)
    con.close()

def migrate_h():
    con = connect_db(DB_H)
    con.execute("""
        CREATE TABLE IF NOT EXISTS score_H(
            instId TEXT PRIMARY KEY,
            avg_pnl REAL,
            winrate REAL,
            trades INTEGER,
            last_update INTEGER
        )
    """)
    con.close()

def compute_score():
    ensure_closed_schema()
    con_c = connect_db(DB_CLOSED)
    rows = con_c.execute("SELECT instId, pnl FROM positions_closed").fetchall()
    con_c.close()
    if not rows:
        logging.info("Aucun trade clos, pas de calcul.")
        return
    data = {}
    for inst,pnl in rows:
        data.setdefault(inst,[]).append(pnl)
    con_h = connect_db(DB_H)
    for inst,vals in data.items():
        avg = statistics.mean(vals)
        winrate = sum(1 for v in vals if v>0)/len(vals)
        con_h.execute("""
            INSERT OR REPLACE INTO score_H(instId,avg_pnl,winrate,trades,last_update)
            VALUES(?,?,?,?,?)
        """,(inst,avg,winrate,len(vals),int(time.time()*1000)))
    con_h.close()
    logging.info(f"Scores H mis à jour pour {len(data)} instruments.")

def main(argv):
    migrate_h()
    compute_score()

if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))

----- FILE: /opt/scalp/project/scripts/H_score.py -----
#!/usr/bin/env python3
import sqlite3, time

DB_H="/opt/scalp/project/data/x_history.db"

def latest_H():
    con=sqlite3.connect(DB_H)
    row=con.execute("SELECT score_H FROM v_H_latest").fetchone()
    con.close()
    return float(row[0]) if row and row[0] else 0.0

def run():
    h=latest_H()
    print(f"[H] score_H={h:.3f}")

if __name__=="__main__":
    run()

----- FILE: /opt/scalp/project/scripts/OA_HYBRID.py -----
#!/usr/bin/env python3
import time
import sqlite3
import ccxt
from datetime import datetime

DB_WS = "/opt/scalp/project/data/ws.db"
DB_OA = "/opt/scalp/project/data/oa.db"
DB_U  = "/opt/scalp/project/data/u.db"

TF_MS = {
    "5m":  5  * 60 * 1000,
    "15m": 15 * 60 * 1000,
    "30m": 30 * 60 * 1000
}

WS_TABLE = {
    "5m":  "ws_ohlcv_5m",
    "15m": "ws_ohlcv_15m",
    "30m": "ws_ohlcv_30m"
}

OA_TABLE = {
    "5m":  "ohlcv_5m",
    "15m": "ohlcv_15m",
    "30m": "ohlcv_30m"
}

###############################################################################
# DB helpers
###############################################################################
def conn_ws():
    c = sqlite3.connect(DB_WS, timeout=10)
    c.execute("PRAGMA journal_mode=WAL;")
    return c

def conn_oa():
    c = sqlite3.connect(DB_OA, timeout=10)
    c.execute("PRAGMA journal_mode=WAL;")
    return c

def load_universe():
    conn = sqlite3.connect(DB_U)
    rows = conn.execute("SELECT instId FROM universe ORDER BY instId;").fetchall()
    conn.close()
    return [r[0] for r in rows]

###############################################################################
# WS query
###############################################################################
def get_last_ws_closed(ws_con, instId, tf):
    table = WS_TABLE[tf]
    row = ws_con.execute(
        f"SELECT ts, open, high, low, close, volume FROM {table} WHERE instId=? ORDER BY ts DESC LIMIT 1",
        (instId,)
    ).fetchone()
    return row if row else None

###############################################################################
# CCXT fallback
###############################################################################
def fetch_ccxt(instId, tf, since_ms):
    ex = ccxt.bitget({
        "options":{"defaultType":"swap"},
        "rateLimit":10
    })
    market = instId.replace("/", "")
    tf_ccxt = tf
    return ex.fetch_ohlcv(market, timeframe=tf_ccxt, limit=5, since=since_ms)

###############################################################################
# Insert into OA
###############################################################################
def insert_oa(oa_con, tf, instId, ts, o, h, l, c, v):
    table = OA_TABLE[tf]
    oa_con.execute(
        f"""
        INSERT OR REPLACE INTO {table}(instId, ts, open, high, low, close, volume)
        VALUES (?,?,?,?,?,?,?)
        """,
        (instId, ts, o, h, l, c, v)
    )

###############################################################################
# HYBRID LOGIC
###############################################################################
def process_instrument(instId, ws_con, oa_con):
    now_ms = int(time.time() * 1000)

    for tf in ("5m","15m","30m"):
        tf_size = TF_MS[tf]

        # 1. Get latest WS closed candle
        ws = get_last_ws_closed(ws_con, instId, tf)
        ws_ts = ws[0] if ws else None

        # 2. Get OA last candle
        row = oa_con.execute(
            f"SELECT ts FROM {OA_TABLE[tf]} WHERE instId=? ORDER BY ts DESC LIMIT 1",
            (instId,)
        ).fetchone()
        oa_ts = row[0] if row else 0

        # CASE A: WS is fresh → use WS
        if ws_ts and (now_ms - ws_ts) < (tf_size * 1.5):
            if ws_ts > oa_ts:
                insert_oa(oa_con, tf, instId, ws_ts, *ws[1:])
                print(f"[OA] WS {tf} {instId} ts={ws_ts} applied")
            continue

        # CASE B: WS missing or stale → fallback REST
        since_ms = oa_ts if oa_ts else now_ms - (tf_size * 5)
        ohlcv = fetch_ccxt(instId, tf, since_ms)
        if not ohlcv:
            continue

        for ts, o, h, l, c, v in ohlcv:
            if ts > oa_ts:
                insert_oa(oa_con, tf, instId, ts, o, h, l, c, v)
                print(f"[OA] CCXT {tf} {instId} ts={ts} applied")

###############################################################################
# MAIN
###############################################################################
def main():
    t0 = time.time()
    ws_con = conn_ws()
    oa_con = conn_oa()

    universe = load_universe()
    for instId in universe:
        try:
            process_instrument(instId, ws_con, oa_con)
        except Exception as e:
            print(f"[OA] ERROR {instId}: {e}")

    oa_con.commit()
    ws_con.close()
    oa_con.close()

    print(f"[OA] HYBRID sync completed in {time.time()-t0:.3f}s")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/OA_ohlcv_A.py -----
#!/usr/bin/env python3
import time
import sqlite3
import ccxt
from datetime import datetime

DB_U  = "/opt/scalp/project/data/u.db"
DB_OA = "/opt/scalp/project/data/oa.db"

TF_MS = {
    "5m":  5  * 60 * 1000,
    "15m": 15 * 60 * 1000,
    "30m": 30 * 60 * 1000
}

TABLE = {
    "5m":  "ohlcv_5m",
    "15m": "ohlcv_15m",
    "30m": "ohlcv_30m"
}

###############################################################################
# DB helpers
###############################################################################
def load_universe():
    c = sqlite3.connect(DB_U)
    rows = c.execute("SELECT instId FROM universe ORDER BY instId").fetchall()
    c.close()
    return [r[0] for r in rows]

def conn_oa():
    c = sqlite3.connect(DB_OA, timeout=10)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA synchronous=NORMAL;")
    return c

###############################################################################
# CCXT fetch
###############################################################################
EX = ccxt.bitget({"options":{"defaultType":"swap"}})

def fetch_missing(instId, tf, since_ts):
    market = instId.replace("/", "")
    try:
        return EX.fetch_ohlcv(market, timeframe=tf, since=since_ts, limit=5)
    except Exception as e:
        print(f"[OA] CCXT error {instId} {tf}: {e}")
        return []

###############################################################################
# Insert rows
###############################################################################
def insert_row(con, tf, instId, ts, o, h, l, c, v):
    con.execute(
        f"INSERT OR REPLACE INTO {TABLE[tf]}(instId, ts, open, high, low, close, volume)"
        f" VALUES (?,?,?,?,?,?,?)",
        (instId, ts, o, h, l, c, v)
    )

###############################################################################
# Main OA logic
###############################################################################
def sync_inst(con, instId):
    now = int(time.time() * 1000)

    for tf, tf_ms in TF_MS.items():

        row = con.execute(
            f"SELECT ts FROM {TABLE[tf]} WHERE instId=? ORDER BY ts DESC LIMIT 1",
            (instId,)
        ).fetchone()

        last_ts = row[0] if row else (now - tf_ms * 10)

        # Fetch only the missing window
        ohlcv = fetch_missing(instId, tf, last_ts)
        if not ohlcv:
            continue

        for ts, o, h, l, c, v in ohlcv:

            # Normaliser timestamp (sécurité)
            if ts < 10**11:   # seconde → millisecondes
                ts = ts * 1000

            # Validation strict millisecondes
            if ts > now + 3600 * 1000:
                print(f"[OA] WARNING {instId} {tf} future-ts={ts}")
                continue

            if ts > last_ts:
                insert_row(con, tf, instId, ts, o, h, l, c, v)
                print(f"[OA] {instId} {tf} synced ts={ts}")

###############################################################################
# Entrypoint
###############################################################################
def main():
    t0 = time.time()

    universe = load_universe()
    con = conn_oa()

    for instId in universe:
        try:
            sync_inst(con, instId)
        except Exception as e:
            print(f"[OA] ERROR {instId}: {e}")

    con.commit()
    con.close()

    print(f"[OA] Sync OK in {time.time() - t0:.3f}s")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/OB_collect.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3
import ccxt
import logging
import pandas as pd
import numpy as np

ROOT = "/opt/scalp/project"
DB_OB = f"{ROOT}/data/ob.db"
DB_U  = f"{ROOT}/data/u.db"
DB_B  = f"{ROOT}/data/b.db"
LOG   = f"{ROOT}/logs/ob_collect.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s OB_COLLECT %(levelname)s %(message)s"
)
log = logging.getLogger("OB_COLLECT")

# ==========================================================
# DB
# ==========================================================
def conn(path):
    c = sqlite3.connect(path, timeout=10, isolation_level=None)
    c.execute("PRAGMA busy_timeout=5000;")
    return c

def load_universe():
    cu = conn(DB_U)
    xs = [r[0] for r in cu.execute("SELECT instId FROM universe;")]
    cu.close()
    return xs

def last_ts(conn, table, inst):
    try:
        r = conn.execute(
            f"SELECT ts FROM {table} WHERE instId=? ORDER BY ts DESC LIMIT 1",
            (inst,)
        ).fetchone()
        return r[0] if r else 0
    except:
        return 0

# ==========================================================
# FETCH CCXT
# ==========================================================
EX = ccxt.bitget()

def fetch_tf(inst, tf, limit=200):
    try:
        symbol = inst.replace("/", "")
        rows = EX.fetch_ohlcv(symbol, timeframe=tf, limit=limit)
        return [(ts, o, h, l, c, v) for ts,o,h,l,c,v in rows]
    except Exception as e:
        log.error(f"{inst} {tf} FAIL {e}")
        return []

# ==========================================================
# AGGREGATION 3m
# ==========================================================
def aggregate_3m(conn):
    conn.execute("""
    INSERT OR REPLACE INTO ohlcv_3m(instId, ts, o, h, l, c, v)
    SELECT instId,
           (ts/180000)*180000 AS ts,
           FIRST_VALUE(o) OVER w,
           MAX(h)         OVER w,
           MIN(l)         OVER w,
           LAST_VALUE(c)  OVER w,
           SUM(v)         OVER w
    FROM ohlcv_1m
    WINDOW w AS (
        PARTITION BY instId, (ts/180000)
        ORDER BY ts
        ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING
    );
    """)

# ==========================================================
# FEAT FUNCTIONS
# ==========================================================
def ema(s, span): return s.ewm(span=span, adjust=False).mean()

def rsi(s, period=14):
    d = s.diff()
    up = d.clip(lower=0).ewm(alpha=1/period).mean()
    dn = -d.clip(upper=0).ewm(alpha=1/period).mean()
    rs = up / dn.replace({0: np.nan})
    return 100 - (100/(1+rs))

def atr(df, period=14):
    hl = (df.h - df.l).abs()
    hc = (df.h - df.c.shift()).abs()
    lc = (df.l - df.c.shift()).abs()
    tr = pd.concat([hl, hc, lc], axis=1).max(axis=1)
    return tr.ewm(alpha=1/period, adjust=False).mean()

def macd_full(c, f=12, sl=26, sig=9):
    e1 = ema(c,f)
    e2 = ema(c,sl)
    macd = e1 - e2
    signal = macd.ewm(span=sig, adjust=False).mean()
    hist = macd - signal
    return macd, signal, hist

def bollinger(s, period=20, k=2):
    mid = s.rolling(period).mean()
    std = s.rolling(period).std()
    up = mid + k*std
    low = mid - k*std
    width = up - low
    return mid, std, up, low, width

def _f(x):
    try:
        if pd.isna(x):
            return None
    except:
        pass
    return float(x) if x is not None else None

# ==========================================================
# FEAT BUILDER
# ==========================================================
def build_feat(inst, tf, co, cb):
    t_ob   = f"ohlcv_{tf}"
    t_feat = f"feat_{tf}"

    df = pd.read_sql_query(
        f"SELECT ts,o,h,l,c,v FROM {t_ob} WHERE instId=? ORDER BY ts",
        co, params=(inst,)
    )
    if len(df) < 20:
        return 0

    c = df.c

    df["ema9"]  = ema(c,9)
    df["ema12"] = ema(c,12)
    df["ema21"] = ema(c,21)
    df["ema26"] = ema(c,26)
    df["ema50"] = ema(c,50)

    df["macd"], df["macdsignal"], df["macdhist"] = macd_full(c)
    df["rsi"] = rsi(c)
    df["atr"] = atr(df)

    mid,std,up,low,width = bollinger(c)
    df["bb_mid"]   = mid
    df["bb_std"]   = std
    df["bb_up"]    = up
    df["bb_low"]   = low
    df["bb_width"] = width

    df["mom"]   = c.diff(10)
    df["roc"]   = (c / c.shift(10) - 1) * 100
    df["slope"] = (df["ema12"] - df["ema12"].shift(10)) / 10
    df["ctx"]   = None

    rows = []
    for r in df.itertuples(index=False):
        rows.append((
            inst, int(r.ts),
            _f(r.o), _f(r.h), _f(r.l), _f(r.c), _f(r.v),
            _f(r.ema9), _f(r.ema12), _f(r.ema21), _f(r.ema26), _f(r.ema50),
            _f(r.macd), _f(r.macdsignal), _f(r.macdhist),
            _f(r.rsi), _f(r.atr),
            _f(r.bb_mid), _f(r.bb_std), _f(r.bb_up), _f(r.bb_low), _f(r.bb_width),
            _f(r.mom), _f(r.roc), _f(r.slope),
            None
        ))

    cb.executemany(f"""
    INSERT OR REPLACE INTO {t_feat} (
        instId,ts,
        o,h,l,c,v,
        ema9,ema12,ema21,ema26,ema50,
        macd,macdsignal,macdhist,
        rsi,atr,
        bb_mid,bb_std,bb_up,bb_low,bb_width,
        mom,roc,slope,
        ctx
    ) VALUES ({','.join(['?']*26)})
    """, rows)

    return len(rows)

# ==========================================================
# MAIN
# ==========================================================
def main():
    log.info("OB START")

    co = conn(DB_OB)
    cb = conn(DB_B)
    coins = load_universe()

    for inst in coins:

        # -----------------------------------------------------
        # 1m incremental
        # -----------------------------------------------------
        last = last_ts(co, "ohlcv_1m", inst)
        rows = fetch_tf(inst, "1m")
        new_1m = [r for r in rows if r[0] > last]

        if new_1m:
            co.executemany(
                "INSERT INTO ohlcv_1m VALUES (?,?,?,?,?,?,?)",
                [(inst,)+r for r in new_1m]
            )
        log.info(f"{inst} new1m={len(new_1m)}")

        # -----------------------------------------------------
        # 5m incremental
        # -----------------------------------------------------
        last = last_ts(co, "ohlcv_5m", inst)
        rows = fetch_tf(inst, "5m")
        new_5m = [r for r in rows if r[0] > last]

        if new_5m:
            co.executemany(
                "INSERT INTO ohlcv_5m VALUES (?,?,?,?,?,?,?)",
                [(inst,)+r for r in new_5m]
            )
        log.info(f"{inst} new5m={len(new_5m)}")

    # ---------------------------------------------------------
    # 3m aggregation
    # ---------------------------------------------------------
    try:
        aggregate_3m(co)
        log.info("3m aggregation OK")
    except Exception as e:
        log.error(f"3m aggregation FAIL {e}")

    # ---------------------------------------------------------
    # FEAT BUILDER
    # ---------------------------------------------------------
    log.info("OB → FEAT builder START")

    for inst in coins:
        for tf in ("1m","3m","5m"):
            try:
                n = build_feat(inst, tf, co, cb)
                log.info(f"{inst} feat {tf} → {n}")
            except Exception as e:
                log.error(f"{inst} feat {tf} FAIL {e}")

    log.info("feat builder OK")
    log.info("OB DONE")


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/OB_collect_active.py -----
#!/usr/bin/env python3
# coding: utf-8
"""
SCALP - OB_COLLECT (incrémental)
→ Collecte les OHLCV Bitget pour TF 1m et 5m
→ Reconstitue TF 3m à partir de TF1
→ N'insère que les chandelles manquantes
→ Stocke dans ob.db
"""

import os, sys, time, logging, sqlite3, datetime
import ccxt

DB = "/opt/scalp/project/data/ob.db"
LOG = "/opt/scalp/project/logs/ob_collect.log"
EXCHANGE = ccxt.bitget()

logging.basicConfig(filename=LOG,
                    level=logging.INFO,
                    format="%(asctime)s OB_COLLECT %(levelname)s %(message)s")

# --- INIT DB ---------------------------------------------------------------
def init_db():
    conn = sqlite3.connect(DB)
    cur = conn.cursor()
    for tf in ("1m", "3m", "5m"):
        cur.execute(f"""
        CREATE TABLE IF NOT EXISTS feat_{tf} (
            instId TEXT NOT NULL,
            ts INTEGER NOT NULL,
            open REAL, high REAL, low REAL, close REAL, vol REAL,
            PRIMARY KEY(instId, ts)
        );
        """)
    conn.commit()
    return conn

# --- FETCH OHLCV -----------------------------------------------------------
def fetch_ohlcv(inst, tf, since=None, limit=200):
    try:
        ohlcv = EXCHANGE.fetch_ohlcv(inst, tf, since=since, limit=limit)
        return [(inst, int(c[0]), c[1], c[2], c[3], c[4], c[5]) for c in ohlcv]
    except Exception as e:
        logging.error(f"{inst} {tf} fetch error: {e}")
        return []

# --- BUILD 3M --------------------------------------------------------------
def rebuild_3m(cur):
    cur.execute("DELETE FROM feat_3m;")
    cur.execute("""
        INSERT INTO feat_3m(instId, ts, open, high, low, close, vol)
        SELECT instId,
               (ts/180000)*180000 AS ts,
               FIRST_VALUE(open) OVER w AS open,
               MAX(high) OVER w AS high,
               MIN(low) OVER w AS low,
               LAST_VALUE(close) OVER w AS close,
               SUM(vol) OVER w AS vol
        FROM feat_1m
        WINDOW w AS (PARTITION BY instId, (ts/180000) ORDER BY ts ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING)
        GROUP BY instId, (ts/180000)
        ORDER BY instId, ts;
    """)

# --- MAIN LOOP -------------------------------------------------------------
def collect_all():
    conn = init_db()
    cur = conn.cursor()

    insts = [
        "BTC/USDT", "ETH/USDT", "SOL/USDT", "LTC/USDT", "BNB/USDT",
        "XRP/USDT", "DOGE/USDT", "ADA/USDT", "DOT/USDT", "LINK/USDT",
        "FIL/USDT", "ICP/USDT", "ATOM/USDT", "TRX/USDT", "AAVE/USDT",
        "BCH/USDT", "ETC/USDT", "UNI/USDT", "XLM/USDT", "XTZ/USDT"
    ]

    total_inserted = 0
    logging.info("start")

    for inst in insts:
        for tf in ("1m", "5m"):
            cur.execute(f"SELECT MAX(ts) FROM feat_{tf} WHERE instId=?", (inst,))
            ts_max = cur.fetchone()[0]
            since = ts_max + 60_000 if ts_max else None
            rows = fetch_ohlcv(inst, tf, since=since, limit=450 if not ts_max else 2)
            if not rows:
                continue
            cur.executemany(f"""
                INSERT OR IGNORE INTO feat_{tf}(instId, ts, open, high, low, close, vol)
                VALUES (?, ?, ?, ?, ?, ?, ?);
            """, rows)
            total_inserted += len(rows)

    rebuild_3m(cur)
    conn.commit()
    conn.close()
    logging.info(f"done: {total_inserted} rows inserted.")

# --- ENTRYPOINT ------------------------------------------------------------
if __name__ == "__main__":
    try:
        collect_all()
    except Exception as e:
        logging.error(f"FATAL: {e}")

----- FILE: /opt/scalp/project/scripts/S_cleanup_signals.sh -----
#!/usr/bin/env bash
set -euo pipefail
sqlite3 /opt/scalp/project/data/signals.db "
WITH ranked AS (
  SELECT rowid,
         ROW_NUMBER() OVER (PARTITION BY instId ORDER BY ts_signal DESC) AS rn
  FROM signals_for_open
)
DELETE FROM signals_for_open WHERE rn > 10;
"

----- FILE: /opt/scalp/project/scripts/S_cleanup_ticks.sh -----
#!/usr/bin/env bash
set -euo pipefail
sqlite3 /opt/scalp/project/data/t.db "
DELETE FROM ticks
WHERE ts < strftime('%s','now','-1 hour') * 1000;
"

----- FILE: /opt/scalp/project/scripts/T_ticks.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import asyncio
import json
import logging
import sqlite3
import time
import websockets

ROOT = "/opt/scalp/project"
DB_T = f"{ROOT}/data/t.db"
DB_U = f"{ROOT}/data/u.db"
LOG_FILE = f"{ROOT}/logs/ticks_daemon.log"

logging.basicConfig(
    filename=LOG_FILE,
    level=logging.INFO,
    format="%(asctime)s T_TICKS %(levelname)s %(message)s"
)
log = logging.getLogger("T_TICKS")

WS_URL = "wss://ws.bitget.com/v2/ws/public"


# ---------------------------------------------------------
# Charger la liste des paires depuis u.db (table universe)
# ---------------------------------------------------------
def load_universe():
    try:
        conn = sqlite3.connect(DB_U, timeout=3)
        rows = conn.execute("SELECT instId FROM universe").fetchall()
        conn.close()
        insts = [r[0].replace("/", "") for r in rows]   # ex: BTC/USDT → BTCUSDT
        return insts
    except Exception as e:
        log.error(f"Cannot load universe: {e}")
        return ["BTCUSDT"]


# ---------------------------------------------------------
# Enregistrement dans t.db
# ---------------------------------------------------------
def save_tick(instId, lastPr, ts):
    conn = sqlite3.connect(DB_T, timeout=2)
    conn.execute("""
    INSERT INTO ticks(instId,lastPr,ts)
    VALUES(?,?,?)
    ON CONFLICT(instId) DO UPDATE SET
        lastPr=excluded.lastPr,
        ts=excluded.ts;
    """, (instId, float(lastPr), int(ts)))
    conn.commit()
    conn.close()


# ---------------------------------------------------------
# WebSocket main
# ---------------------------------------------------------
async def run_ws():
    while True:
        insts = load_universe()
        n = len(insts)
        log.info(f"Universe loaded: {n} pairs")

        try:
            async with websockets.connect(WS_URL, ping_interval=10, ping_timeout=10) as ws:

                sub = {
                    "op": "subscribe",
                    "args": [
                        {
                            "instType": "USDT-FUTURES",
                            "channel": "ticker",
                            "instId": s
                        } for s in insts
                    ]
                }

                await ws.send(json.dumps(sub))
                log.info(f"Subscribed to {n} pairs")

                async for msg in ws:
                    try:
                        data = json.loads(msg)
                        if "data" not in data:
                            continue

                        d = data["data"][0]
                        instId = d["instId"]
                        lastPr = d["lastPr"]
                        ts = d["ts"]

                        save_tick(instId, lastPr, ts)

                    except Exception as e:
                        log.error(f"Tick parse/save error: {e}")

        except Exception as e:
            log.error(f"WS lost, reconnecting: {e}")
            await asyncio.sleep(5)


# ---------------------------------------------------------
# ENTRY POINT
# ---------------------------------------------------------
if __name__ == "__main__":
    log.info("T_TICKS starting…")
    asyncio.run(run_ws())

----- FILE: /opt/scalp/project/scripts/T_ticks_debug.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import asyncio, json, websockets, datetime, re

WS_URL = "wss://ws.bitget.com/mix/v1/stream"

# liste restreinte pour test
INSTRUMENTS = ["BTCUSDT", "ETHUSDT", "BNBUSDT"]

async def listen():
    async with websockets.connect(WS_URL, ping_interval=20) as ws:
        subs = []
        for inst in INSTRUMENTS:
            subs.append({
                "op": "subscribe",
                "args": [f"ticker:{inst}_UMCBL"]
            })
        for s in subs:
            await ws.send(json.dumps(s))
        print("Subscriptions sent:", subs)

        count = 0
        while True:
            msg = await ws.recv()
            ts = datetime.datetime.now().strftime("%H:%M:%S")
            print(f"\n[{ts}] RAW:", msg[:200])  # tronqué si long

            # test parsing brut
            try:
                clean = re.search(r'\{.*\}', msg)
                obj = json.loads(clean.group(0)) if clean else None
                if obj:
                    ticker = obj.get("data", [{}])[0]
                    inst = ticker.get("symbol", "?")
                    last = ticker.get("last", "?")
                    print(f"→ Parsed {inst} = {last}")
            except Exception as e:
                print("⚠️ Parse error:", e)

            count += 1
            if count >= 5:
                break

asyncio.run(listen())

----- FILE: /opt/scalp/project/scripts/T_ticks_test_raw.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import asyncio, json, time, websockets

WS_URL = "wss://ws.bitget.com/v2/ws/public"
INSTRUMENTS = ["BTCUSDT", "ETHUSDT", "BNBUSDT"]

async def test():
    async with websockets.connect(WS_URL, ping_interval=None) as ws:
        subs = [{"instType": "USDT-FUTURES", "channel": "ticker", "instId": i} for i in INSTRUMENTS]
        msg = {"op": "subscribe", "args": subs}
        await ws.send(json.dumps(msg))
        print("✅ Sent:", msg)

        async def pinger():
            while True:
                await asyncio.sleep(10)
                await ws.send('{"op":"ping"}')
        asyncio.create_task(pinger())

        while True:
            raw = await ws.recv()
            ts = time.strftime("%H:%M:%S")
            print(f"\n[{ts}] RAW →", raw[:250])

            try:
                data = json.loads(raw)
                if "data" in data:
                    for d in data["data"]:
                        inst = d.get("instId")
                        pr   = d.get("lastPr")
                        if inst and pr:
                            print(f"→ Parsed {inst} = {pr}")
            except Exception as e:
                print("⚠️ Parse error:", e)

asyncio.run(test())

----- FILE: /opt/scalp/project/scripts/U_universe.py -----
import ccxt, sqlite3

DBU = "/opt/scalp/project/data/u.db"
TOP = 20

exchange = ccxt.bitget()
markets = exchange.load_markets()

perps = []
for s,info in markets.items():
    if info.get('swap',False) and info['quote'] == 'USDT':
        inst = s.replace(":USDT","")  # -> BTC/USDT:USDT → BTC/USDT
        perps.append(inst)

perps = perps[:TOP]

con = sqlite3.connect(DBU, timeout=5)
con.execute("DELETE FROM universe;")
for inst in perps:
    con.execute("INSERT OR IGNORE INTO universe(instId) VALUES (?)", (inst,))
con.commit()
con.close()

print("[U] OK :", perps)

----- FILE: /opt/scalp/project/scripts/WS_candles_to_wsdb.py -----
#!/usr/bin/env python3
import asyncio
import json
import sqlite3
import websockets
import time
from datetime import datetime

WS_URL = "wss://ws.bitget.com/v2/ws/public"

DB_WS = "/opt/scalp/project/data/ws.db"
DB_U  = "/opt/scalp/project/data/u.db"

TF = {
    "candle5m":  5  * 60 * 1000,
    "candle15m": 15 * 60 * 1000,
    "candle30m": 30 * 60 * 1000
}

###############################################################################
# DB Helpers
###############################################################################
def load_universe():
    conn = sqlite3.connect(DB_U)
    rows = conn.execute("SELECT instId FROM universe ORDER BY instId;").fetchall()
    conn.close()
    return [r[0] for r in rows]

def wsdb_conn():
    conn = sqlite3.connect(DB_WS, timeout=10, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    return conn

###############################################################################
# WS Subscription builder
###############################################################################
def build_sub(universe):
    args = []
    for instId in universe:
        args.append({"instType":"USDT-FUTURES","channel":"candle5m","instId":instId})
        args.append({"instType":"USDT-FUTURES","channel":"candle15m","instId":instId})
        args.append({"instType":"USDT-FUTURES","channel":"candle30m","instId":instId})
    return {"op":"subscribe", "args":args}

###############################################################################
# Insert into WS DB (closed candles only)
###############################################################################
def insert_ws_candle(conn, table, instId, ts, o, h, l, c, v):
    conn.execute(
        f"""
        INSERT OR REPLACE INTO {table}(instId, ts, open, high, low, close, volume)
        VALUES (?,?,?,?,?,?,?)
        """,
        (instId, ts, o, h, l, c, v)
    )

###############################################################################
# Main WebSocket loop
###############################################################################
async def run_ws():
    universe = load_universe()
    sub = build_sub(universe)

    ws_conn = wsdb_conn()

    while True:
        try:
            print("[WS] Connecting...")
            async with websockets.connect(
                WS_URL,
                ping_interval=None,
                ping_timeout=None,
                close_timeout=1
            ) as ws:

                print("[WS] Connected, subscribing…")
                await ws.send(json.dumps(sub))

                async def keep_alive():
                    while True:
                        await asyncio.sleep(15)
                        try:
                            await ws.send(json.dumps({"event":"ping"}))
                        except:
                            break

                asyncio.create_task(keep_alive())

                while True:
                    raw = await ws.recv()
                    data = json.loads(raw)

                    # Skip ping/pong
                    if isinstance(data, dict) and data.get("event") in ("ping","pong"):
                        continue

                    if "arg" not in data or "data" not in data:
                        continue

                    channel = data["arg"]["channel"]
                    instId  = data["arg"]["instId"]
                    rows    = data["data"]

                    if channel not in TF:
                        continue

                    tf_ms = TF[channel]
                    table = f"ws_ohlcv_{channel.replace('candle','')}"  # candle5m -> ws_ohlcv_5m

                    for k in rows:
                        ts = int(k[0])

                        # Only take closed candles
                        if ts % tf_ms != 0:
                            continue

                        o = float(k[1])
                        h = float(k[2])
                        l = float(k[3])
                        c = float(k[4])
                        v = float(k[5])

                        insert_ws_candle(ws_conn, table, instId, ts, o, h, l, c, v)

                        print(
                            f"[WS] CLOSED {channel} {instId} "
                            f"ts={ts} o={o} h={h} l={l} c={c} v={v}"
                        )

        except Exception as e:
            print(f"[WS] Error: {e}, retry in 5s…")
            await asyncio.sleep(5)

###############################################################################
# Entrypoint
###############################################################################
if __name__ == "__main__":
    asyncio.run(run_ws())

----- FILE: /opt/scalp/project/scripts/WS_multi_candles.py -----
#!/usr/bin/env python3
import asyncio
import json
import websockets
import sqlite3
import time

WS_URL = "wss://ws.bitget.com/v2/ws/public"
DB_U = "/opt/scalp/project/data/u.db"

# Timeframes in ms for identifying CLOSED candles
TF = {
    "candle5m":  5 * 60 * 1000,
    "candle15m": 15 * 60 * 1000,
    "candle30m": 30 * 60 * 1000
}

################################################################
# Load universe from u.db
################################################################
def load_universe():
    conn = sqlite3.connect(DB_U)
    rows = conn.execute("SELECT instId FROM universe ORDER BY instId;").fetchall()
    conn.close()
    return [r[0] for r in rows]


################################################################
# Build subscription message
################################################################
def build_sub(universe):
    args = []
    for inst in universe:
        args.append({"instType":"USDT-FUTURES","channel":"candle5m","instId":inst})
        args.append({"instType":"USDT-FUTURES","channel":"candle15m","instId":inst})
        args.append({"instType":"USDT-FUTURES","channel":"candle30m","instId":inst})
    return {"op":"subscribe","args":args}


################################################################
# Core WS loop
################################################################
async def ws_loop():
    universe = load_universe()
    sub = build_sub(universe)

    while True:
        try:
            print("[WS] Connecting...")
            async with websockets.connect(WS_URL, ping_interval=20) as ws:
                print("[WS] Connected, subscribing…")
                await ws.send(json.dumps(sub))

                while True:
                    raw = await ws.recv()
                    data = json.loads(raw)

                    # Ignore heartbeats
                    if isinstance(data, dict) and data.get("event") in ("ping","pong"):
                        continue

                    # Only candle messages
                    if "arg" not in data or "data" not in data:
                        continue

                    channel = data["arg"].get("channel")
                    inst    = data["arg"].get("instId")
                    rows    = data["data"]

                    if channel not in TF:
                        continue

                    tf_ms = TF[channel]

                    # Process each kline
                    for k in rows:
                        ts     = int(k[0])
                        open_  = float(k[1])
                        high   = float(k[2])
                        low    = float(k[3])
                        close  = float(k[4])
                        volume = float(k[5])

                        # === FILTER: closed candles only ===
                        if ts % tf_ms != 0:
                            continue

                        print(f"[{channel}] CLOSED {inst}  ts={ts}  "
                              f"open={open_} high={high} low={low} "
                              f"close={close} vol={volume}")

        except Exception as e:
            print(f"[WS] Error: {e}, retry in 5s…")
            await asyncio.sleep(5)


################################################################
# Entry point
################################################################
if __name__ == "__main__":
    asyncio.run(ws_loop())

----- FILE: /opt/scalp/project/scripts/b_dash.py -----
#!/usr/bin/env python3
import sqlite3, time
from datetime import datetime

DB_A = "/opt/scalp/project/data/a.db"
DB_B = "/opt/scalp/project/data/b.db"
DB_T = "/opt/scalp/project/data/t.db"

def get_ctx():
    with sqlite3.connect(DB_A) as con:
        cur = con.cursor()
        cur.execute("SELECT instId, ctx FROM v_ctx_latest WHERE ctx IN ('bullish','bearish','range');")
        return cur.fetchall()

def get_last_tick(inst):
    with sqlite3.connect(DB_T) as con:
        cur = con.cursor()
        cur.execute("SELECT lastPr, ts_ms FROM v_ticks_latest WHERE instId=? LIMIT 1;", (inst,))
        r = cur.fetchone()
    if not r:
        return None, None, None, None
    pr, ts_ms = r
    ts_s = ts_ms / 1000
    hms = datetime.fromtimestamp(ts_s).strftime("%H:%M:%S")
    delta = round(time.time() - ts_s, 1)
    return pr, ts_s, hms, delta

def get_last_signal(inst):
    with sqlite3.connect(DB_B) as con:
        cur = con.cursor()
        cur.execute("SELECT ts FROM signals WHERE instId=? ORDER BY ts DESC LIMIT 1;", (inst,))
        r = cur.fetchone()
    if not r:
        return None, None, None
    ts = float(r[0])
    hms = datetime.fromtimestamp(ts).strftime("%H:%M:%S")
    delta = round(time.time() - ts, 1)
    return ts, hms, delta

def dash():
    print(f"===== DASH {datetime.now().strftime('%H:%M:%S')} =====")
    for inst, ctx in get_ctx():
        pr, ts_tick, hms_tick, delta_tick = get_last_tick(inst)
        ts_sig, hms_sig, delta_sig = get_last_signal(inst)

        delta = "-"
        if ts_tick and ts_sig:
            delta = round(ts_tick - ts_sig, 1)

        flag = "⚠️" if delta_tick and delta_tick > 5 else "✅"
        print(f"{inst:<10} | ctx={ctx:<8} | price={pr or '-':<10} | "
              f"tick={hms_tick or '-':>8} | sig={hms_sig or '-':>8} | "
              f"Δ={delta:<6} | {flag}")
    print("=" * 90)

if __name__ == "__main__":
    while True:
        try:
            dash()
            time.sleep(3)
        except KeyboardInterrupt:
            break
        except Exception as e:
            print("B_DASH ERROR:", e)
            time.sleep(5)

----- FILE: /opt/scalp/project/scripts/budget.py -----
#!/usr/bin/env python3
import sqlite3, time

DB = "/opt/scalp/project/data/budget.db"

def get_conn():
    c = sqlite3.connect(DB)
    c.execute("PRAGMA journal_mode=WAL;")
    c.row_factory = sqlite3.Row
    return c

def bus_notify(instId, event):
    conn = get_conn()
    cur = conn.cursor()
    cur.execute("INSERT INTO bus_out (instId, event) VALUES (?,?)", (instId, event))
    conn.commit()
    conn.close()
    print(f"[BUDGET] Event logged: {instId} → {event}")

def main():
    while True:
        print("[BUDGET] Updating exposure / allocation")
        bus_notify("ALL", "BUDGET_UPDATED")
        time.sleep(20)

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/budget_updater.py -----
#!/usr/bin/env python3
import sqlite3, time, logging, sys
from pathlib import Path

DB_BUDGET  = Path("/opt/scalp/project/data/budget.db")
DB_OPENER  = Path("/opt/scalp/project/data/opener.db")
DB_CLOSER  = Path("/opt/scalp/project/data/closer.db")
DB_REC     = Path("/opt/scalp/project/data/recorder.db")
LOG        = "/opt/scalp/project/logs/budget.log"

# -----------------------------------------------------
def setup_logging():
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s BUDGET %(levelname)s %(message)s",
        handlers=[logging.FileHandler(LOG), logging.StreamHandler(sys.stdout)],
        force=True,
    )

# -----------------------------------------------------
def connect(path):
    c = sqlite3.connect(path, timeout=30, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA busy_timeout=5000;")
    return c

# -----------------------------------------------------
def get_margin_engaged(con_o):
    """Somme de la marge estimée sur les positions ouvertes/pending."""
    try:
        res = con_o.execute("""
            SELECT SUM(entry * qty / leverage)
            FROM trades_open_init
            WHERE status IN ('pending','open');
        """).fetchone()[0]
        return round(res or 0.0, 4)
    except Exception:
        return 0.0

def get_pnl_real(con_r):
    """Somme du PnL réalisé total depuis l’historique."""
    try:
        res = con_r.execute("SELECT SUM(pnl_net) FROM trades_recorded;").fetchone()[0]
        if res is None:
            res = con_r.execute("SELECT SUM(pnl) FROM trades_recorded;").fetchone()[0]
        return round(res or 0.0, 4)
    except Exception:
        return 0.0

def get_capital_initial(con_b):
    """Capital initial du budget (si non encore présent, crée 1000 USDT)."""
    con_b.execute("""
        CREATE TABLE IF NOT EXISTS budget_state(
            id INTEGER PRIMARY KEY CHECK (id=1),
            balance REAL DEFAULT 1000.0,
            margin REAL DEFAULT 0.0,
            pnl_real REAL DEFAULT 0.0,
            ts_update INTEGER
        );
    """)
    con_b.commit()
    row = con_b.execute("SELECT balance FROM budget_state WHERE id=1;").fetchone()
    if not row:
        con_b.execute("INSERT OR REPLACE INTO budget_state(id,balance,margin,pnl_real,ts_update) VALUES (1,1000.0,0.0,0.0,strftime('%s','now'));")
        con_b.commit()
        return 1000.0
    return float(row[0])

# -----------------------------------------------------
def update_budget():
    con_b = connect(DB_BUDGET)
    con_o = connect(DB_OPENER)
    con_r = connect(DB_REC)

    capital = get_capital_initial(con_b)
    margin  = get_margin_engaged(con_o)
    pnl_real = get_pnl_real(con_r)

    new_balance = capital + pnl_real
    ts_now = int(time.time())

    con_b.execute("""
        UPDATE budget_state
        SET balance=?, margin=?, pnl_real=?, ts_update=?
        WHERE id=1;
    """, (new_balance, margin, pnl_real, ts_now))
    con_b.commit()

    con_b.execute("""
        CREATE VIEW IF NOT EXISTS v_budget_overview AS
        SELECT
            ROUND(balance,4) AS balance,
            ROUND(margin,4)  AS margin,
            ROUND(pnl_real,4) AS pnl_real,
            datetime(ts_update,'unixepoch','localtime') AS ts_local
        FROM budget_state;
    """)
    con_b.commit()

    con_b.close(); con_o.close(); con_r.close()
    logging.info(f"MAJ budget → balance={new_balance:.2f} margin={margin:.2f} pnl_real={pnl_real:.2f}")

# -----------------------------------------------------
def main(_):
    setup_logging()
    logging.info("Budget updater started ✓")
    try:
        update_budget()
    except Exception as e:
        logging.exception(f"Erreur mise à jour budget: {e}")
        raise SystemExit(1)

if __name__ == "__main__":
    raise SystemExit(main(sys.argv[1:]))

----- FILE: /opt/scalp/project/scripts/closer.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3, time, logging

ROOT = "/opt/scalp/project"
DB_GEST = f"{ROOT}/data/gest.db"
DB_CLOSE = f"{ROOT}/data/closer.db"
LOG = f"{ROOT}/logs/closer.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s CLOSER %(levelname)s %(message)s"
)
log = logging.getLogger("CLOSER")


def conn(path):
    c = sqlite3.connect(path, timeout=5, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA synchronous=NORMAL;")
    c.execute("PRAGMA busy_timeout=3000;")
    return c


# -----------------------------------------------------------
# READ FROM gest (trade to_close)
# -----------------------------------------------------------
def get_to_close():
    g = conn(DB_GEST)
    q = """
    SELECT
        uid,
        instId,
        side,
        entry,
        qty,
        lev,
        margin,
        sl,
        tp,
        atr_signal,
        price_signal,
        score_B,
        ctx,
        ts_open,
        ts_follow,
        ts_signal
    FROM v_for_closer
    ORDER BY ts_follow DESC;
    """
    rows = g.execute(q).fetchall()
    g.close()
    return rows


# -----------------------------------------------------------
# Compute close: pnl, price, reason
# -----------------------------------------------------------
def compute_close(side, entry, qty, margin, price):
    if side == "buy":
        pnl = (price - entry) * qty
    else:
        pnl = (entry - price) * qty

    pnl_pct = (pnl / margin) * 100 if margin != 0 else 0
    return pnl, pnl_pct


# -----------------------------------------------------------
# Write into closer.db
# -----------------------------------------------------------
def write_close(uid, instId, side,
                entry, qty, lev, margin,
                sl, tp,
                price_close, pnl_usdt, pnl_pct,
                reason):

    c = conn(DB_CLOSE)
    ts_close = int(time.time()*1000)

    c.execute("""
    INSERT OR REPLACE INTO trades_closer (
        uid, instId, side,
        entry, qty, lev, margin,
        sl, tp,
        price_close, ts_close,
        pnl_usdt, pnl_pct,
        reason_close
    )
    VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?);
    """, (
        uid, instId, side,
        entry, qty, lev, margin,
        sl, tp,
        price_close, ts_close,
        pnl_usdt, pnl_pct,
        reason
    ))

    log.info(f"CLOSE {uid} {instId} price={price_close} pnl={pnl_usdt}usd {pnl_pct}% reason={reason}")

    c.close()


# -----------------------------------------------------------
# MAIN LOOP
# -----------------------------------------------------------
def main():
    log.info("CLOSER start")

    while True:
        try:
            rows = get_to_close()

            for r in rows:
                (uid, instId, side,
                 entry, qty, lev, margin,
                 sl, tp,
                 atr_signal, price_signal,
                 scoreB, ctx,
                 ts_open, ts_follow, ts_signal) = r

                price_close = price_signal
                pnl_usdt, pnl_pct = compute_close(side, entry, qty, margin, price_close)

                reason = "sl_hit" if (side == "buy" and price_close <= sl) or \
                                   (side == "sell" and price_close >= sl) \
                         else "tp_hit"

                write_close(uid, instId, side,
                            entry, qty, lev, margin,
                            sl, tp,
                            price_close, pnl_usdt, pnl_pct,
                            reason)

        except Exception as e:
            log.error(f"close loop error: {e}")

        time.sleep(0.4)


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/closer_finalize.py -----
import sqlite3, time, logging

DB_FOLLOW = "/opt/scalp/project/data/follower.db"
DB_CLOSER = "/opt/scalp/project/data/closer.db"
DB_REC = "/opt/scalp/project/data/recorder.db"
DB_BUDGET = "/opt/scalp/project/data/budget.db"
DB_TICKS = "/opt/scalp/project/data/t.db"

logging.basicConfig(level=logging.INFO, format="%(asctime)s CLOSER FIX %(levelname)s %(message)s")

def get_last_price(instId):
    """Récupère le dernier prix connu depuis t.db"""
    try:
        with sqlite3.connect(DB_TICKS) as ct:
            cur = ct.cursor()
            cur.execute("SELECT lastPr FROM v_ticks_latest WHERE instId=? LIMIT 1;", (instId,))
            row = cur.fetchone()
            return float(row[0]) if row else None
    except Exception as e:
        logging.warning(f"{instId}: erreur lecture ticks → {e}")
        return None

def finalize_closures():
    con_f = sqlite3.connect(DB_FOLLOW)
    con_c = sqlite3.connect(DB_CLOSER)
    con_r = sqlite3.connect(DB_REC)
    con_b = sqlite3.connect(DB_BUDGET)

    cur_f = con_f.cursor()
    cur_c = con_c.cursor()
    cur_r = con_r.cursor()
    cur_b = con_b.cursor()

    rows = cur_f.execute("""
        SELECT instId, side, entry, last, qty, reason_close,
               score_U, score_A, score_B, ts_open, ts_update
        FROM trades_follow
        WHERE status='to_close'
    """).fetchall()

    if not rows:
        logging.info("Aucune position à finaliser.")
        return

    for instId, side, entry, last, qty, reason, sU, sA, sB, ts_open, ts_close in rows:
        if last is None:
            last = get_last_price(instId)
            if last is None:
                last = entry
                logging.warning(f"{instId}: aucun prix disponible → fallback entry={entry}")
            else:
                logging.info(f"{instId}: récupéré last depuis ticks = {last}")

        pnl = round((last - entry) * qty * (1 if side == 'buy' else -1), 6)
        logging.info(f"→ CLOSE {instId} {side} pnl={pnl} reason={reason}")

        cur_c.execute("""
            INSERT INTO positions_closed(instId,side,entry,exit,qty,pnl,sl,tp,ts_open,ts_close,reason_exit,score_U,score_A,score_B)
            VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)
        """, (instId,side,entry,last,qty,pnl,0,0,ts_open,ts_close,reason,sU,sA,sB))

        cur_r.execute("""
            INSERT INTO trades_recorded(instId,side,entry,exit,pnl,reason_exit,ts_open,ts_close,score_U,score_A,score_B)
            VALUES (?,?,?,?,?,?,?,?,?,?,?)
        """, (instId,side,entry,last,pnl,reason,ts_open,ts_close,sU,sA,sB))

        cur_b.execute("""
            INSERT INTO ledger(src,instId,type,amount,ts)
            VALUES ('closer',?, 'pnl_real', ?, strftime('%s','now')*1000)
        """, (instId, pnl))

        cur_f.execute("UPDATE trades_follow SET status='closed' WHERE instId=?;", (instId,))

    con_c.commit()
    con_r.commit()
    con_b.commit()
    con_f.commit()
    logging.info(f"✓ {len(rows)} clôture(s) finalisée(s).")

if __name__ == "__main__":
    finalize_closures()

----- FILE: /opt/scalp/project/scripts/follower.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3, time, logging, math

ROOT = "/opt/scalp/project"
DB_GEST = f"{ROOT}/data/gest.db"
DB_FOLLOW = f"{ROOT}/data/follower.db"
LOG = f"{ROOT}/logs/follower.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s FOLLOWER %(levelname)s %(message)s"
)
log = logging.getLogger("FOLLOWER")


def conn(path):
    c = sqlite3.connect(path, timeout=5, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA synchronous=NORMAL;")
    c.execute("PRAGMA busy_timeout=3000;")
    return c


# -----------------------------------------------------------
# READ FROM GEST
# -----------------------------------------------------------
def get_opened_follow():
    g = conn(DB_GEST)
    q = """
    SELECT
        uid, instId, side,
        entry, qty, lev, margin,
        sl, tp,
        ts_open,
        atr_signal, price_signal,
        score_B, ctx,
        status
    FROM v_for_follower
    ORDER BY ts_open DESC;
    """
    rows = g.execute(q).fetchall()
    g.close()
    return rows


# -----------------------------------------------------------
# CLEANUP: remove trades not in GEST anymore (status != opened/followed)
# -----------------------------------------------------------
def cleanup_removed(active_uids):
    f = conn(DB_FOLLOW)
    q = "SELECT uid FROM trades_follower;"
    existing = [row[0] for row in f.execute(q).fetchall()]

    for uid in existing:
        if uid not in active_uids:
            f.execute("DELETE FROM trades_follower WHERE uid=?;", (uid,))
            log.info(f"CLEAN REMOVE {uid}")

    f.close()


# -----------------------------------------------------------
# FOLLOWER LOGIC (ATR, BE, TRAILING, PYRAMIDE)
# + DETECT TO_CLOSE (SL/TP touché)
# -----------------------------------------------------------
def compute_follow(side, entry, sl, tp, atr, price):

    # 1) SL/TP touché → fermeture immédiate
    if (side == "buy" and price <= sl) or (side == "sell" and price >= sl):
        return sl, tp, False, False, True  # to_close=True

    if (side == "buy" and price >= tp) or (side == "sell" and price <= tp):
        return sl, tp, False, False, True

    # 2) Sinon mise à jour
    be_trigger = False
    pyr = False

    # ----- Breakeven -----
    be_price = entry + atr if side == "buy" else entry - atr
    if (side == "buy" and price >= be_price) or (side == "sell" and price <= be_price):
        sl = entry
        be_trigger = True

    # ----- Trailing SL -----
    trail = 2.5 * atr
    if side == "buy":
        sl = max(sl, price - trail)
    else:
        sl = min(sl, price + trail)

    # ----- Dynamic TP -----
    tp = entry + 6 * atr if side == "buy" else entry - 6 * atr

    # ----- Pyramide -----
    pyr_price = entry + 3 * atr if side == "buy" else entry - 3 * atr
    if (side == "buy" and price > pyr_price) or (side == "sell" and price < pyr_price):
        pyr = True

    return sl, tp, be_trigger, pyr, False  # to_close=False


# -----------------------------------------------------------
# WRITE FOLLOWER.DB (20 colonnes)
# -----------------------------------------------------------
def write_follow(uid, instId, side,
                 entry, qty, lev, margin,
                 sl, tp, atr, price_signal,
                 scoreB, ctx, ts_open,
                 new_sl, new_tp,
                 be, pyr, to_close):

    f = conn(DB_FOLLOW)
    ts_follow = int(time.time() * 1000)

    status = "to_close" if to_close else "followed"

    f.execute("""
    INSERT OR REPLACE INTO trades_follower (
        uid, instId, side,
        entry, qty, lev, margin,
        sl, tp,
        atr, price_open, score_B, ctx,
        ts_open, ts_follow,
        sl_updated, tp_updated, be_triggered, pyramided,
        status
    )
    VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?);
    """, (
        uid, instId, side,
        entry, qty, lev, margin,
        new_sl, new_tp,
        atr, price_signal, scoreB, ctx,
        ts_open, ts_follow,
        int(new_sl != sl),
        int(new_tp != tp),
        int(be),
        int(pyr),
        status
    ))

    log.info(f"FOLLOW {uid} {instId} SL={new_sl} TP={new_tp} BE={be} PYR={pyr} → {status}")
    f.close()


# -----------------------------------------------------------
# MAIN LOOP
# -----------------------------------------------------------
def main():
    log.info("FOLLOWER start")

    while True:
        try:
            rows = get_opened_follow()

            # UIDs actifs côté GEST
            active_uids = [r[0] for r in rows]

            # Nettoyage des trades à supprimer
            cleanup_removed(active_uids)

            # Traitement des trades actifs
            for r in rows:
                (uid, instId, side,
                 entry, qty, lev, margin,
                 sl, tp,
                 ts_open,
                 atr, price_signal,
                 scoreB, ctx,
                 status_gest) = r

                new_sl, new_tp, be, pyr, to_close = compute_follow(
                    side, entry, sl, tp, atr, price_signal
                )

                write_follow(
                    uid, instId, side,
                    entry, qty, lev, margin,
                    sl, tp, atr, price_signal,
                    scoreB, ctx, ts_open,
                    new_sl, new_tp,
                    be, pyr, to_close
                )

        except Exception as e:
            log.error(f"follow loop error: {e}")

        time.sleep(0.4)


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/gen_schema_ref.sh -----
#!/usr/bin/env bash
set -euo pipefail

# -------- CONFIG --------
DATA_DIR="/opt/scalp/project/data"
OUT="${DATA_DIR}/schema_ref.sql"

# -------- INIT --------
{
  echo "-- === SCHEMA GLOBAL SCALP ==="
  echo "-- Généré le $(date '+%Y-%m-%d %H:%M:%S')"
  echo
} > "$OUT"

# -------- EXTRACTION COMPLÈTE --------
for DB in oa.db a.db ob.db b.db budget.db gest.db h.db t.db u.db  triggers.db signals.db opener.db follower.db closer.db recorder.db; do
  DB_PATH="${DATA_DIR}/${DB}"
  echo "-- --- $DB ---" >> "$OUT"
  if [[ -f "$DB_PATH" ]]; then
    # Sortie complète des tables et vues
    sqlite3 "$DB_PATH" "SELECT sql FROM sqlite_master WHERE type IN ('table','view') AND sql NOT NULL ORDER BY name;" \
      >> "$OUT" 2>/dev/null || true
    echo >> "$OUT"
  else
    echo "-- (absent) $DB" >> "$OUT"
    echo >> "$OUT"
  fi
done

echo "-- === FIN ===" >> "$OUT"
echo "✅ Schéma complet sauvegardé dans $OUT"

----- FILE: /opt/scalp/project/scripts/gest.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3
import time
import logging

ROOT = "/opt/scalp/project"

DB_G   = f"{ROOT}/data/gest.db"
DB_SIG = f"{ROOT}/data/signals.db"
DB_O   = f"{ROOT}/data/opener.db"
DB_F   = f"{ROOT}/data/follower.db"
DB_C   = f"{ROOT}/data/closer.db"
DB_R   = f"{ROOT}/data/recorder.db"

LOG    = f"{ROOT}/logs/gest.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s GEST %(levelname)s %(message)s"
)
log = logging.getLogger("GEST")


def conn(path):
    c = sqlite3.connect(path, timeout=3, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA busy_timeout=3000;")
    return c


# ======================================================================
# A — INGEST SIGNALS → gest.trades (status='new')
# ======================================================================
def ingest_signals(cg):
    c = conn(DB_SIG)

    rows = c.execute("""
        SELECT
            uid,           -- 0
            instId,        -- 1
            side,          -- 2
            reason,        -- 3
            ctx,           -- 4
            score_A,       -- 5
            score_B,       -- 6
            price_signal,  -- 7
            atr_signal,    -- 8
            ts_signal,     -- 9
            ts_local,      -- 10
            hms            -- 11
        FROM v_for_gest
        ORDER BY ts_signal DESC
        LIMIT 100
    """).fetchall()

    for r in rows:
        uid = r[0]
        exists = cg.execute("SELECT 1 FROM trades WHERE uid=?", (uid,)).fetchone()
        if exists:
            continue

        cg.execute("""
            INSERT INTO trades (
                uid, instId, side, reason, ctx,
                score_A, score_B,
                price_signal, atr_signal,
                ts_signal, ts_local, hms,
                status, ts_status_update
            )
            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'new', strftime('%s','now')*1000)
        """, (
            r[0], r[1], r[2], r[3], r[4],
            r[5], r[6],
            r[7], r[8],
            r[9], r[10], r[11]
        ))

        log.info(f"INGEST SIGNAL {uid}")

    c.close()


# ======================================================================
# B — INGEST OPENER → OPENED
# ======================================================================
def ingest_opener(cg):
    c = conn(DB_O)

    rows = c.execute("""
        SELECT
            uid,        -- 0
            instId,     -- 1
            side,       -- 2
            entry,      -- 3
            qty,        -- 4
            lev,        -- 5
            margin,     -- 6
            sl,         -- 7
            tp,         -- 8
            ts_open     -- 9
        FROM v_opener_for_gest
        ORDER BY ts_open DESC
        LIMIT 200
    """).fetchall()

    for r in rows:
        uid = r[0]

        cg.execute("""
            UPDATE trades SET
                entry=?, qty=?, lev=?, margin=?,
                sl=?, tp=?, ts_open=?,
                status='opened',
                ts_status_update=strftime('%s','now')*1000
            WHERE uid=? AND status='new'
        """, (
            r[3], r[4], r[5], r[6],
            r[7], r[8], r[9],
            uid
        ))

    c.close()


# ======================================================================
# C — INGEST FOLLOWER → FOLLOWED
# ======================================================================
def ingest_follower(cg):
    c = conn(DB_F)

    rows = c.execute("""
        SELECT
            uid,        -- 0
            instId,     -- 1
            side,       -- 2
            entry,      -- 3
            qty,        -- 4
            lev,        -- 5
            margin,     -- 6
            sl,         -- 7
            tp,         -- 8
            ts_open,    -- 9
            atr_signal, -- 10
            price_signal,-- 11
            score_B,    -- 12
            ctx,        -- 13
            status,     -- 14
            ts_follow   -- 15 (from trades_follower)
        FROM v_follower_for_gest
        ORDER BY ts_open DESC
        LIMIT 200
    """).fetchall()

    for r in rows:
        uid = r[0]

        # follower updates only if status is opened
        cg.execute("""
            UPDATE trades SET
                sl=?, tp=?, ts_follow=?,
                status='followed',
                ts_status_update=strftime('%s','now')*1000
            WHERE uid=? AND status='opened'
        """, (
            r[7],  # sl
            r[8],  # tp
            r[15], # ts_follow
            uid
        ))

    c.close()


# ======================================================================
# D — INGEST CLOSER → CLOSED
# ======================================================================
def ingest_closer(cg):
    c = conn(DB_C)

    rows = c.execute("""
        SELECT
            uid,          -- 0
            instId,       -- 1
            side,         -- 2
            entry,        -- 3
            qty,          -- 4
            lev,          -- 5
            margin,       -- 6
            sl,           -- 7
            tp,           -- 8
            atr_signal,   -- 9
            price_signal, -- 10
            score_B,      -- 11
            ctx,          -- 12
            ts_open,      -- 13
            ts_follow,    -- 14
            ts_signal,    -- 15
            price_close,  -- 16
            pnl_usdt,     -- 17
            pnl_pct,      -- 18
            ts_close,     -- 19
            reason_close  -- 20
        FROM v_closer_for_gest
        ORDER BY ts_close DESC
        LIMIT 200
    """).fetchall()

    for r in rows:
        uid = r[0]

        cg.execute("""
            UPDATE trades SET
                price_close=?, pnl_usdt=?, pnl_pct=?, ts_close=?, reason_close=?,
                status='closed',
                ts_status_update=strftime('%s','now')*1000
            WHERE uid=? AND status='to_close'
        """, (
            r[16], r[17], r[18], r[19], r[20],
            uid
        ))

    c.close()


# ======================================================================
# E — INGEST RECORDER → RECORDED
# ======================================================================
def ingest_recorder(cg):
    c = conn(DB_R)

    rows = c.execute("""
        SELECT
            uid,        -- 0
            ts_recorded -- 1
        FROM v_recorder_for_gest
        ORDER BY ts_recorded DESC
        LIMIT 200
    """).fetchall()

    for r in rows:
        uid = r[0]

        cg.execute("""
            UPDATE trades SET
                ts_record=?, status='recorded',
                ts_status_update=strftime('%s','now')*1000
            WHERE uid=? AND status='closed'
        """, (r[1], uid))

    c.close()


# ======================================================================
# MAIN LOOP
# ======================================================================
def main():
    while True:
        try:
            cg = conn(DB_G)

            ingest_signals(cg)
            ingest_opener(cg)
            ingest_follower(cg)
            ingest_closer(cg)
            ingest_recorder(cg)

            cg.close()

        except Exception as e:
            log.error(f"loop error: {e}")

        time.sleep(0.25)


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/health_dash.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3, sys
from datetime import datetime

DB_O = "/opt/scalp/project/data/opener.db"
DB_F = "/opt/scalp/project/data/follower.db"
DB_BUDGET = "/opt/scalp/project/data/budget.db"

def connect(db):
    con = sqlite3.connect(db)
    con.row_factory = sqlite3.Row
    return con

def print_table(title, rows):
    print(f"\n--- {title} ---")
    if not rows:
        print("(vide)")
        return
    headers = rows[0].keys()
    print(" | ".join(headers))
    for r in rows:
        print(" | ".join(str(r[k]) for k in headers))

def main():
    print(f"===== PIPELINE HEALTH {datetime.now().strftime('%H:%M:%S')} =====")
    co = connect(DB_O)
    cf = connect(DB_F)
    cb = connect(DB_BUDGET)

    print_table("OPEN POSITIONS (opener)", co.execute(
        "SELECT instId, side, status, margin_used FROM trades_open_init ORDER BY ts_open DESC LIMIT 10;"
    ).fetchall())

    print_table("FOLLOW POSITIONS (follower)", cf.execute(
        "SELECT instId, side, status, pnl FROM trades_follow ORDER BY ts_update DESC LIMIT 10;"
    ).fetchall())

    print_table("BUDGET OVERVIEW", cb.execute(
        "SELECT * FROM v_budget_overview;"
    ).fetchall())

    co.close(); cf.close(); cb.close()
    print("\n===== END DASH =====")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/opener.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3, time, logging

ROOT = "/opt/scalp/project"
DB_GEST = f"{ROOT}/data/gest.db"
DB_OPEN = f"{ROOT}/data/opener.db"
LOG     = f"{ROOT}/logs/opener.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s OPENER %(levelname)s %(message)s"
)
log = logging.getLogger("OPENER")


def conn(path):
    c = sqlite3.connect(path, timeout=5, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA synchronous=NORMAL;")
    c.execute("PRAGMA busy_timeout=3000;")
    return c


def get_new_trades():
    gest = conn(DB_GEST)
    q = """
    SELECT uid, instId, side,
           price_signal,
           atr_signal,
           score_B,
           ctx,
           ts_signal
    FROM v_for_opener
    ORDER BY ts_signal DESC;
    """
    rows = gest.execute(q).fetchall()
    gest.close()
    return rows


def compute_opening(price, atr, side):
    lev = 10
    qty = 50 / price
    entry = price
    margin = qty * price / lev
    sl = price - 2 * atr if side == "buy" else price + 2 * atr
    tp = price + 4 * atr if side == "buy" else price - 4 * atr
    return entry, qty, lev, margin, sl, tp


def write_open(uid, instId, side, entry, qty, lev, margin, sl, tp):
    op = conn(DB_OPEN)
    ts_open = int(time.time()*1000)
    op.execute("""
    INSERT OR REPLACE INTO trades_opener (
        uid, instId, side,
        entry, qty, lev, margin, sl, tp,
        ts_open, status
    ) VALUES (?,?,?,?,?,?,?,?,?,?,?);
    """, (uid, instId, side,
          entry, qty, lev, margin, sl, tp,
          ts_open, "opened"))
    op.close()
    log.info(f"OPEN {uid} {instId} {side} entry={entry} qty={qty} lev={lev}")


def main():
    log.info("OPENER start")
    while True:
        rows = get_new_trades()
        for r in rows:
            uid, instId, side, price, atr, scoreB, ctx, ts_signal = r
            entry, qty, lev, margin, sl, tp = compute_opening(price, atr, side)
            write_open(uid, instId, side, entry, qty, lev, margin, sl, tp)
        time.sleep(0.3)


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/recorder.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sqlite3, time, logging

ROOT = "/opt/scalp/project"
DB_GEST = f"{ROOT}/data/gest.db"
DB_REC = f"{ROOT}/data/recorder.db"
LOG = f"{ROOT}/logs/recorder.log"

logging.basicConfig(
    filename=LOG,
    level=logging.INFO,
    format="%(asctime)s RECORDER %(levelname)s %(message)s"
)
log = logging.getLogger("RECORDER")


def conn(path):
    c = sqlite3.connect(path, timeout=5, isolation_level=None)
    c.execute("PRAGMA journal_mode=WAL;")
    c.execute("PRAGMA synchronous=NORMAL;")
    c.execute("PRAGMA busy_timeout=3000;")
    return c


# -----------------------------------------------------------
# READ FROM GEST
# -----------------------------------------------------------
def get_closed_for_record():
    g = conn(DB_GEST)
    q = """
        SELECT
            uid,
            instId,
            side,
            reason,
            ctx,
            score_A,
            score_B,
            price_signal,
            atr_signal,
            ts_signal,

            entry,
            qty,
            lev,
            margin,
            sl_init,
            tp_init,
            ts_open,
            ts_follow,

            price_close,
            pnl_usdt,
            pnl_pct,
            ts_close,
            reason_close
        FROM v_for_recorder
        ORDER BY ts_close DESC;
    """
    rows = g.execute(q).fetchall()
    g.close()
    return rows


# -----------------------------------------------------------
# WRITE INTO RECORDER.DB
# -----------------------------------------------------------
def write_record(
    uid, instId, side, reason, ctx,
    score_A, score_B,
    price_signal, atr_signal, ts_signal,
    entry, qty, lev, margin,
    sl_init, tp_init, ts_open,
    sl_final, tp_final, be_triggered, pyramided, ts_follow,
    price_close, pnl_usdt, pnl_pct, ts_close, reason_close
):

    ts_recorded = int(time.time() * 1000)
    r = conn(DB_REC)

    r.execute("""
    INSERT OR REPLACE INTO trades_record (
        uid,
        instId, side, reason, ctx,
        score_A, score_B,
        price_signal, atr_signal, ts_signal,
        entry, qty, lev, margin,
        sl_init, tp_init, ts_open,
        sl_final, tp_final, be_triggered, pyramided, ts_follow,
        price_close, pnl_usdt, pnl_pct, ts_close, reason_close,
        ts_recorded
    )
    VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?);
    """, (
        uid,
        instId, side, reason, ctx,
        score_A, score_B,
        price_signal, atr_signal, ts_signal,
        entry, qty, lev, margin,
        sl_init, tp_init, ts_open,
        sl_final, tp_final, be_triggered, pyramided, ts_follow,
        price_close, pnl_usdt, pnl_pct, ts_close, reason_close,
        ts_recorded
    ))

    log.info(f"RECORDED {uid} {instId} pnl={pnl_usdt}usdt ctx={ctx} reason_close={reason_close}")
    r.close()


# -----------------------------------------------------------
# MAIN LOOP
# -----------------------------------------------------------
def main():
    log.info("RECORDER start")

    while True:
        try:
            rows = get_closed_for_record()

            for r in rows:
                write_record(*r)

        except Exception as e:
            log.error(f"record loop error: {e}")

        time.sleep(0.4)


if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/signals.py -----
#!/usr/bin/env python3
import sqlite3
import logging
from datetime import datetime

DB_TRIG = "/opt/scalp/project/data/triggers.db"
DB_A    = "/opt/scalp/project/data/a.db"
DB_SIG  = "/opt/scalp/project/data/signals.db"

logging.basicConfig(
    filename="/opt/scalp/project/logs/signals.log",
    level=logging.INFO,
    format="%(asctime)s SIGNAL %(levelname)s %(message)s"
)

# -----------------------------------------------------------------------------
# DB CONNECT
# -----------------------------------------------------------------------------
def connect(path):
    conn = sqlite3.connect(path, timeout=5, isolation_level=None)
    conn.execute("PRAGMA journal_mode=WAL;")
    conn.execute("PRAGMA synchronous=NORMAL;")
    return conn

# -----------------------------------------------------------------------------
# MAKE UID — STABLE UID BASED ON TS_TRIGGER
# -----------------------------------------------------------------------------
def make_uid(instId, side, ts_trigger):
    """
    ts_trigger = UNIQUE timestamp of trigger (origin)
    → UID stable = T<ts_trigger>_<INST>_<SIDE>
    """
    clean = instId.replace("/", "")
    return f"T{ts_trigger}_{clean}_{side}"

# -----------------------------------------------------------------------------
# LOAD TRIGGERS
# -----------------------------------------------------------------------------
def load_triggers():
    conn = connect(DB_TRIG)
    cur = conn.cursor()
    cur.execute("""
        SELECT instId, side, reason, score_B, ctx, price, atr, ts_trigger
        FROM v_triggers_latest
        ORDER BY ts_trigger DESC;
    """)
    rows = cur.fetchall()
    conn.close()
    return rows

# -----------------------------------------------------------------------------
# LOAD CTX SCORE_A FROM A.DB
# -----------------------------------------------------------------------------
def load_score_A():
    conn = connect(DB_A)
    cur = conn.cursor()
    cur.execute("SELECT instId, score_A, ctx FROM v_ctx_latest;")
    rows = cur.fetchall()
    conn.close()
    return {r[0]: (r[1], r[2]) for r in rows}

# -----------------------------------------------------------------------------
# SAVE SIGNAL INTO SIGNALS.DB
# -----------------------------------------------------------------------------
def save_signal(row):
    instId, side, reason, score_B, ctx_trig, price, atr, ts_trigger = row

    # score_A + ctx from a.db
    scoreA_map = load_score_A()
    score_A, ctx_A = scoreA_map.get(instId, (None, None))

    # choose ctx from triggers if available, otherwise ctx_A
    ctx = ctx_trig if ctx_trig is not None else ctx_A

    # stable UID based on ts_trigger
    trade_uid = make_uid(instId, side, ts_trigger)

    ts_local = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    hms = datetime.now().strftime("%H:%M:%S")

    conn = connect(DB_SIG)
    cur = conn.cursor()
    cur.execute("""
        INSERT OR REPLACE INTO signals
        (trade_uid, instId, side, reason, ctx,
         score_A, score_B, price, atr,
         ts_signal, ts_local, hms)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);
    """, (
        trade_uid, instId, side, reason, ctx,
        score_A, score_B, price, atr,
        ts_trigger, ts_local, hms
    ))
    conn.commit()
    conn.close()

    logging.info(f"[SIGNAL] {trade_uid} {instId} {side} scoreB={score_B}")

# -----------------------------------------------------------------------------
# MAIN
# -----------------------------------------------------------------------------
def main():
    logging.info("SIGNAL generator START")
    rows = load_triggers()
    for r in rows:
        save_signal(r)
    logging.info("SIGNAL generator DONE")

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/ticks.py -----
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import asyncio
import json
import sqlite3
import threading
import time
from queue import Queue
import websockets

DB_PATH = "/opt/scalp/project/data/t.db"
DB_A_PATH = "/opt/scalp/project/data/a.db"
WS_URL = "wss://ws.bitget.com/mix/v1/stream"
FLUSH_INTERVAL = 0.5  # secondes
QUEUE_MAX = 5000

q = Queue(maxsize=QUEUE_MAX)
lock = threading.Lock()
stop_event = threading.Event()

# --- Connexion SQLite optimisée ---
def init_db():
    conn = sqlite3.connect(DB_PATH, check_same_thread=False, timeout=10)
    cur = conn.cursor()
    cur.executescript("""
        PRAGMA journal_mode=WAL;
        PRAGMA synchronous=OFF;
        CREATE TABLE IF NOT EXISTS ticks(
            instId TEXT PRIMARY KEY,
            price REAL,
            ts_ms INTEGER
        );
        CREATE INDEX IF NOT EXISTS idx_ticks_ts ON ticks(ts_ms);
    """)
    conn.commit()
    return conn

# --- Récupération des symboles depuis le contexte A ---
def get_symbols():
    conn = sqlite3.connect(DB_A_PATH)
    cur = conn.cursor()
    cur.execute("SELECT instId FROM v_ctx_latest;")
    symbols = [r[0].replace("/", "") for r in cur.fetchall()]
    conn.close()
    return symbols

# --- Thread d'écriture SQLite ---
def writer_thread():
    conn = init_db()
    cur = conn.cursor()
    last_flush = 0
    buffer = []
    while not stop_event.is_set():
        try:
            tick = q.get(timeout=FLUSH_INTERVAL)
            buffer.append(tick)
        except:
            pass

        now = time.time()
        if now - last_flush >= FLUSH_INTERVAL and buffer:
            with lock:
                cur.executemany(
                    "INSERT INTO ticks (instId, price, ts_ms) VALUES (?, ?, ?) "
                    "ON CONFLICT(instId) DO UPDATE SET price=excluded.price, ts_ms=excluded.ts_ms;",
                    buffer,
                )
                conn.commit()
                buffer.clear()
                last_flush = now

    conn.commit()
    conn.close()

# --- Fonction WS principale ---
async def ws_consumer():
    symbols = get_symbols()
    args = [
        {"instType": "USDT-FUTURES", "channel": "ticker", "instId": s}
        for s in symbols
    ]
    sub_msg = json.dumps({"op": "subscribe", "args": args})
    print(f"[ticks] Subscribing to {len(symbols)} symbols...")

    async with websockets.connect(WS_URL, ping_interval=15, ping_timeout=10) as ws:
        await ws.send(sub_msg)
        print("[ticks] Connected & subscribed.")
        async for msg in ws:
            data = json.loads(msg)
            if "data" not in data:
                continue
            for d in data["data"]:
                try:
                    instId = d["instId"]
                    price = float(d["lastPr"])
                    ts_ms = int(d["ts"])
                    if not q.full():
                        q.put((instId, price, ts_ms))
                except Exception as e:
                    pass

# --- Main ---
def main():
    wt = threading.Thread(target=writer_thread, daemon=True)
    wt.start()

    try:
        asyncio.run(ws_consumer())
    except KeyboardInterrupt:
        print("Stopping...")
    finally:
        stop_event.set()
        wt.join()

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/triggers.py -----
#!/usr/bin/env python3
# SCALP - TRIGGERS MODULE (V5 HYBRIDE PRO)
# Features 1m + Validation Tick + Filtre Expert
# Compatible SCALP guidelines (U/OA/A/OB/T/B/X + gest bus)

import sqlite3
import time
import logging

DB_B   = "/opt/scalp/project/data/b.db"
DB_T   = "/opt/scalp/project/data/t.db"
DB_TRG = "/opt/scalp/project/data/triggers.db"

LOOP_SLEEP   = 1.0
COOLDOWN_MS  = 90_000
BOOST_FACTOR = 1.20   # +20% pour re-déclencher

logging.basicConfig(
    filename="/opt/scalp/project/logs/triggers.log",
    level=logging.INFO,
    format="%(asctime)s TRIG %(levelname)s %(message)s"
)

def connect(db):
    conn = sqlite3.connect(db, timeout=5, isolation_level=None)
    conn.row_factory = sqlite3.Row
    return conn

def normalize(x, low, high):
    if x is None:
        return 0.0
    if x <= low:
        return 0.0
    if x >= high:
        return 1.0
    return (x - low) / (high - low)

# -------------------------------------------------------
# LOAD FEATURES
# -------------------------------------------------------
def load_feat(instId):
    conn = connect(DB_B)
    q = """
        SELECT 
            ema12, ema26,
            macd, macdsignal,
            atr, bb_width, 
            mom, roc, slope,
            c AS price
        FROM feat_1m
        WHERE instId=?
        ORDER BY ts DESC
        LIMIT 1;
    """
    row = conn.execute(q, (instId,)).fetchone()
    conn.close()
    return row

# -------------------------------------------------------
# LOAD LAST TICK  (AAVE/USDT → AAVEUSDT)
# -------------------------------------------------------
def load_tick(instId):
    tick_id = instId.replace("/", "")
    conn = connect(DB_T)
    row = conn.execute(
        "SELECT lastPr FROM v_ticks_latest WHERE instId=? LIMIT 1;",
        (tick_id,)
    ).fetchone()
    conn.close()
    return row["lastPr"] if row else None

# -------------------------------------------------------
# DETECT (features only)
# -------------------------------------------------------
def detect(inst, f):
    if f is None:
        return None

    price = f["price"]
    if not price or price <= 0:
        return None

    bb     = f["bb_width"] or 0.0
    atr    = f["atr"] or 0.0
    bbw    = bb / price
    slope  = f["slope"] or 0.0
    roc    = f["roc"] or 0.0
    mom    = f["mom"] or 0.0
    ema12  = f["ema12"]
    ema26  = f["ema26"]
    macd   = f["macd"]
    macds  = f["macdsignal"]

    # EXPANSION
    if bbw > 0.007:
        score = normalize(bbw, 0.005, 0.02)
        side  = "buy" if mom > 0 else "sell"
        return ("VOLATILITY_EXPANSION", side, score, price, atr)

    # SQUEEZE
    if bbw < 0.002:
        score = normalize(0.003 - bbw, 0.0, 0.003)
        side  = "buy" if macd > macds else "sell"
        return ("VOLATILITY_SQUEEZE", side, score, price, atr)

    # MOMENTUM
    if abs(roc) > 0.30:
        score = normalize(abs(roc), 0.30, 1.0)
        side = "buy" if roc > 0 else "sell"
        return ("MOMENTUM", side, score, price, atr)

    # BREAKOUT
    if abs(slope) > 0.0015:
        score = normalize(abs(slope), 0.0015, 0.008)
        side  = "buy" if slope > 0 else "sell"
        return ("BREAKOUT", side, score, price, atr)

    # EMA SHIFT
    if ema12 and ema26:
        diff = (ema12 - ema26) / price
        if abs(diff) > 0.002:
            score = normalize(abs(diff), 0.002, 0.01)
            side  = "buy" if diff > 0 else "sell"
            return ("EMA_TREND_SHIFT", side, score, price, atr)

    return None

# -------------------------------------------------------
# TICK VALIDATION
# -------------------------------------------------------
def validate_tick(reason, side, score, price, atr, tick_price):
    if tick_price is None:
        return False

    # BREAKOUT
    if reason == "BREAKOUT":
        if side == "buy"  and tick_price > price * 1.0004:
            return True
        if side == "sell" and tick_price < price * 0.9996:
            return True
        return False

    # MOMENTUM
    if reason == "MOMENTUM":
        return abs(tick_price - price) > atr * 0.50

    # SQUEEZE
    if reason == "VOLATILITY_SQUEEZE":
        return abs(tick_price - price) < atr * 0.20

    # EXPANSION
    if reason == "VOLATILITY_EXPANSION":
        return abs(tick_price - price) > atr * 0.80

    # EMA SHIFT
    if reason == "EMA_TREND_SHIFT":
        if side == "buy"  and tick_price > price * 1.0002:
            return True
        if side == "sell" and tick_price < price * 0.9998:
            return True
        return False

    return False

# -------------------------------------------------------
# LOAD / SAVE STATE
# -------------------------------------------------------
def load_state(inst):
    conn = connect(DB_TRG)
    row = conn.execute(
        "SELECT reason, side, score, ts FROM trig_state WHERE instId=?",
        (inst,)
    ).fetchone()
    conn.close()
    return row

def save_state(inst, reason, side, score, ts):
    conn = connect(DB_TRG)
    conn.execute("""
        INSERT INTO trig_state(instId, reason, side, score, ts)
        VALUES(?,?,?,?,?)
        ON CONFLICT(instId)
        DO UPDATE SET reason=excluded.reason,
                      side=excluded.side,
                      score=excluded.score,
                      ts=excluded.ts;
    """, (inst, reason, side, score, ts))
    conn.close()

# -------------------------------------------------------
# SHOULD EMIT?
# -------------------------------------------------------
def should_emit(inst, reason, side, score):
    prev = load_state(inst)
    now  = int(time.time() * 1000)

    if prev is None:
        return True
    if reason != prev["reason"]:
        return True
    if side != prev["side"]:
        return True
    if score > prev["score"] * BOOST_FACTOR:
        return True
    if now - prev["ts"] >= COOLDOWN_MS:
        return True
    return False

# -------------------------------------------------------
# INSERT TRIGGER
# -------------------------------------------------------
def push_trigger(inst, reason, side, score, price, atr):
    ts = int(time.time() * 1000)
    conn = connect(DB_TRG)
    conn.execute("""
        INSERT INTO triggers(
            instId, tf, side, reason, score,
            ctx, confidence, price, atr, ts, consumed
        )
        VALUES(?, '1m', ?, ?, ?, NULL, ?, ?, ?, ?, 0)
    """, (inst, side, reason, score, score, price, atr, ts))
    conn.close()

    logging.info(f"TRIGGER {inst} {side} {reason} score={score:.3f}")
    save_state(inst, reason, side, score, ts)

# -------------------------------------------------------
# MAIN LOOP
# -------------------------------------------------------
def main():
    print("SCALP TRIGGERS V5 HYBRIDE PRO — running")

    while True:
        try:
            conn = connect(DB_B)
            insts = conn.execute("SELECT DISTINCT instId FROM feat_1m;").fetchall()
            conn.close()

            for row in insts:
                inst = row["instId"]

                f = load_feat(inst)
                raw = detect(inst, f)
                if not raw:
                    continue

                reason, side, score, price, atr = raw

                tick_price = load_tick(inst)
                if not validate_tick(reason, side, score, price, atr, tick_price):
                    continue

                if should_emit(inst, reason, side, score):
                    push_trigger(inst, reason, side, score, price, atr)

            time.sleep(LOOP_SLEEP)

        except Exception as e:
            logging.error(f"Loop error: {e}")
            time.sleep(1)

if __name__ == "__main__":
    main()

----- FILE: /opt/scalp/project/scripts/ws_test_candle15m.py -----
#!/usr/bin/env python3
import asyncio
import json
import websockets
import time

WS_URL = "wss://ws.bitget.com/v2/ws/public"

SUB = {
    "op": "subscribe",
    "args": [
        {
            "instType": "USDT-FUTURES",
            "channel": "candle15m",
            "instId": "BTCUSDT"
        }
    ]
}

async def run():
    while True:
        try:
            print("[WS] Connecting to Bitget...")
            async with websockets.connect(WS_URL, ping_interval=20) as ws:

                print("[WS] Connected, subscribing...")
                await ws.send(json.dumps(SUB))

                while True:
                    msg = await ws.recv()
                    data = json.loads(msg)

                    # Heartbeat
                    if isinstance(data, dict) and data.get("event") in ("ping", "pong"):
                        continue

                    # Data OK
                    if "data" in data:
                        k = data["data"][0]

                        ts     = int(k[0])
                        open_  = float(k[1])
                        high   = float(k[2])
                        low    = float(k[3])
                        close  = float(k[4])
                        volume = float(k[5])

                        print(f"[15m] ts={ts} open={open_} high={high} low={low} close={close} volume={volume}")

        except Exception as e:
            print(f"[WS] Error: {e}, reconnect in 5s...")
            await asyncio.sleep(5)

if __name__ == "__main__":
    asyncio.run(run())

----- FILE: /opt/scalp/project/sql/fix_pipeline_schema.sql -----
PRAGMA foreign_keys=off;
BEGIN TRANSACTION;

--------------------------------------------------------------------
-- 1) TRIGGERS.DB — corriger table triggers
--------------------------------------------------------------------
-- Colonnes attendues :
-- id | instId | tf | side | reason | score | ctx | confidence | price | atr | ts | consumed

-- Ajout des colonnes manquantes (idempotent)
ALTER TABLE triggers ADD COLUMN tf TEXT DEFAULT '1m';
ALTER TABLE triggers ADD COLUMN ctx TEXT;
ALTER TABLE triggers ADD COLUMN confidence REAL DEFAULT 0;
ALTER TABLE triggers ADD COLUMN price REAL;
ALTER TABLE triggers ADD COLUMN atr REAL;

--------------------------------------------------------------------
-- 2) SIGNALS.DB — corriger table signals + recréer v_for_gest
--------------------------------------------------------------------
-- Colonnes attendues :
-- id | trade_uid | instId | side | reason
-- score_U | score_A | score_B
-- price | atr | ctx | ts_signal

ALTER TABLE signals ADD COLUMN price REAL;
ALTER TABLE signals ADD COLUMN atr REAL;
ALTER TABLE signals ADD COLUMN ctx TEXT;

DROP VIEW IF EXISTS v_for_gest;

CREATE VIEW v_for_gest AS
WITH latest AS (
    SELECT
        trade_uid,
        instId,
        side,
        score_U,
        score_A,
        score_B,
        reason,
        ctx,
        price,
        atr,
        ts_signal,
        ROW_NUMBER() OVER (
            PARTITION BY instId
            ORDER BY ts_signal DESC
        ) AS rn
    FROM signals
)
SELECT trade_uid, instId, side,
       score_U, score_A, score_B,
       reason, ctx, price, atr, ts_signal
FROM latest
WHERE rn = 1;

--------------------------------------------------------------------
-- 3) GEST.DB — corriger trades_all
--------------------------------------------------------------------
-- Colonnes attendues :
-- trade_uid | instId | side
-- score_U | score_A | score_B
-- reason | ctx | price | atr
-- ts_signal | state | ts_create | ts_update

ALTER TABLE trades_all ADD COLUMN score_U REAL;
ALTER TABLE trades_all ADD COLUMN score_A REAL;
ALTER TABLE trades_all ADD COLUMN score_B REAL;
ALTER TABLE trades_all ADD COLUMN reason TEXT;
ALTER TABLE trades_all ADD COLUMN ctx TEXT;
ALTER TABLE trades_all ADD COLUMN price REAL;
ALTER TABLE trades_all ADD COLUMN atr REAL;
ALTER TABLE trades_all ADD COLUMN ts_signal INTEGER;

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/gest_schema.sql -----
CREATE TABLE IF NOT EXISTS trades (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,
    entry_signal REAL,
    scoreA REAL,
    scoreB REAL,
    reason TEXT,
    ts_signal INTEGER,

    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,
    sl REAL,
    tp REAL,
    ts_open INTEGER,

    follow_price REAL,
    follow_sl REAL,
    follow_tp REAL,
    ts_follow INTEGER,

    close_price REAL,
    close_reason TEXT,
    ts_close INTEGER,

    pnl_real REAL,
    ts_record INTEGER,

    status TEXT
);

----- FILE: /opt/scalp/project/sql/gest_update_view_opener.sql -----
DROP VIEW IF EXISTS v_for_opener;

CREATE VIEW v_for_opener AS
SELECT
    trade_uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    score_U,
    price,
    atr,
    ts_signal,
    ts_local,
    hms,
    status
FROM trades_all
WHERE status = 'new'
ORDER BY ts_signal DESC;


----- FILE: /opt/scalp/project/sql/gest_view_opener_for_gest.sql -----
DROP VIEW IF EXISTS v_opener_for_gest;

CREATE VIEW v_opener_for_gest AS
SELECT
    trade_uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    score_U,
    price,
    atr,
    ts_signal,
    ts_local,
    hms
FROM trades_all
WHERE status = 'opened'
ORDER BY ts_signal DESC;

----- FILE: /opt/scalp/project/sql/init_signals.sql -----
DROP TABLE IF EXISTS signals;
DROP VIEW IF EXISTS v_for_gest;

CREATE TABLE signals (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_uid  TEXT NOT NULL,
    instId     TEXT NOT NULL,
    reason     TEXT NOT NULL,
    side       TEXT NOT NULL,
    score_U    REAL DEFAULT 0,
    score_A    REAL DEFAULT 0,
    score_B    REAL NOT NULL,
    price      REAL,
    atr        REAL,
    ctx        TEXT,
    ts_signal  INTEGER NOT NULL
);

CREATE VIEW v_for_gest AS
WITH latest AS (
    SELECT
        trade_uid,
        instId,
        side,
        score_U,
        score_A,
        score_B,
        reason,
        ctx,
        price,
        atr,
        ts_signal,
        ROW_NUMBER() OVER (
            PARTITION BY instId ORDER BY ts_signal DESC
        ) AS rn
    FROM signals
)
SELECT * FROM latest WHERE rn = 1;


----- FILE: /opt/scalp/project/sql/migration_gest_trades_all.sql -----
PRAGMA foreign_keys=off;
BEGIN TRANSACTION;

DROP TABLE IF EXISTS trades_all;

CREATE TABLE trades_all (
    trade_uid TEXT PRIMARY KEY,
    instId TEXT NOT NULL,
    side TEXT NOT NULL,

    -- Scores
    score_U REAL DEFAULT 0,
    score_A REAL DEFAULT 0,
    score_B REAL DEFAULT 0,

    -- Contexte & enrichissements
    reason TEXT,
    ctx TEXT,
    price REAL,
    atr REAL,

    -- Horodatage signal
    ts_signal INTEGER,

    -- State machine GEST
    state TEXT NOT NULL,
    ts_create INTEGER NOT NULL,
    ts_update INTEGER NOT NULL
);

CREATE INDEX idx_trades_inst ON trades_all(instId);
CREATE INDEX idx_trades_state ON trades_all(state);
CREATE INDEX idx_trades_ts ON trades_all(ts_signal);

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/migration_signals_full_reset.sql -----
PRAGMA foreign_keys=off;

BEGIN TRANSACTION;

DROP TABLE IF EXISTS signals;

CREATE TABLE signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_uid TEXT UNIQUE NOT NULL,
    instId TEXT NOT NULL,
    reason TEXT NOT NULL,
    side TEXT NOT NULL,

    -- Scores
    score_U REAL NOT NULL DEFAULT 0,
    score_A REAL NOT NULL DEFAULT 0,
    score_B REAL NOT NULL DEFAULT 0,

    -- Enrichissements
    price REAL DEFAULT NULL,
    atr REAL DEFAULT NULL,
    atr_mult REAL DEFAULT NULL,
    ctx TEXT DEFAULT NULL,

    -- Horodatage
    ts_signal INTEGER NOT NULL
);

CREATE INDEX idx_sig_instId    ON signals(instId);
CREATE INDEX idx_sig_ts        ON signals(ts_signal);
CREATE INDEX idx_sig_uid       ON signals(trade_uid);

DROP VIEW IF EXISTS v_for_gest;

CREATE VIEW v_for_gest AS
WITH latest AS (
    SELECT
        trade_uid,
        instId,
        side,
        score_U,
        score_A,
        score_B,
        reason,
        ctx,
        price,
        atr,
        ts_signal,
        ROW_NUMBER() OVER (PARTITION BY instId ORDER BY ts_signal DESC) AS rn
    FROM signals
)
SELECT
    trade_uid,
    instId,
    side,
    score_U,
    score_A,
    score_B,
    reason,
    ctx,
    price,
    atr,
    ts_signal
FROM latest
WHERE rn = 1;

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/opener_schema.sql -----
CREATE TABLE IF NOT EXISTS trades_opener (
    uid TEXT PRIMARY KEY,
    instId TEXT,
    side TEXT,
    entry REAL,
    qty REAL,
    lev REAL,
    margin REAL,
    sl REAL,
    tp REAL,
    ts_open INTEGER,
    status TEXT
);

----- FILE: /opt/scalp/project/sql/reset_gest.sql -----
PRAGMA foreign_keys=off;
BEGIN TRANSACTION;

DROP TABLE IF EXISTS trades_all;

CREATE TABLE trades_all (
    trade_uid TEXT PRIMARY KEY,
    instId TEXT NOT NULL,
    side TEXT NOT NULL,
    score_U REAL DEFAULT 0,
    score_A REAL DEFAULT 0,
    score_B REAL DEFAULT 0,
    reason TEXT,
    ctx TEXT,
    price REAL,
    atr REAL,
    ts_signal INTEGER,
    state TEXT NOT NULL,
    ts_create INTEGER NOT NULL,
    ts_update INTEGER NOT NULL
);

CREATE INDEX idx_gest_instId ON trades_all(instId);
CREATE INDEX idx_gest_state ON trades_all(state);
CREATE INDEX idx_gest_ts     ON trades_all(ts_signal);

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/reset_signals.sql -----
PRAGMA foreign_keys=off;
BEGIN TRANSACTION;

DROP TABLE IF EXISTS signals;

CREATE TABLE signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_uid TEXT UNIQUE NOT NULL,
    instId TEXT NOT NULL,
    reason TEXT NOT NULL,
    side TEXT NOT NULL,
    score_U REAL NOT NULL DEFAULT 0,
    score_A REAL NOT NULL DEFAULT 0,
    score_B REAL NOT NULL DEFAULT 0,
    price REAL DEFAULT NULL,
    atr REAL DEFAULT NULL,
    ctx TEXT DEFAULT NULL,
    ts_signal INTEGER NOT NULL
);

CREATE INDEX idx_sig_instId    ON signals(instId);
CREATE INDEX idx_sig_ts        ON signals(ts_signal);
CREATE INDEX idx_sig_uid       ON signals(trade_uid);

DROP VIEW IF EXISTS v_for_gest;

CREATE VIEW v_for_gest AS
WITH latest AS (
    SELECT *,
           ROW_NUMBER() OVER (PARTITION BY instId ORDER BY ts_signal DESC) AS rn
    FROM signals
)
SELECT
    trade_uid,
    instId,
    side,
    score_U,
    score_A,
    score_B,
    reason,
    ctx,
    price,
    atr,
    ts_signal
FROM latest
WHERE rn = 1;

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/reset_triggers.sql -----
PRAGMA foreign_keys=off;
BEGIN TRANSACTION;

DROP TABLE IF EXISTS triggers;

CREATE TABLE triggers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    instId TEXT NOT NULL,
    side TEXT NOT NULL,
    reason TEXT NOT NULL,
    score REAL NOT NULL,
    tf TEXT NOT NULL,         -- demandé par B_triggers.py
    price REAL DEFAULT NULL,  -- demandé par signals.py
    atr REAL DEFAULT NULL,    -- demandé par signals.py
    ts INTEGER NOT NULL,
    consumed INTEGER DEFAULT NULL
);

CREATE INDEX idx_trig_instId   ON triggers(instId);
CREATE INDEX idx_trig_ts       ON triggers(ts);
CREATE INDEX idx_trig_tf       ON triggers(tf);
CREATE INDEX idx_trig_consumed ON triggers(consumed);

COMMIT;
PRAGMA foreign_keys=on;

----- FILE: /opt/scalp/project/sql/signals_schema.sql -----
PRAGMA journal_mode=WAL;
PRAGMA synchronous=NORMAL;

DROP TABLE IF EXISTS signals;
CREATE TABLE signals (
    trade_uid TEXT PRIMARY KEY,
    instId    TEXT NOT NULL,
    side      TEXT NOT NULL,
    reason    TEXT NOT NULL,
    ctx       TEXT,
    score_A   REAL,
    score_B   REAL,
    price     REAL NOT NULL,
    atr       REAL,
    ts_signal INTEGER NOT NULL,
    ts_local  TEXT,
    hms       TEXT
);

DROP VIEW IF EXISTS v_for_gest;
CREATE VIEW v_for_gest AS
SELECT
    trade_uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price,
    atr,
    ts_signal,
    ts_local,
    hms
FROM signals
ORDER BY ts_signal DESC;

----- FILE: /opt/scalp/project/sql/view_closer_for_gest.sql -----
CREATE VIEW IF NOT EXISTS v_closer_for_gest AS
SELECT
    uid,
    close_price,
    close_reason,
    ts_close
FROM trades_closer
WHERE status = 'to_close';

----- FILE: /opt/scalp/project/sql/view_follower_for_gest.sql -----
CREATE VIEW IF NOT EXISTS v_follower_for_gest AS
SELECT
    uid,
    follow_price,
    follow_sl,
    follow_tp,
    ts_follow
FROM trades_follower
WHERE status = 'followed';

----- FILE: /opt/scalp/project/sql/view_gest_for_opener.sql -----
CREATE VIEW IF NOT EXISTS v_for_opener AS
SELECT
    uid,
    instId,
    side,
    entry_signal,
    scoreA,
    scoreB,
    reason,
    ts_signal
FROM trades
WHERE status = 'new';

----- FILE: /opt/scalp/project/sql/view_opener_for_gest.sql -----
CREATE VIEW IF NOT EXISTS v_opener_for_gest AS
SELECT
    uid,
    instId,
    side,
    entry AS entry_open,
    qty,
    lev,
    margin,
    sl,
    tp,
    ts_open
FROM trades_opener
WHERE status = 'opened';

----- FILE: /opt/scalp/project/sql/view_recorder_for_gest.sql -----
CREATE VIEW IF NOT EXISTS v_recorder_for_gest AS
SELECT
    uid,
    pnl_real,
    ts_record
FROM trades_record
WHERE status = 'recorded';

----- FILE: /opt/scalp/project/sql/view_signals_for_gest.sql -----
CREATE VIEW IF NOT EXISTS v_signals_for_gest AS
SELECT
    trade_uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price,
    atr,
    ts_signal,
    datetime(ts_signal/1000, 'unixepoch', 'localtime') AS ts_local,
    strftime('%H:%M:%S', ts_signal/1000, 'unixepoch', 'localtime') AS hms
FROM signals_B
ORDER BY ts_signal DESC;

----- FILE: /opt/scalp/project/sql/ws_schema.sql -----
PRAGMA journal_mode=WAL;

CREATE TABLE IF NOT EXISTS ws_ohlcv_5m (
    instId  TEXT NOT NULL,
    ts      INTEGER NOT NULL,
    open    REAL NOT NULL,
    high    REAL NOT NULL,
    low     REAL NOT NULL,
    close   REAL NOT NULL,
    volume  REAL NOT NULL,
    PRIMARY KEY (instId, ts)
);

CREATE TABLE IF NOT EXISTS ws_ohlcv_15m (
    instId  TEXT NOT NULL,
    ts      INTEGER NOT NULL,
    open    REAL NOT NULL,
    high    REAL NOT NULL,
    low     REAL NOT NULL,
    close   REAL NOT NULL,
    volume  REAL NOT NULL,
    PRIMARY KEY (instId, ts)
);

CREATE TABLE IF NOT EXISTS ws_ohlcv_30m (
    instId  TEXT NOT NULL,
    ts      INTEGER NOT NULL,
    open    REAL NOT NULL,
    high    REAL NOT NULL,
    low     REAL NOT NULL,
    close   REAL NOT NULL,
    volume  REAL NOT NULL,
    PRIMARY KEY (instId, ts)
);

CREATE INDEX IF NOT EXISTS idx_ws5_inst_ts  ON ws_ohlcv_5m(instId, ts);
CREATE INDEX IF NOT EXISTS idx_ws15_inst_ts ON ws_ohlcv_15m(instId, ts);
CREATE INDEX IF NOT EXISTS idx_ws30_inst_ts ON ws_ohlcv_30m(instId, ts);


========== DATABASE STRUCTURE ==========

----- DATABASE: /opt/scalp/project/data/a.db -----
-- TABLES --
bus_out            feat_15m           ohlcv_15m          v_ctx_latest     
ctx_A              feat_30m           ohlcv_30m          v_ohlcv_freshness
ctx_a_latest       feat_5m            ohlcv_5m         

[TABLE: bus_out]
0|id|INTEGER|0||1
1|src|TEXT|0||0
2|instId|TEXT|0||0
3|event|TEXT|0||0
4|payload|TEXT|0|'{}'|0
5|ts|INTEGER|0|strftime('%s','now')*1000|0
6|ack|INTEGER|0|0|0

[TABLE: feat_15m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0
7|ema12|REAL|0||0
8|ema26|REAL|0||0
9|macd|REAL|0||0
10|rsi14|REAL|0||0

[TABLE: ohlcv_15m]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

[TABLE: v_ctx_latest]
0|instId|TEXT|0||0
1|ctx|TEXT|0||0
2|score_A|REAL|0||0

[TABLE: ctx_A]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|score_U|REAL|0||0
3|score_A|REAL|0||0
4|p_buy|REAL|0||0
5|p_sell|REAL|0||0
6|ctx|TEXT|0||0

[TABLE: feat_30m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0
7|ema12|REAL|0||0
8|ema26|REAL|0||0
9|macd|REAL|0||0
10|rsi14|REAL|0||0

[TABLE: ohlcv_30m]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

[TABLE: v_ohlcv_freshness]
0|instId|TEXT|0||0
1|tf||0||0
2|n_candles||0||0
3|ts_last||0||0
4|last_local||0||0
5|age_s||0||0

[TABLE: ctx_a_latest]
0|instId|TEXT|0||1
1|ts|INTEGER|0||0
2|score_U|REAL|0||0
3|score_A|REAL|0||0
4|p_buy|REAL|0||0
5|p_sell|REAL|0||0
6|ctx|TEXT|0||0

[TABLE: feat_5m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0
7|ema12|REAL|0||0
8|ema26|REAL|0||0
9|macd|REAL|0||0
10|rsi14|REAL|0||0

[TABLE: ohlcv_5m]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

-- VIEWS --
v_ctx_latest|CREATE VIEW v_ctx_latest AS
SELECT
    instId,
    ctx,
    score_A
FROM ctx_A_latest
v_ohlcv_freshness|CREATE VIEW v_ohlcv_freshness AS
SELECT instId, '5m' AS tf,
       COUNT(*) AS n_candles,
       MAX(ts) AS ts_last,
       datetime(MAX(ts)/1000,'unixepoch','localtime') AS last_local,
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1) AS age_s
FROM ohlcv_5m
GROUP BY instId
UNION ALL
SELECT instId, '15m',
       COUNT(*), MAX(ts),
       datetime(MAX(ts)/1000,'unixepoch','localtime'),
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1)
FROM ohlcv_15m
GROUP BY instId
UNION ALL
SELECT instId, '30m',
       COUNT(*), MAX(ts),
       datetime(MAX(ts)/1000,'unixepoch','localtime'),
       ROUND((strftime('%s','now')*1000 - MAX(ts))/1000.0,1)
FROM ohlcv_30m
GROUP BY instId
ORDER BY tf, age_s ASC

-- INDEXES --
idx_bus_out_ack|CREATE INDEX idx_bus_out_ack ON bus_out(ack)
idx_bus_out_ts|CREATE INDEX idx_bus_out_ts  ON bus_out(ts)
sqlite_autoindex_ctx_a_latest_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/b.db -----
-- TABLES --
feat_1m    feat_3m    feat_5m    v_feat_1m  v_feat_3m  v_feat_5m

[TABLE: feat_1m]
0|instId|TEXT|0||1
1|ts|INTEGER|0||2
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0

[TABLE: feat_3m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0

[TABLE: feat_5m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0

[TABLE: v_feat_1m]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0
26|age_ms||0||0

[TABLE: v_feat_3m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0
26|age_ms||0||0

[TABLE: v_feat_5m]
0|instId|TEXT|0||0
1|ts|INT|0||0
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0
7|ema9|REAL|0||0
8|ema12|REAL|0||0
9|ema21|REAL|0||0
10|ema26|REAL|0||0
11|ema50|REAL|0||0
12|macd|REAL|0||0
13|macdsignal|REAL|0||0
14|macdhist|REAL|0||0
15|rsi|REAL|0||0
16|atr|REAL|0||0
17|bb_mid|REAL|0||0
18|bb_std|REAL|0||0
19|bb_up|REAL|0||0
20|bb_low|REAL|0||0
21|bb_width|REAL|0||0
22|mom|REAL|0||0
23|roc|REAL|0||0
24|slope|REAL|0||0
25|ctx|TEXT|0||0
26|age_ms||0||0

-- VIEWS --
v_feat_1m|CREATE VIEW v_feat_1m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_1m
v_feat_3m|CREATE VIEW v_feat_3m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_3m
v_feat_5m|CREATE VIEW v_feat_5m AS
SELECT *,
       (strftime('%s','now')*1000 - ts) AS age_ms
FROM feat_5m

-- INDEXES --
idx_feat1|CREATE INDEX idx_feat1 ON feat_1m(instId, ts DESC)
idx_feat3|CREATE INDEX idx_feat3 ON feat_3m(instId, ts DESC)
idx_feat5|CREATE INDEX idx_feat5 ON feat_5m(instId, ts DESC)
sqlite_autoindex_feat_1m_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/budget.db -----
-- TABLES --
budget_state       ledger             v_budget_overview
bus_out            v_balance          v_exposure       

[TABLE: budget_state]
0|id|INTEGER|0||1
1|balance|REAL|0|1000.0|0
2|margin|REAL|0|0.0|0
3|pnl_real|REAL|0|0.0|0
4|ts_update|INTEGER|0||0

[TABLE: ledger]
0|id|INTEGER|0||1
1|src|TEXT|0||0
2|instId|TEXT|0||0
3|side|TEXT|0||0
4|type|TEXT|0||0
5|amount|REAL|0||0
6|ts|INTEGER|0|strftime('%s','now')*1000|0

[TABLE: v_budget_overview]
0|balance||0||0
1|margin||0||0
2|pnl_real||0||0
3|last_update||0||0

[TABLE: bus_out]
0|id|INTEGER|0||1
1|instId|TEXT|0||0
2|event|TEXT|0||0
3|ts|INTEGER|0|strftime('%s','now')*1000|0
4|ack|INTEGER|0|0|0
5|payload|TEXT|0||0
6|src|TEXT|0||0

[TABLE: v_balance]
0|balance_usdt||0||0

[TABLE: v_exposure]
0|instId|TEXT|0||0
1|margin||0||0
2|pnl_real||0||0

-- VIEWS --
v_balance|CREATE VIEW v_balance AS
SELECT ROUND(SUM(CASE
  WHEN type IN ('deposit','pnl_real') THEN amount
  WHEN type='withdraw' THEN -amount
  ELSE 0 END),6) AS balance_usdt
FROM ledger
v_budget_overview|CREATE VIEW v_budget_overview AS
SELECT
  ROUND(balance,6) AS balance,
  ROUND(margin,6)  AS margin,
  ROUND(pnl_real,6) AS pnl_real,
  datetime(ts_update,'unixepoch','localtime') AS last_update
FROM budget_state
v_exposure|CREATE VIEW v_exposure AS
SELECT
    instId,
    ROUND(SUM(CASE WHEN type='margin' THEN amount ELSE 0 END),6) AS margin,
    ROUND(SUM(CASE WHEN type='pnl_real' THEN amount ELSE 0 END),6) AS pnl_real
FROM ledger
GROUP BY instId
ORDER BY ABS(margin) DESC

-- INDEXES --
idx_bus_out_ack|CREATE INDEX idx_bus_out_ack ON bus_out(ack)
idx_bus_out_ts|CREATE INDEX idx_bus_out_ts ON bus_out(ts)

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/closer.db -----
-- TABLES --
trades_closer      v_closer_for_gest

[TABLE: trades_closer]
0|uid|TEXT|0||1
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|entry|REAL|0||0
4|qty|REAL|0||0
5|lev|REAL|0||0
6|margin|REAL|0||0
7|sl|REAL|0||0
8|tp|REAL|0||0
9|price_close|REAL|0||0
10|ts_close|INTEGER|0||0
11|pnl_usdt|REAL|0||0
12|pnl_pct|REAL|0||0
13|reason_close|TEXT|0||0

[TABLE: v_closer_for_gest]
0|uid|TEXT|0||0
1|instId|TEXT|0||0
2|price_close|REAL|0||0
3|pnl_usdt|REAL|0||0
4|pnl_pct|REAL|0||0
5|ts_close|INTEGER|0||0
6|reason_close|TEXT|0||0

-- VIEWS --
v_closer_for_gest|CREATE VIEW v_closer_for_gest AS
SELECT
    uid,
    instId,
    price_close,
    pnl_usdt,
    pnl_pct,
    ts_close,
    reason_close
FROM trades_closer
ORDER BY ts_close DESC

-- INDEXES --
sqlite_autoindex_trades_closer_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/follower.db -----
-- TABLES --
(locked or unreadable)

-- VIEWS --
v_follower_for_gest|CREATE VIEW v_follower_for_gest AS
SELECT
    uid, instId, side,
    entry, qty, lev,
    sl, tp,
    ts_follow,
    status
FROM trades_follower
WHERE status='followed'
ORDER BY ts_follow DESC

-- INDEXES --
sqlite_autoindex_trades_follower_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/gest.db -----
-- TABLES --
trades          v_for_closer    v_for_follower  v_for_opener    v_for_recorder

[TABLE: trades]
0|uid|TEXT|0||1
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|reason|TEXT|0||0
4|ctx|TEXT|0||0
5|score_A|REAL|0||0
6|score_B|REAL|0||0
7|price_signal|REAL|0||0
8|atr_signal|REAL|0||0
9|ts_signal|INTEGER|0||0
10|ts_local|TEXT|0||0
11|hms|TEXT|0||0
12|entry|REAL|0||0
13|qty|REAL|0||0
14|lev|REAL|0||0
15|margin|REAL|0||0
16|sl|REAL|0||0
17|tp|REAL|0||0
18|ts_open|INTEGER|0||0
19|status|TEXT|0||0
20|ts_status_update|INTEGER|0||0

[TABLE: v_for_closer]

[TABLE: v_for_follower]
0|uid|TEXT|0||0
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|entry|REAL|0||0
4|qty|REAL|0||0
5|lev|REAL|0||0
6|margin|REAL|0||0
7|sl|REAL|0||0
8|tp|REAL|0||0
9|ts_open|INTEGER|0||0
10|atr_signal|REAL|0||0
11|price_signal|REAL|0||0
12|score_B|REAL|0||0
13|ctx|TEXT|0||0
14|status|TEXT|0||0

[TABLE: v_for_opener]
0|uid|TEXT|0||0
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|price_signal|REAL|0||0
4|atr_signal|REAL|0||0
5|score_B|REAL|0||0
6|ctx|TEXT|0||0
7|ts_signal|INTEGER|0||0

[TABLE: v_for_recorder]

-- VIEWS --
v_for_closer|CREATE VIEW v_for_closer AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    atr_signal,
    price_signal,
    score_B,
    ctx,
    ts_open,
    ts_follow,
    ts_signal
FROM trades
WHERE status='to_close'
ORDER BY ts_follow DESC
v_for_follower|CREATE VIEW v_for_follower AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    ts_open,
    atr_signal,
    price_signal,
    score_B,
    ctx,
    status
FROM trades
WHERE status IN ('opened','followed')
ORDER BY ts_open DESC
v_for_opener|CREATE VIEW v_for_opener AS
SELECT
    uid,
    instId,
    side,
    price_signal,
    atr_signal,
    score_B,
    ctx,
    ts_signal
FROM trades
WHERE status='new'
ORDER BY ts_signal DESC
v_for_recorder|CREATE VIEW v_for_recorder AS
SELECT
    uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price_signal,
    atr_signal,
    ts_signal,

    entry,
    qty,
    lev,
    margin,
    sl AS sl_init,
    tp AS tp_init,
    ts_open,
    ts_follow,

    price_close,
    pnl_usdt,
    pnl_pct,
    ts_close,
    reason_close
FROM trades
WHERE status='closed'
ORDER BY ts_close DESC

-- INDEXES --
sqlite_autoindex_trades_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/h.db -----
-- TABLES --
bus_out  score_H

[TABLE: bus_out]
0|id|INTEGER|0||1
1|src|TEXT|0||0
2|instId|TEXT|0||0
3|event|TEXT|0||0
4|payload|TEXT|0|'{}'|0
5|ts|INTEGER|0|strftime('%s','now')*1000|0
6|ack|INTEGER|0|0|0

[TABLE: score_H]
0|instId|TEXT|0||1
1|avg_pnl|REAL|0||0
2|winrate|REAL|0||0
3|trades|INTEGER|0||0
4|last_update|INTEGER|0||0

-- VIEWS --

-- INDEXES --
idx_bus_out_ack|CREATE INDEX idx_bus_out_ack ON bus_out(ack)
idx_bus_out_ts|CREATE INDEX idx_bus_out_ts  ON bus_out(ts)
sqlite_autoindex_score_H_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/oa.db -----
-- TABLES --
bus_out             ohlcv_30m           v_ohlcv_15m_latest  v_ohlcv_5m_latest 
ohlcv_15m           ohlcv_5m            v_ohlcv_30m_latest

[TABLE: bus_out]
0|id|INTEGER|0||1
1|src|TEXT|0||0
2|instId|TEXT|0||0
3|event|TEXT|0||0
4|payload|TEXT|0|'{}'|0
5|ts|INTEGER|0|strftime('%s','now')*1000|0
6|ack|INTEGER|0|0|0

[TABLE: ohlcv_30m]
0|instId|TEXT|0||1
1|ts|INTEGER|0||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

[TABLE: v_ohlcv_15m_latest]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

[TABLE: v_ohlcv_5m_latest]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

[TABLE: ohlcv_15m]
0|instId|TEXT|0||1
1|ts|INTEGER|0||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

[TABLE: ohlcv_5m]
0|instId|TEXT|0||1
1|ts|INTEGER|0||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

[TABLE: v_ohlcv_30m_latest]
0|instId|TEXT|0||0
1|ts|INTEGER|0||0
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|volume|REAL|0||0

-- VIEWS --
v_ohlcv_15m_latest|CREATE VIEW v_ohlcv_15m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_15m
WHERE ts IN (
  SELECT ts FROM ohlcv_15m AS o2
  WHERE o2.instId = ohlcv_15m.instId
  ORDER BY ts DESC
  LIMIT 150
)
v_ohlcv_30m_latest|CREATE VIEW v_ohlcv_30m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_30m
WHERE ts IN (
  SELECT ts FROM ohlcv_30m AS o2
  WHERE o2.instId = ohlcv_30m.instId
  ORDER BY ts DESC
  LIMIT 150
)
v_ohlcv_5m_latest|CREATE VIEW v_ohlcv_5m_latest AS
SELECT instId, ts, open, high, low, close, volume
FROM ohlcv_5m
WHERE ts IN (
  SELECT ts FROM ohlcv_5m AS o2
  WHERE o2.instId = ohlcv_5m.instId
  ORDER BY ts DESC
  LIMIT 150
)

-- INDEXES --
idx_bus_out_ack|CREATE INDEX idx_bus_out_ack ON bus_out(ack)
idx_bus_out_ts|CREATE INDEX idx_bus_out_ts  ON bus_out(ts)
sqlite_autoindex_ohlcv_15m_1|
sqlite_autoindex_ohlcv_30m_1|
sqlite_autoindex_ohlcv_5m_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/ob.db -----
-- TABLES --
feat_1m   feat_3m   feat_5m   ohlcv_1m  ohlcv_3m  ohlcv_5m

[TABLE: feat_1m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

[TABLE: feat_3m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

[TABLE: feat_5m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|0||0
3|high|REAL|0||0
4|low|REAL|0||0
5|close|REAL|0||0
6|vol|REAL|0||0

[TABLE: ohlcv_1m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0

[TABLE: ohlcv_3m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0

[TABLE: ohlcv_5m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|o|REAL|0||0
3|h|REAL|0||0
4|l|REAL|0||0
5|c|REAL|0||0
6|v|REAL|0||0

-- VIEWS --

-- INDEXES --
sqlite_autoindex_feat_1m_1|
sqlite_autoindex_feat_3m_1|
sqlite_autoindex_feat_5m_1|
sqlite_autoindex_ohlcv_1m_1|
sqlite_autoindex_ohlcv_3m_1|
sqlite_autoindex_ohlcv_5m_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/opener.db -----
-- TABLES --
trades_opener      v_opener_for_gest

[TABLE: trades_opener]
0|uid|TEXT|0||1
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|entry|REAL|0||0
4|qty|REAL|0||0
5|lev|REAL|0||0
6|margin|REAL|0||0
7|sl|REAL|0||0
8|tp|REAL|0||0
9|ts_open|INTEGER|0||0
10|status|TEXT|0||0

[TABLE: v_opener_for_gest]
0|uid|TEXT|0||0
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|entry|REAL|0||0
4|qty|REAL|0||0
5|lev|REAL|0||0
6|margin|REAL|0||0
7|sl|REAL|0||0
8|tp|REAL|0||0
9|ts_open|INTEGER|0||0

-- VIEWS --
v_opener_for_gest|CREATE VIEW v_opener_for_gest AS
SELECT
    uid,
    instId,
    side,
    entry,
    qty,
    lev,
    margin,
    sl,
    tp,
    ts_open
FROM trades_opener
WHERE status='opened'
ORDER BY ts_open DESC

-- INDEXES --
sqlite_autoindex_trades_opener_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/recorder.db -----
-- TABLES --
trades_record        v_recorder_for_gest

[TABLE: trades_record]
0|uid|TEXT|0||1
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|reason|TEXT|0||0
4|ctx|TEXT|0||0
5|score_A|REAL|0||0
6|score_B|REAL|0||0
7|price_signal|REAL|0||0
8|atr_signal|REAL|0||0
9|ts_signal|INTEGER|0||0
10|entry|REAL|0||0
11|qty|REAL|0||0
12|lev|REAL|0||0
13|margin|REAL|0||0
14|sl_init|REAL|0||0
15|tp_init|REAL|0||0
16|ts_open|INTEGER|0||0
17|sl_final|REAL|0||0
18|tp_final|REAL|0||0
19|be_triggered|INTEGER|0||0
20|pyramided|INTEGER|0||0
21|ts_follow|INTEGER|0||0
22|price_close|REAL|0||0
23|pnl_usdt|REAL|0||0
24|pnl_pct|REAL|0||0
25|ts_close|INTEGER|0||0
26|reason_close|TEXT|0||0
27|ts_recorded|INTEGER|0||0

[TABLE: v_recorder_for_gest]
0|uid|TEXT|0||0
1|ts_recorded|INTEGER|0||0

-- VIEWS --
v_recorder_for_gest|CREATE VIEW v_recorder_for_gest AS
SELECT
    uid,
    ts_recorded
FROM trades_record
ORDER BY ts_recorded DESC

-- INDEXES --
sqlite_autoindex_trades_record_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/signals.db -----
-- TABLES --
signals     v_for_gest

[TABLE: signals]
0|trade_uid|TEXT|0||1
1|instId|TEXT|1||0
2|side|TEXT|1||0
3|reason|TEXT|1||0
4|ctx|TEXT|0||0
5|score_A|REAL|0||0
6|score_B|REAL|0||0
7|price|REAL|1||0
8|atr|REAL|0||0
9|ts_signal|INTEGER|1||0
10|ts_local|TEXT|0||0
11|hms|TEXT|0||0

[TABLE: v_for_gest]
0|uid|TEXT|0||0
1|instId|TEXT|0||0
2|side|TEXT|0||0
3|reason|TEXT|0||0
4|ctx|TEXT|0||0
5|score_A|REAL|0||0
6|score_B|REAL|0||0
7|price_signal|REAL|0||0
8|atr_signal|REAL|0||0
9|ts_signal|INTEGER|0||0
10|ts_local|TEXT|0||0
11|hms|TEXT|0||0

-- VIEWS --
v_for_gest|CREATE VIEW v_for_gest AS
SELECT
    trade_uid AS uid,
    instId,
    side,
    reason,
    ctx,
    score_A,
    score_B,
    price AS price_signal,
    atr AS atr_signal,
    ts_signal,
    ts_local,
    hms
FROM signals
ORDER BY ts_signal DESC

-- INDEXES --
sqlite_autoindex_signals_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/t.db -----
-- TABLES --
ticks           v_ticks_latest

[TABLE: ticks]
0|instId|TEXT|0||1
1|lastPr|REAL|0||0
2|ts|INTEGER|0||0
3|ts_ms|INTEGER|0||0

[TABLE: v_ticks_latest]
0|instId|TEXT|0||0
1|lastPr|REAL|0||0
2|ts|INTEGER|0||0
3|hms||0||0
4|age_hms||0||0

-- VIEWS --
v_ticks_latest|CREATE VIEW v_ticks_latest AS
SELECT
  t.instId,
  t.lastPr,
  t.ts,
  strftime('%H:%M:%S', t.ts/1000, 'unixepoch','localtime') AS hms,
  printf('%02d:%02d:%02d',
     ((strftime('%s','now')*1000 - t.ts)/1000)/3600,
     (((strftime('%s','now')*1000 - t.ts)/1000)%3600)/60,
     ((strftime('%s','now')*1000 - t.ts)/1000)%60
  ) AS age_hms
FROM ticks t
ORDER BY t.instId

-- INDEXES --
sqlite_autoindex_ticks_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/triggers.db -----
-- TABLES --
trig_state         triggers           v_triggers_latest

[TABLE: trig_state]
0|instId|TEXT|0||1
1|reason|TEXT|0||0
2|side|TEXT|0||0
3|score|REAL|0||0
4|ts|INTEGER|0||0

[TABLE: triggers]
0|id|INTEGER|0||1
1|instId|TEXT|1||0
2|tf|TEXT|1||0
3|side|TEXT|1||0
4|reason|TEXT|1||0
5|score|REAL|1|0|0
6|ctx|TEXT|0||0
7|confidence|REAL|1|0|0
8|price|REAL|0||0
9|atr|REAL|0||0
10|ts|INTEGER|1||0
11|consumed|INTEGER|0|0|0

[TABLE: v_triggers_latest]
0|instId|TEXT|0||0
1|side|TEXT|0||0
2|reason|TEXT|0||0
3|score_B|REAL|0||0
4|ctx|TEXT|0||0
5|price|REAL|0||0
6|atr|REAL|0||0
7|ts_trigger|INTEGER|0||0

-- VIEWS --
v_triggers_latest|CREATE VIEW v_triggers_latest AS
WITH last_ts AS (
    SELECT instId, MAX(ts) AS ts_last
    FROM triggers
    GROUP BY instId
)
SELECT
    t.instId,
    t.side,
    t.reason,
    t.score   AS score_B,
    t.ctx,
    t.price,
    t.atr,
    t.ts      AS ts_trigger
FROM triggers t
JOIN last_ts lt
  ON lt.instId = t.instId AND lt.ts_last = t.ts
ORDER BY t.ts DESC

-- INDEXES --
idx_trig_consumed|CREATE INDEX idx_trig_consumed ON triggers(consumed)
idx_trig_instId|CREATE INDEX idx_trig_instId   ON triggers(instId)
idx_trig_tf|CREATE INDEX idx_trig_tf       ON triggers(tf)
idx_trig_ts|CREATE INDEX idx_trig_ts       ON triggers(ts)
sqlite_autoindex_trig_state_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/u.db -----
-- TABLES --
bus_out   universe

[TABLE: bus_out]
0|id|INTEGER|0||1
1|src|TEXT|0||0
2|instId|TEXT|0||0
3|event|TEXT|0||0
4|payload|TEXT|0|'{}'|0
5|ts|INTEGER|0|strftime('%s','now')*1000|0
6|ack|INTEGER|0|0|0

[TABLE: universe]
0|instId|TEXT|0||1
1|score_U|REAL|0|0|0
2|active|INTEGER|0|1|0

-- VIEWS --

-- INDEXES --
idx_bus_out_ack|CREATE INDEX idx_bus_out_ack ON bus_out(ack)
idx_bus_out_ts|CREATE INDEX idx_bus_out_ts  ON bus_out(ts)
sqlite_autoindex_universe_1|

-- TRIGGERS --


----- DATABASE: /opt/scalp/project/data/ws.db -----
-- TABLES --
ws_ohlcv_15m  ws_ohlcv_30m  ws_ohlcv_5m 

[TABLE: ws_ohlcv_15m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|1||0
3|high|REAL|1||0
4|low|REAL|1||0
5|close|REAL|1||0
6|volume|REAL|1||0

[TABLE: ws_ohlcv_30m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|1||0
3|high|REAL|1||0
4|low|REAL|1||0
5|close|REAL|1||0
6|volume|REAL|1||0

[TABLE: ws_ohlcv_5m]
0|instId|TEXT|1||1
1|ts|INTEGER|1||2
2|open|REAL|1||0
3|high|REAL|1||0
4|low|REAL|1||0
5|close|REAL|1||0
6|volume|REAL|1||0

-- VIEWS --

-- INDEXES --
idx_ws15_inst_ts|CREATE INDEX idx_ws15_inst_ts ON ws_ohlcv_15m(instId, ts)
idx_ws30_inst_ts|CREATE INDEX idx_ws30_inst_ts ON ws_ohlcv_30m(instId, ts)
idx_ws5_inst_ts|CREATE INDEX idx_ws5_inst_ts  ON ws_ohlcv_5m(instId, ts)
sqlite_autoindex_ws_ohlcv_15m_1|
sqlite_autoindex_ws_ohlcv_30m_1|
sqlite_autoindex_ws_ohlcv_5m_1|

-- TRIGGERS --

